## 时间复杂度和空间复杂度

1，常数操作，固定时间的操作，执行时间和数据量无关

2，时间复杂度，一个和数据量有关、只要高阶项、不要低阶项、不要常数项的操作次数表达式

3，严格固定流程的算法，一定强调最差情况！比如插入排序

4，算法流程上利用随机行为作为重要部分的，要看平均或者期望的时间复杂度，因为最差的时间复杂度无意义

用生成相邻值不同的数组来说明

5，算法流程上利用随机行为作为重要部分的，还有随机快速排序

也只在乎平均或者期望的时间复杂度，因为最差的时间复杂度无意义

6，时间复杂度的内涵：描述算法运行时间和数据量大小的关系，而且当数据量很大很大时，这种关系相当的本质，并且排除了低阶项、常数时间的干扰

7，空间复杂度，强调额外空间；常数项时间，放弃理论分析、选择用实验来确定，因为不同常数操作的时间不同

8，什么叫最优解，先满足时间复杂度最优，然后尽量少用空间的解

9，时间复杂度的均摊，用动态数组的扩容来说明（等比数列、均摊的意义）

并查集、单调队列、单调栈、哈希表等结构，

10，不要用代码结构来判断时间复杂度，比如只有一个while循环的冒泡排序，其实时间复杂度O(N^2)

11，不要用代码结构来判断时间复杂度，比如：N/1 + N/2 + N/3 + … + N/N，这个流程的时间复杂度是O(N * logN)，著名的调和级数

12，时间复杂度只能是对算法流程充分理解才能分析出来，而不是简单的看代码结构！这是一个常见的错误！

甚至有些算法的实现用了多层循环嵌套，但时间复杂度是O(N)的。在

13，常见复杂度一览：

O(1) O(logN) O(N) O(N*logN) O(N^2) … O(N^k) O(2^N) … O(k^N) … O(N!)

14，时间复杂度非常重要，可以直接判断某个方法能不能通过一个题目，根据数据量猜解法。

| 复杂度     | 适用范围 (n) | 适用于                   |
| ---------- | ------------ | ------------------------ |
| O(log⁡n)    | n≤10的18次方 | 二分查找、堆操作         |
| O(sqrt{n}) | n≤10的10次方 | 质因数分解、根号优化算法 |
| O(n)       | n≤10的8次方  | 线性遍历、单调栈、并查集 |
| O(nlog⁡n)   | n≤10的6次方  | 排序、树状数组、线段树   |
| O(n^2)     | n≤10的4次方  | 暴力枚举、动态规划       |
| O(n^3)     | n≤500        | Floyd、矩阵乘法          |
| O(2^n)     | n≤20         | 状态压缩 DP、回溯        |
| O(n!)      | n≤10         | 全排列搜索               |



## **递归和master公式**

1）从思想上理解递归：对于新手来说，递归去画调用图是非常重要的，有利于分析递归

2）从实际上理解递归：递归不是玄学，底层是利用系统栈来实现的

3）任何递归函数都一定可以改成非递归，不用系统帮你压栈（系统栈空间），自己压栈呗（内存空间）

4）递归改成非递归的必要性：

​    a. 工程上几乎一定要改，除非确定数据量再大递归也一定不深，归并排序、快速排序、线段树、很多的平衡树等，后面都讲

​    b. 算法笔试或者比赛中（能通过就不改）

5）master公式

​    a. 所有子问题规模相同的递归才能用master公式，T(n) = a * T(n/b) + O(n^c)，a、b、c都是常数

​    b. 如果log(b,a)  < c，复杂度为：O(n^c)

​    c. 如果log(b,a)  > c，复杂度为：O(n^log(b,a))

​    d. 如果log(b,a) == c，复杂度为：O(n^c * logn)

6）一个补充

​    T(n) = 2*T(n/2) + O(n*logn)，时间复杂度是O(n * ((logn)的平方))，证明过程比较复杂，记住即可



## 对数器

**非常重要的自我验证技巧**

对数器的试用场景

- 你在网上找到了某个公司的面试题，你想了好久，感觉自己会做，但是你找不到在线测试，你好心烦..

- 你和朋友交流面试题，你想了好久，感觉自己会做，但是你找不到在线测试，你好心烦..

- 你在网上做笔试，但是前几个测试用例都过了，突然一个巨大无比数据量来了，结果你的代码报错了，如此大的数据量根本看不出哪错了，甚至有的根本不提示哪个例子错了，怎么debug？你好心烦…

  对数器的实现

  1，你想要测的方法a（最优解）

  2，实现复杂度不好但是容易实现的方法b（暴力解）

  3，实现一个随机样本产生器（长度也随机、值也随机）

  4，把方法a和方法b跑相同的输入样本，看看得到的结果是否一样

  5，如果有一个随机样本使得比对结果不一致，打印这个出错的样本进行人工干预，改对方法a和方法b

  6，当样本数量很多时比对测试依然正确，可以确定方法a（最优解）已经正确。 

  关键是第5步，找到一个数据量小的错误样本，便于你去带入debug

  然后把错误例子带入代码一步一步排查

  Print大法、断点技术都可以

  对数器的门槛其实是比较高的，因为往往需要在两种不同思路下实现功能相同的两个方法，暴力一个、想象中的最优解是另一个。

  以后的很多题目都会用到对数器，几乎可以验证任何方法，尤其在验证贪心、观察规律方面很有用

  到时候会丰富很多对数器的实战用法，这里只是一个简单易懂的示例

  

## **对数器打表找规律的技巧**

对数器打表找规律的使用场景 : 输入参数是简单类型，返回值也是简单类型

对数器打表找规律的过程

1) 可以用最暴力的实现求入参不大情况下的答案，往往只需要最基本的递归能力

2) 打印入参不大情况下的答案，然后观察规律

3) 把规律变成代码，就是最优解了



## **二分搜索**

1）在有序数组中确定num存在还是不存在

2）在有序数组中找>=num的最左位置

3）在有序数组中找<=num的最右位置

4）二分搜索不一定发生在有序数组上（比如寻找峰值问题）

如果数组长度为n，那么二分搜索搜索次数是log n次，以2为底

##### 寻找峰值

峰值元素是指其值严格大于左右相邻值的元素

给你一个整数数组 nums，已知任何两个相邻的值都不相等

找到峰值元素并返回其索引

数组可能包含多个峰值，在这种情况下，返回 任何一个峰值 所在位置即可。

你可以假设 nums[-1] = nums[n] = 无穷小

你必须实现时间复杂度为 O(log n) 的算法来解决此问题。

测试链接 : https://leetcode.cn/problems/find-peak-element/

```c++
// 保证arr有序，才能用这个方法
bool exist(const vector<int>& arr, int num) {
    if (arr.empty()) {
        return false;
    }
    int l = 0, r = arr.size() - 1, m = 0;
    while (l <= r) {
        m = (l + r) / 2;
        if (arr[m] == num) {
            return true;
        } else if (arr[m] > num) {
            r = m - 1;
        } else {
            l = m + 1;
        }
    }
    return false;
}
// 保证arr有序，才能用这个方法
// 有序数组中找>=num的最左位置
int findLeft(const vector<int>& arr, int num) {
    int l = 0, r = arr.size() - 1, m = 0;
    int ans = -1;
    while (l <= r) {
        m = l + ((r - l) >> 1);  // 计算中点
        if (arr[m] >= num) {
            ans = m;
            r = m - 1;  // 继续在左半边查找
        } else {
            l = m + 1;  // 继续在右半边查找
        }
    }
    return ans;
}
// 保证arr有序，才能用这个方法
// 有序数组中找<=num的最右位置
int findRight(const vector<int>& arr, int num) {
    int l = 0, r = arr.size() - 1, m = 0;
    int ans = -1;
    while (l <= r) {
        m = l + ((r - l) >> 1);  // 计算中点
        if (arr[m] <= num) {
            ans = m;
            l = m + 1;  // 继续在右半边查找
        } else {
            r = m - 1;  // 继续在左半边查找
        }
    }
    return ans;
}
/ 峰值元素是指其值严格大于左右相邻值的元素
// 给你一个整数数组 nums，已知任何两个相邻的值都不相等
// 找到峰值元素并返回其索引
// 数组可能包含多个峰值，在这种情况下，返回 任何一个峰值 所在位置即可。
// 你可以假设 nums[-1] = nums[n] = 无穷小
// 你必须实现时间复杂度为 O(log n) 的算法来解决此问题。
class Solution {
public:
    static int findPeakElement(const vector<int>& arr) {
        int n = arr.size();
        if (n == 1) {
            return 0;
        }
        if (arr[0] > arr[1]) {
            return 0;
        }
        if (arr[n - 1] > arr[n - 2]) {
            return n - 1;
        }
        
        int l = 1, r = n - 2, m = 0, ans = -1;
        while (l <= r) {
            m = l + (r - l) / 2;  // 计算中点
            if (arr[m - 1] > arr[m]) {
                r = m - 1;  // 在左半边查找
            } else if (arr[m] < arr[m + 1]) {
                l = m + 1;  // 在右半边查找
            } else {
                ans = m;  // 找到峰值
                break;
            }
        }
        return ans;
    }
};
```



## 二分答案

1）估计 最终答案可能的范围 是什么，可以定的粗略，反正二分不了几次

2）分析 问题的答案 和 给定条件 之间的 单调性，大部分时候只需要用到 自然智慧

3）建立一个f函数，当答案固定的情况下，判断 给定的条件是否达标

4）在 最终答案可能的范围上不断二分搜索，每次用f函数判断，直到二分结束，找到最合适的答案

核心点：分析单调性、建立f函数

##### 计算等位时间(大厂面试题1)

给定一个数组arr长度为n，表示n个服务员，每服务一个客人的时间

给定一个正数m，表示有m个人等位，如果你是刚来的人，每个客人都遵循有空位就上的原则

请问你需要等多久？

假设m远远大于n，比如n <= 10^3, m <= 10^9，该怎么做是最优解？

```c++
#include <iostream>
#include <vector>
#include <queue>
#include <algorithm>
#include <climits>
#include <cstdlib>
using namespace std;

// 堆模拟 - 验证方法 (非最优解，时间复杂度O(m * log(n)))
int waitingTime1(const vector<int>& arr, int m) {
    // 优先队列，按第一个元素升序排列
    priority_queue<pair<int, int>, vector<pair<int, int>>, greater<pair<int, int>>> heap;
    int n = arr.size();
    for (int i = 0; i < n; ++i) {
        heap.push({0, arr[i]}); // {醒来时间, 服务一个客人需要的时间}
    }
    for (int i = 0; i < m; ++i) {
        auto cur = heap.top();
        heap.pop();
        cur.first += cur.second;
        heap.push(cur);
    }
    return heap.top().first;
}

// 二分答案法 - 最优解
int waitingTime2(const vector<int>& arr, int w) {
    int minTime = INT_MAX;
    for (int x : arr) {
        minTime = min(minTime, x);
    }
    int ans = 0;
    int l = 0, r = minTime * w;
    while (l <= r) {
        int m = l + ((r - l) >> 1);
        if (f(arr, m) >= w + 1) {
            ans = m;
            r = m - 1;
        } else {
            l = m + 1;
        }
    }
    return ans;
}

// 如果每个服务员工作 time 分钟，可以接待几位客人（结束和开始的客人都算）
int f(const vector<int>& arr, int time) {
    int ans = 0;
    for (int num : arr) {
        ans += (time / num) + 1;
    }
    return ans;
}

// 对数器测试
vector<int> randomArray(int n, int v) {
    vector<int> arr(n);
    for (int i = 0; i < n; ++i) {
        arr[i] = rand() % v + 1;
    }
    return arr;
}

int main() {
    cout << "\u6d4b\u8bd5\u5f00\u59cb" << endl;
    int N = 50;
    int V = 30;
    int M = 3000;
    int testTime = 20000;
    for (int i = 0; i < testTime; ++i) {
        int n = rand() % N + 1;
        vector<int> arr = randomArray(n, V);
        int m = rand() % M;
        int ans1 = waitingTime1(arr, m);
        int ans2 = waitingTime2(arr, m);
        if (ans1 != ans2) {
            cout << "\u51fa\u9519\u4e86!" << endl;
        }
    }
    cout << "\u6d4b\u8bd5\u7ed3\u675f" << endl;
    return 0;
}

```

##### 刀砍毒杀怪兽问题(大厂面试题2)

怪兽的初始血量是一个整数hp，给出每一回合刀砍和毒杀的数值cuts和poisons

第i回合如果用刀砍，怪兽在这回合会直接损失cuts[i]的血，不再有后续效果

第i回合如果用毒杀，怪兽在这回合不会损失血量，但是之后每回合都损失poisons[i]的血量

并且你选择的所有毒杀效果，在之后的回合会叠加

两个数组cuts、poisons，长度都是n，代表你一共可以进行n回合

每一回合你只能选择刀砍或者毒杀中的一个动作

如果你在n个回合内没有直接杀死怪兽，意味着你已经无法有新的行动了

但是怪兽如果有中毒效果的话，那么怪兽依然会不停扣血，直到血量耗尽的那回合死掉

返回至少多少回合怪兽会死掉

数据范围 : 1<=n<=10^5；1<=hp<=10^9；1<=cuts[i]、poisons[i]<=10^9

```c++
#include <bits/stdc++.h>
using namespace std;
// 判断是否可以在 limit 回合内杀死怪兽
bool f(vector<int>& cuts, vector<int>& poisons, long long hp, int limit) {
    int n = min((int)cuts.size(), limit);
    for (int i = 0, j = 1; i < n; i++, j++) {
        hp -= max((long long)cuts[i], (long long)(limit - j) * poisons[i]);
        if (hp <= 0) {
            return true;
        }
    }
    return false;
}

// 二分答案法（最优解）
int fast2(vector<int>& cuts, vector<int>& poisons, int hp) {
    int ans = INT_MAX;
    int l = 1, r = hp + 1, m;
    while (l <= r) {
        m = l + ((r - l) >> 1);
        if (f(cuts, poisons, hp, m)) {
            ans = m;
            r = m - 1;
        } else {
            l = m + 1;
        }
    }
    return ans;
}
```

##### 其它例题

```c++
#include <iostream>
#include <vector>
#include <algorithm>
#include <cmath>
// 爱吃香蕉的珂珂
// 珂珂喜欢吃香蕉。这里有 n 堆香蕉，第 i 堆中有 piles[i] 根香蕉
// 警卫已经离开了，将在 h 小时后回来。
// 珂珂可以决定她吃香蕉的速度 k （单位：根/小时)
// 每个小时，她将会选择一堆香蕉，从中吃掉 k 根
// 如果这堆香蕉少于 k 根，她将吃掉这堆的所有香蕉，然后这一小时内不会再吃更多的香蕉
// 珂珂喜欢慢慢吃，但仍然想在警卫回来前吃掉所有的香蕉。
// 返回她可以在 h 小时内吃掉所有香蕉的最小速度 k（k 为整数）
// 测试链接 : https://leetcode.cn/problems/koko-eating-bananas/
using namespace std;
// 计算在给定速度下，吃完所有香蕉所需的时间
long calculateHours(const vector<int>& piles, int speed) {
    long hours = 0;
    for (int pile : piles) {
        // (a + b - 1) / b 实现向上取整
        hours += (pile + speed - 1) / speed;
    }
    return hours;
}

// 寻找最小的吃香蕉速度
int minEatingSpeed(vector<int>& piles, int h) {
    int l = 1; // 最小速度
    int r = *max_element(piles.begin(), piles.end()); // 最大速度
    int ans = 0;

    while (l <= r) {
        int m = l + (r - l) / 2; // 防止溢出计算中间值
        if (calculateHours(piles, m) <= h) {
            // 如果当前速度可以在 h 小时内吃完所有香蕉
            ans = m; // 记录答案
            r = m - 1; // 尝试更小的速度
        } else {
            // 当前速度不满足条件，增加速度
            l = m + 1;
        }
    }
    return ans;
}

int main() {
    // 示例输入
    vector<int> piles = {3, 6, 7, 11};
    int h = 8;

    // 计算最小吃香蕉速度
    int result = minEatingSpeed(piles, h);
    
    // 输出结果
    cout << "Minimum eating speed: " << result << endl;

    return 0;
}
```

```c++
#include <iostream>
#include <vector>
#include <climits> // for INT_MAX
// 分割数组的最大值(画匠问题)
// 给定一个非负整数数组 nums 和一个整数 m
// 你需要将这个数组分成 m 个非空的连续子数组。
// 设计一个算法使得这 m 个子数组各自和的最大值最小。
// 测试链接 : https://leetcode.cn/problems/split-array-largest-sum/
class SplitArrayLargestSum {
public:
    // 时间复杂度 O(n * log(sum))，额外空间复杂度 O(1)
    static int splitArray(const std::vector<int>& nums, int k) {
        long long sum = 0;
        for (int num : nums) {
            sum += num;
        }

        long long ans = 0;
        // [0, sum] 二分
        long long l = 0, r = sum;
        while (l <= r) {
            long long m = l + ((r - l) >> 1);
            // 必须让数组每一部分的累加和 <= m，请问划分成几个部分才够!
            long long need = f(nums, m);
            if (need <= k) {
                // 达标
                ans = m;
                r = m - 1;
            } else {
                l = m + 1;
            }
        }
        return static_cast<int>(ans);
    }

private:
    // 必须让数组 arr 每一部分的累加和 <= limit，请问划分成几个部分才够!
    // 返回需要的部分数量
    static int f(const std::vector<int>& arr, long long limit) {
        int parts = 1;
        long long sum = 0;
        for (int num : arr) {
            if (num > limit) {
                return INT_MAX;
            }
            if (sum + num > limit) {
                parts++;
                sum = num;
            } else {
                sum += num;
            }
        }
        return parts;
    }
};

int main() {
    std::vector<int> nums = {7, 2, 5, 10, 8};
    int k = 2;

    int result = SplitArrayLargestSum::splitArray(nums, k);
    std::cout << "The minimized largest sum is: " << result << std::endl;

    return 0;
}
```

**机器人跳跃问题**

机器人正在玩一个古老的基于DOS的游戏，游戏中有N+1座建筑，从0到N编号，从左到右排列

编号为0的建筑高度为0个单位，编号为i的建筑的高度为H(i)个单位

起初， 机器人在编号为0的建筑处

每一步，它跳到下一个（右边）建筑。假设机器人在第k个建筑，且它现在的能量值是E

下一步它将跳到第个k+1建筑

它将会得到或者失去正比于与H(k+1)与E之差的能量

如果 H(k+1) > E 那么机器人就失去H(k+1)-E的能量值

否则它将得到E-H(k+1)的能量值

游戏目标是到达第个N建筑，在这个过程中，能量值不能为负数个单位

现在的问题是机器人以多少能量值开始游戏，才可以保证成功完成游戏

测试链接 : https://www.nowcoder.com/practice/7037a3d57bbd4336856b8e16a9cafd71>

**找出第K小的数对距离**

数对 (a,b) 由整数 a 和 b 组成，其数对距离定义为 a 和 b 的绝对差值。

给你一个整数数组 nums 和一个整数 k

数对由 nums[i] 和 nums[j] 组成且满足 0 <= i < j < nums.length

返回 所有数对距离中 第 k 小的数对距离。

测试链接 : <https://leetcode.cn/problems/find-k-th-smallest-pair-distance/>

**同时运行N台电脑的最长时间**

你有 n 台电脑。给你整数 n 和一个下标从 0 开始的整数数组 batteries

其中第 i 个电池可以让一台电脑 运行 batteries[i] 分钟

你想使用这些电池让 全部 n 台电脑 同时 运行。

一开始，你可以给每台电脑连接 至多一个电池

然后在任意整数时刻，你都可以将一台电脑与它的电池断开连接，

并连接另一个电池，你可以进行这个操作 任意次

新连接的电池可以是一个全新的电池，也可以是别的电脑用过的电池

断开连接和连接新的电池不会花费任何时间。注意，你不能给电池充电。

请你返回你可以让 n 台电脑同时运行的 最长 分钟数。

测试链接 : https://leetcode.cn/problems/maximum-running-time-of-n-computers



## 同余原理

求最大公约数

1) 欧几里得算法的过程 : 辗转相除法

gcd(a, b) { return b == 0 ? a : gcd(b, a % b); }

2) 正确性的证明过程见代码注释部分，我润色的证明过程非常好懂，不过直接记忆过程即可

3) 求gcd(a,b)，其中a>b，时间复杂度为O((log a)的3次方)，时间复杂度证明略，这个复杂度足够好了

4) 简单转化就可以求最小公倍数

同余原理

1) 加法、乘法每一步计算完后直接取模，减法则为(a-b+m)%m

2) 要确保过程中不溢出，所以往往乘法运算的用long类型做中间变量

##### 辗转相除法

```c++
#include <iostream>
#include <algorithm>

using namespace std;
// 证明辗转相除法就是证明如下关系：
// gcd(a, b) = gcd(b, a % b)
// 假设a % b = r，即需要证明的关系为：gcd(a, b) = gcd(b, r)
// 证明过程：
// 因为a % b = r，所以如下两个等式必然成立
// 1) a = b * q + r，q为0、1、2、3....中的一个整数
// 2) r = a − b * q，q为0、1、2、3....中的一个整数
// 假设u是a和b的公因子，则有: a = s * u, b = t * u
// 把a和b带入2)得到，r = s * u - t * u * q = (s - t * q) * u
// 这说明 : u如果是a和b的公因子，那么u也是r的因子
// 假设v是b和r的公因子，则有: b = x * v, r = y * v
// 把b和r带入1)得到，a = x * v * q + y * v = (x * q + y) * v
// 这说明 : v如果是b和r的公因子，那么v也是a的公因子
// 综上，a和b的每一个公因子 也是 b和r的一个公因子，反之亦然
// 所以，a和b的全体公因子集合 = b和r的全体公因子集合
// 即gcd(a, b) = gcd(b, r)
// 证明结束
class GcdAndLcm {
public:
    // 计算最大公约数 (GCD) 的函数
    // 使用辗转相除法 (欧几里得算法)
    static long gcd(long a, long b) {
        return b == 0 ? a : gcd(b, a % b);
    }

    // 计算最小公倍数 (LCM) 的函数
    // 利用公式: lcm(a, b) = (a / gcd(a, b)) * b
    static long lcm(long a, long b) {
        return (a / gcd(a, b)) * b;
    }
};

int main() {
    // 测试数据
    long a = 12, b = 18;

    // 输出最大公约数和最小公倍数
    cout << "GCD of " << a << " and " << b << " is: " << GcdAndLcm::gcd(a, b) << endl;
    cout << "LCM of " << a << " and " << b << " is: " << GcdAndLcm::lcm(a, b) << endl;

    return 0;
}
```

```c++
#include <iostream>
#include <algorithm>
#include <cmath>
// 一个正整数如果能被 a 或 b 整除，那么它是神奇的。
// 给定三个整数 n , a , b ，返回第 n 个神奇的数字。
// 因为答案可能很大，所以返回答案 对 1000000007 取模
// 测试链接 : https://leetcode.cn/problems/nth-magical-number/
using namespace std;

class GcdAndLcm {
public:
    // 计算最大公约数 (GCD) 的函数
    // 使用辗转相除法 (欧几里得算法)
    static long gcd(long a, long b) {
        return b == 0 ? a : gcd(b, a % b);
    }

    // 计算最小公倍数 (LCM) 的函数
    // 利用公式: lcm(a, b) = (a / gcd(a, b)) * b
    static long lcm(long a, long b) {
        return (a / gcd(a, b)) * b;
    }
};

class NthMagicalNumber {
public:
    // 找到第 n 个神奇数字
    static int nthMagicalNumber(int n, int a, int b) {
        const int MOD = 1000000007;
        long lcm_ab = GcdAndLcm::lcm(a, b);
        long ans = 0;

        // 二分查找范围
        long left = 0, right = (long)n * min(a, b);
        while (left <= right) {
            long mid = left + (right - left) / 2;
            // 计算当前范围内的神奇数字数量
            if (mid / a + mid / b - mid / lcm_ab >= n) {
                ans = mid;
                right = mid - 1;
            } else {
                left = mid + 1;
            }
        }
        return (int)(ans % MOD);
    }
};

int main() {
    // 测试数据
    int n = 5, a = 2, b = 3;

    // 输出第 n 个神奇数字
    cout << "The " << n << "th magical number is: " << NthMagicalNumber::nthMagicalNumber(n, a, b) << endl;

    return 0;
}

```

##### 加法、减法、乘法的同余原理

```c++
#include <iostream>
#include <cstdlib>
#include <ctime>
using namespace std;

// 加法、减法、乘法的同余原理
// 不包括除法，因为除法必须求逆元，后续课讲述

class SameMod {
public:
    // 为了测试，生成随机数
    static long random() {
        return rand() % 1000000000; // 限制随机数的范围
    }

    // 计算 ((a + b) * (c - d) + (a * c - b * d)) % mod 的非负结果（使用大整数）
    static int f1(long a, long b, long c, long d, int mod) {
        long o1 = a % mod; // a % mod
        long o2 = b % mod; // b % mod
        long o3 = c % mod; // c % mod
        long o4 = d % mod; // d % mod

        long o5 = (o1 + o2) % mod; // a + b
        long o6 = (o3 - o4 + mod) % mod; // c - d
        long o7 = (o1 * o3) % mod; // a * c
        long o8 = (o2 * o4) % mod; // b * d
        long o9 = (o5 * o6) % mod; // (a + b) * (c - d)
        long o10 = (o7 - o8 + mod) % mod; // (a * c - b * d)
        long o11 = (o9 + o10) % mod; // ((a + b) * (c - d) + (a * c - b * d))
        if (o11 < 0) {
            // 如果结果为负数，加上mod返回
            return (o11 + mod) % mod;
        } else {
            return o11;
        }
    }

    // 计算 ((a + b) * (c - d) + (a * c - b * d)) % mod 的非负结果（使用标准int类型）
    static int f2(long a, long b, long c, long d, int mod) {
        int o1 = static_cast<int>(a % mod); // a % mod
        int o2 = static_cast<int>(b % mod); // b % mod
        int o3 = static_cast<int>(c % mod); // c % mod
        int o4 = static_cast<int>(d % mod); // d % mod

        int o5 = (o1 + o2) % mod; // a + b
        int o6 = (o3 - o4 + mod) % mod; // c - d
        int o7 = static_cast<int>((static_cast<long>(o1) * o3) % mod); // a * c
        int o8 = static_cast<int>((static_cast<long>(o2) * o4) % mod); // b * d
        int o9 = static_cast<int>((static_cast<long>(o5) * o6) % mod); // (a + b) * (c - d)
        int o10 = (o7 - o8 + mod) % mod; // (a * c - b * d)
        int ans = (o9 + o10) % mod; // ((a + b) * (c - d) + (a * c - b * d)) % mod
        return ans;
    }

    // 主函数，用于测试
    static void test() {
        cout << "测试开始" << endl;
        int testTime = 100000;
        int mod = 1000000007;
        for (int i = 0; i < testTime; i++) {
            long a = random();
            long b = random();
            long c = random();
            long d = random();
            if (f1(a, b, c, d, mod) != f2(a, b, c, d, mod)) {
                cout << "出错了!" << endl;
            }
        }
        cout << "测试结束" << endl;

        cout << "===\n";
        long a = random();
        long b = random();
        long c = random();
        long d = random();
        cout << "a : " << a << endl;
        cout << "b : " << b << endl;
        cout << "c : " << c << endl;
        cout << "d : " << d << endl;
        cout << "===\n";
        cout << "f1 : " << f1(a, b, c, d, mod) << endl;
        cout << "f2 : " << f2(a, b, c, d, mod) << endl;
    }
};

int main() {
    srand(time(0)); // 设置随机种子
    SameMod::test();
    return 0;
}
```

## 排序

##### 选择排序

```c++
    // 选择排序
    void selectionSort(std::vector<int>& arr) {
        if (arr.empty() || arr.size() < 2) {
            return;
        }
        for (size_t i = 0; i < arr.size() - 1; ++i) {
            size_t minIndex = i;
            for (size_t j = i + 1; j < arr.size(); ++j) {
                if (arr[j] < arr[minIndex]) {
                    minIndex = j;
                }
            }
            std::swap(arr[i], arr[minIndex]);
        }
    }
```

##### 冒泡排序

```c++
    void bubbleSort(std::vector<int>& arr) {
        if (arr.empty() || arr.size() < 2) {
            return;
        }
        for (size_t end = arr.size() - 1; end > 0; --end) {
            for (size_t i = 0; i < end; ++i) {
                if (arr[i] > arr[i + 1]) {
                    std::swap(arr[i], arr[i + 1]);
                }
            }
        }
    }
```

##### 插入排序

```c++
    void insertionSort(std::vector<int>& arr) {
        if (arr.empty() || arr.size() < 2) {
            return;
        }
        for (size_t i = 1; i < arr.size(); ++i) {
            for (int j = i - 1; j >= 0 && arr[j] > arr[j + 1]; --j) {
                std::swap(arr[j], arr[j + 1]);
            }
        }
    }
```



## 队列和栈

```c++
#include <queue>
#include <stack>
#include <vector>
#include <iostream>
using namespace std;

// Queue1: 使用STL的queue实现
class Queue1 {
public:
    queue<int> q;
    bool isEmpty() {
        return q.empty();
    }
    void offer(int num) {
        q.push(num);
    }
    int poll() {
        int front = q.front();
        q.pop();
        return front;
    }
    int peek() {
        return q.front();
    }
    int size() {
        return q.size();
    }
};

// Queue2: 使用数组模拟队列
class Queue2 {
public:
    vector<int> queue;
    int l, r;
    Queue2(int n) : queue(n), l(0), r(0) {}
    bool isEmpty() {
        return l == r;
    }
    void offer(int num) {
        queue[r++] = num;
    }
    int poll() {
        return queue[l++];
    }
    int head() {
        return queue[l];
    }
    int tail() {
        return queue[r - 1];
    }
    int size() {
        return r - l;
    }
};

// Stack1: 使用STL的stack实现
class Stack1 {
public:
    stack<int> s;
    bool isEmpty() {
        return s.empty();
    }
    void push(int num) {
        s.push(num);
    }
    int pop() {
        int top = s.top();
        s.pop();
        return top;
    }
    int peek() {
        return s.top();
    }
    int size() {
        return s.size();
    }
};

// Stack2: 使用数组模拟栈
class Stack2 {
public:
    vector<int> stack;
    int size;
    Stack2(int n) : stack(n), size(0) {}
    bool isEmpty() {
        return size == 0;
    }
    void push(int num) {
        stack[size++] = num;
    }
    int pop() {
        return stack[--size];
    }
    int peek() {
        return stack[size - 1];
    }
    int getSize() {
        return size;
    }
};

// MyCircularQueue: 设计循环队列
class MyCircularQueue {
public:
    vector<int> queue;
    int l, r, size, limit;
    MyCircularQueue(int k) : queue(k), l(0), r(0), size(0), limit(k) {}

    bool enQueue(int value) {
        if (isFull()) {
            return false;
        } else {
            queue[r] = value;
            r = (r + 1) % limit;
            size++;
            return true;
        }
    }

    bool deQueue() {
        if (isEmpty()) {
            return false;
        } else {
            l = (l + 1) % limit;
            size--;
            return true;
        }
    }

    int Front() {
        return isEmpty() ? -1 : queue[l];
    }

    int Rear() {
        if (isEmpty()) {
            return -1;
        } else {
            int last = (r - 1 + limit) % limit;
            return queue[last];
        }
    }

    bool isEmpty() {
        return size == 0;
    }

    bool isFull() {
        return size == limit;
    }
};

int main() {
    // 示例使用
    Queue1 q1;
    q1.offer(1);
    q1.offer(2);
    cout << "Queue1 Peek: " << q1.peek() << endl;

    Stack1 s1;
    s1.push(3);
    s1.push(4);
    cout << "Stack1 Pop: " << s1.pop() << endl;

    MyCircularQueue cq(3);
    cq.enQueue(5);
    cq.enQueue(6);
    cout << "CircularQueue Front: " << cq.Front() << endl;

    return 0;
}

```



## 滑动窗口

关键在于找到范围和答案指标之间的单调性

```c++
#include <vector>
#include <climits>
using namespace std;
// 累加和大于等于target的最短子数组长度
// 给定一个含有 n 个正整数的数组和一个正整数 target
// 找到累加和 >= target 的长度最小的子数组并返回其长度
// 如果不存在符合条件的子数组返回0
// 测试链接 : https://leetcode.cn/problems/minimum-size-subarray-sum/
int minSubArrayLen(int target, vector<int>& nums) {
    int ans = INT_MAX; // 初始化最小长度为无穷大
    int l = 0, sum = 0; // 左指针和当前窗口的累加和

    for (int r = 0; r < nums.size(); r++) {
        sum += nums[r]; // 扩大右边界，加入当前元素
        // 收缩左边界直到不满足条件
        while (sum - nums[l] >= target) {
            sum -= nums[l++]; // 缩小窗口
        }
        // 检查当前窗口是否满足条件
        if (sum >= target) {
            ans = min(ans, r - l + 1);
        }
    }
    return ans == INT_MAX ? 0 : ans; // 如果没有找到符合条件的子数组，返回 0
}
```



## 单调队列

```c++
#include <iostream>
#include <vector>
#include <deque>
using namespace std;
// 滑动窗口最大值（单调队列经典用法模版）
// 给你一个整数数组 nums，有一个大小为 k 的滑动窗口从数组的最左侧移动到数组的最右侧
// 你只可以看到在滑动窗口内的 k 个数字。滑动窗口每次只向右移动一位。
// 返回 滑动窗口中的最大值 。
// 测试链接 : https://leetcode.cn/problems/sliding-window-maximum/
class SlidingWindowMaximum {
public:
    static vector<int> maxSlidingWindow(const vector<int>& arr, int k) {
        deque<int> dq; // 双端队列存储索引
        int n = arr.size();
        vector<int> ans;

        // 先形成长度为 k-1 的窗口
        for (int i = 0; i < k - 1; ++i) {
            // 保证队列从大到小排列
            while (!dq.empty() && arr[dq.back()] <= arr[i]) {
                dq.pop_back();
            }
            dq.push_back(i);
        }

        // 遍历滑动窗口
        for (int l = 0, r = k - 1; r < n; ++l, ++r) {
            // 当前 r 位置的数进入窗口
            while (!dq.empty() && arr[dq.back()] <= arr[r]) {
                dq.pop_back();
            }
            dq.push_back(r);
            // 收集当前窗口的最大值
            ans.push_back(arr[dq.front()]);
            // 如果 l 位置的索引已经不在窗口范围内，则移除
            if (dq.front() == l) {
                dq.pop_front();
            }
        }
        return ans;
    }
};
```



## 前缀树

##### 使用类描述实现前缀树

```c++
#include <iostream>
#include <unordered_map>
#include <string>
using namespace std;

// 使用类描述实现前缀树，提供两种实现方式：数组方式和哈希表方式。
// 数组方式的前缀树实现
class Trie1 {
private:
    struct TrieNode {
        int pass; // 记录到达该节点的路径数
        int end;  // 记录以该节点为结尾的单词数
        TrieNode* nexts[26]; // 指向子节点的指针数组，支持26个字母

        TrieNode() : pass(0), end(0) {
            for (int i = 0; i < 26; i++) {
                nexts[i] = nullptr;
            }
        }
    };

    TrieNode* root; // 根节点

public:
    Trie1() {
        root = new TrieNode();
    }

    // 插入一个单词
    void insert(const string& word) {
        TrieNode* node = root;
        node->pass++;
        for (char c : word) {
            int path = c - 'a'; // 计算路径（字符与'a'的差值）
            if (node->nexts[path] == nullptr) {
                node->nexts[path] = new TrieNode();
            }
            node = node->nexts[path];
            node->pass++;
        }
        node->end++;
    }

    // 删除一个单词
    void erase(const string& word) {
        if (countWordsEqualTo(word) > 0) {
            TrieNode* node = root;
            node->pass--;
            for (char c : word) {
                int path = c - 'a';
                if (--node->nexts[path]->pass == 0) {
                    delete node->nexts[path];
                    node->nexts[path] = nullptr;
                    return;
                }
                node = node->nexts[path];
            }
            node->end--;
        }
    }

    // 查询某个单词出现的次数
    int countWordsEqualTo(const string& word) {
        TrieNode* node = root;
        for (char c : word) {
            int path = c - 'a';
            if (node->nexts[path] == nullptr) {
                return 0;
            }
            node = node->nexts[path];
        }
        return node->end;
    }

    // 查询有多少单词以某个前缀开头
    int countWordsStartingWith(const string& prefix) {
        TrieNode* node = root;
        for (char c : prefix) {
            int path = c - 'a';
            if (node->nexts[path] == nullptr) {
                return 0;
            }
            node = node->nexts[path];
        }
        return node->pass;
    }
};

// 哈希表方式的前缀树实现
class Trie2 {
private:
    struct TrieNode {
        int pass; // 记录到达该节点的路径数
        int end;  // 记录以该节点为结尾的单词数
        unordered_map<char, TrieNode*> nexts; // 使用哈希表存储子节点

        TrieNode() : pass(0), end(0) {}
    };

    TrieNode* root; // 根节点

public:
    Trie2() {
        root = new TrieNode();
    }

    // 插入一个单词
    void insert(const string& word) {
        TrieNode* node = root;
        node->pass++;
        for (char c : word) {
            if (node->nexts.find(c) == node->nexts.end()) {
                node->nexts[c] = new TrieNode();
            }
            node = node->nexts[c];
            node->pass++;
        }
        node->end++;
    }

    // 删除一个单词
    void erase(const string& word) {
        if (countWordsEqualTo(word) > 0) {
            TrieNode* node = root;
            node->pass--;
            for (char c : word) {
                TrieNode* next = node->nexts[c];
                if (--next->pass == 0) {
                    node->nexts.erase(c);
                    delete next;
                    return;
                }
                node = next;
            }
            node->end--;
        }
    }

    // 查询某个单词出现的次数
    int countWordsEqualTo(const string& word) {
        TrieNode* node = root;
        for (char c : word) {
            if (node->nexts.find(c) == node->nexts.end()) {
                return 0;
            }
            node = node->nexts[c];
        }
        return node->end;
    }

    // 查询有多少单词以某个前缀开头
    int countWordsStartingWith(const string& prefix) {
        TrieNode* node = root;
        for (char c : prefix) {
            if (node->nexts.find(c) == node->nexts.end()) {
                return 0;
            }
            node = node->nexts[c];
        }
        return node->pass;
    }
};

int main() {
    // 测试数组实现
    Trie1 trie1;
    trie1.insert("apple");
    cout << "Count of 'apple': " << trie1.countWordsEqualTo("apple") << endl;
    trie1.erase("apple");
    cout << "Count of 'apple' after erase: " << trie1.countWordsEqualTo("apple") << endl;

    // 测试哈希表实现
    Trie2 trie2;
    trie2.insert("banana");
    cout << "Count of 'banana': " << trie2.countWordsEqualTo("banana") << endl;
    trie2.erase("banana");
    cout << "Count of 'banana' after erase: " << trie2.countWordsEqualTo("banana") << endl;

    return 0;
}
```

##### 用固定数组实现前缀树

```c++
#include <iostream>
#include <cstring>
#include <vector>
using namespace std;

// 用固定数组实现前缀树，空间使用是静态的
// 推荐在大数据量时使用这种方法
const int MAXN = 150001; // 最大节点数，可以根据数据量调整
int tree[MAXN][26];      // 用于存储前缀树的结构，每个节点有26个子节点
int end[MAXN];           // 用于记录以某个节点为结尾的单词数量
int pass[MAXN];          // 用于记录经过某个节点的路径数量
int cnt;                 // 节点计数器

// 初始化前缀树
void build() {
    cnt = 1;
    memset(tree, 0, sizeof(tree));
    memset(end, 0, sizeof(end));
    memset(pass, 0, sizeof(pass));
}

// 插入单词
void insert(const string& word) {
    int cur = 1; // 从根节点开始
    pass[cur]++;
    for (char c : word) {
        int path = c - 'a'; // 计算路径（字符到'a'的偏移量）
        if (tree[cur][path] == 0) {
            tree[cur][path] = ++cnt; // 如果路径不存在，创建新节点
        }
        cur = tree[cur][path];
        pass[cur]++; // 更新路径计数
    }
    end[cur]++; // 更新单词结尾计数
}

// 查询单词是否存在
int search(const string& word) {
    int cur = 1; // 从根节点开始
    for (char c : word) {
        int path = c - 'a';
        if (tree[cur][path] == 0) {
            return 0; // 如果路径不存在，单词不存在
        }
        cur = tree[cur][path];
    }
    return end[cur]; // 返回单词结尾的计数
}

// 查询以某个前缀开头的单词数量
int prefixNumber(const string& prefix) {
    int cur = 1; // 从根节点开始
    for (char c : prefix) {
        int path = c - 'a';
        if (tree[cur][path] == 0) {
            return 0; // 如果路径不存在，前缀不存在
        }
        cur = tree[cur][path];
    }
    return pass[cur]; // 返回路径计数
}

// 删除单词
void deleteWord(const string& word) {
    if (search(word) > 0) { // 确保单词存在
        int cur = 1; // 从根节点开始
        for (char c : word) {
            int path = c - 'a';
            if (--pass[tree[cur][path]] == 0) { // 如果路径计数为0，删除路径
                tree[cur][path] = 0;
                return;
            }
            cur = tree[cur][path];
        }
        end[cur]--; // 减少单词结尾计数
    }
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    
    int m, op;
    string input;
    while (cin >> m) {
        build(); // 初始化前缀树
        for (int i = 0; i < m; ++i) {
            cin >> op >> input;
            if (op == 1) {
                insert(input); // 插入单词
            } else if (op == 2) {
                deleteWord(input); // 删除单词
            } else if (op == 3) {
                cout << (search(input) > 0 ? "YES" : "NO") << endl; // 查询单词是否存在
            } else if (op == 4) {
                cout << prefixNumber(input) << endl; // 查询前缀数量
            }
        }
    }
    return 0;
}
```



## **一维差分与等差数列差分**

一维差分：太简单了，没有理解难度。不支持边操作、边查询。

等差数列差分问题描述：

一开始1~n范围上的数字都是0。接下来一共有m个操作。

每次操作：l~r范围上依次加上首项s、末项e、公差d的数列

最终1~n范围上的每个数字都要正确得到

等差数列差分的过程：

每个操作调用set方法

所有操作完成后在arr上生成两遍前缀和，即调用build方法

arr里就是最终1~n范围上的每个数字

##### 等差数列差分模版

一开始1~n范围上的数字都是0，一共有m个操作，每次操作为(l,r,s,e,d)

表示在l~r范围上依次加上首项为s、末项为e、公差为d的数列

M个操作做完之后，统计1~n范围上所有数字的最大值和异或和

1 <= n <= 10^7

1 <= m <= 3 * 10^5

1 <= l <= r <= n

```c++
#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

const int MAXN = 10000005;

long long arr[MAXN];  // 用于存储数列的差分数组
int n, m;

void set(int l, int r, int s, int e, int d) {
    arr[l] += s;
    arr[l + 1] += d - s;
    arr[r + 1] -= d + e;
    arr[r + 2] += e;
}

void build() {
    // 计算差分数组的前缀和
    for (int i = 1; i <= n; i++) {
        arr[i] += arr[i - 1];
    }
    // 再次计算前缀和以处理二次累加
    for (int i = 1; i <= n; i++) {
        arr[i] += arr[i - 1];
    }
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    
    while (cin >> n >> m) {
        fill(arr, arr + MAXN, 0);  // 每次操作之前清空差分数组
        
        for (int i = 0, l, r, s, e; i < m; i++) {
            cin >> l >> r >> s >> e;
            int d = (e - s) / (r - l);  // 计算公差
            set(l, r, s, e, d);
        }
        
        build();
        
        long long max_val = 0, xor_val = 0;
        for (int i = 1; i <= n; i++) {
            max_val = max(max_val, arr[i]);
            xor_val ^= arr[i];
        }
        
        cout << xor_val << " " << max_val << "\n";
    }
    
    return 0;
}
```

##### 其它题目

1. 航班预订统计(一维差分)

   这里有 n 个航班，它们分别从 1 到 n 进行编号。

   有一份航班预订表 bookings ，

   表中第 i 条预订记录 bookings[i] = [firsti, lasti, seatsi]

   意味着在从 firsti 到 lasti 

   包含 firsti 和 lasti ）的 每个航班 上预订了 seatsi 个座位。

   请你返回一个长度为 n 的数组 answer，里面的元素是每个航班预定的座位总数。

   ```c++
   #include <vector>
   using namespace std;
   
   class CorporateFlightBookings {
   public:
       vector<int> corpFlightBookings(vector<vector<int>>& bookings, int n) {
           // 创建一个大小为 n + 2 的数组来存储差分信息
           vector<int> cnt(n + 2, 0);
           
           // 处理预定记录并应用差分
           for (const auto& book : bookings) {
               cnt[book[0]] += book[2];
               cnt[book[1] + 1] -= book[2];
           }
           
           // 计算前缀和
           for (int i = 1; i < cnt.size(); ++i) {
               cnt[i] += cnt[i - 1];
           }
           
           // 将结果从差分数组中提取出来
           vector<int> ans(n);
           for (int i = 0; i < n; ++i) {
               ans[i] = cnt[i + 1];
           }
           
           return ans;
       }
   };
   ```

2. 等差数列差分经典题目

   一群人落水后求每个位置的水位高度

   问题描述比较复杂，见测试链接

   测试链接 : <https://www.luogu.com.cn/problem/P5026>

   ```c++
   #include <iostream>
   #include <vector>
   #include <cstdio>
   using namespace std;
   
   const int MAXN = 1000001; // 湖泊最大宽度
   const int OFFSET = 30001; // 偏移量，用于防止数组越界
   const int ARRAY_SIZE = OFFSET + MAXN + OFFSET; // 数组总大小
   
   vector<int> arr(ARRAY_SIZE, 0); // 差分数组
   int n, m; // n: 落水人数, m: 湖泊宽度
   
   // 处理每个落水事件
   void fall(int v, int x) {
       // 等差数列区域1
       set(x - 3 * v + 1, x - 2 * v, 1, v, 1);
       // 等差数列区域2
       set(x - 2 * v + 1, x, v - 1, -v, -1);
       // 等差数列区域3
       set(x + 1, x + 2 * v, -v + 1, v, 1);
       // 等差数列区域4
       set(x + 2 * v + 1, x + 3 * v - 1, v - 1, 1, -1);
   }
   
   // 修改差分数组
   void set(int l, int r, int s, int e, int d) {
       // 偏移量的加持避免负数下标
       arr[l + OFFSET] += s;
       arr[l + 1 + OFFSET] += d - s;
       arr[r + 1 + OFFSET] -= d + e;
       arr[r + 2 + OFFSET] += e;
   }
   
   // 构建实际水位数组
   void build() {
       for (int i = 1; i < ARRAY_SIZE; ++i) {
           arr[i] += arr[i - 1];
       }
       for (int i = 1; i < ARRAY_SIZE; ++i) {
           arr[i] += arr[i - 1];
       }
   }
   
   int main() {
       // 输入加速
       ios::sync_with_stdio(false);
       cin.tie(nullptr);
   
       while (cin >> n >> m) {
           for (int i = 0; i < n; ++i) {
               int v, x;
               cin >> v >> x;
               fall(v, x); // 处理每次落水
           }
           build(); // 构建水位数组
           // 输出正式位置的水位（OFFSET + 1 到 OFFSET + m）
           cout << arr[OFFSET + 1];
           for (int i = 2; i <= m; ++i) {
               cout << " " << arr[OFFSET + i];
           }
           cout << "\n";
       }
   
       return 0;
   }
   ```



## 倍增算法和ST表

倍增算法和ST表

线段上有n个点，给定每个点i往右边跳1步能最远覆盖的点jump[i]

已知从任意点出发都能到达最后的点，并且在i < j时，必有jump[i] <= jump[j]

1）如何构建一张表

可以查询从任意点出发：跳1步、跳2步、跳4步、跳8步..每回最远能到达的点

也就是可以查询，从任意点i出发，任意跳(2的p次方)步，最远能到达的点

2）如何快速计算任意的两点之间，最少跳几步能到达？

构建出的表叫做ST表(Sparse Table)

构建方式：st[i][p] = st[[i][p-1]][p-1]，p代表：跳2的p次方步，表大小为n * logn

利用ST表，从x到y跳跃的过程中，先从最大步长开始，每次步长减少一半去尝试，最终得到答案

时间复杂度O(log n)

[国旗计划](https://www.luogu.com.cn/problem/P4155)

给定点的数量m，点的编号1~m，所有点围成一个环

i号点一定顺时针到达i+1号点，最终m号点顺指针回到1号点

给定n条线段，每条线段(a, b)，表示线段从点a顺时针到点b

输入数据保证所有线段可以把整个环覆盖

输入数据保证每条线段不会完全在另一条线段的内部

也就是线段之间可能有重合但一定互不包含

返回一个长度为n的结果数组ans，ans[x]表示一定选x号线段的情况下

至少选几条线段能覆盖整个环

```c++
#include <iostream>
#include <vector>
#include <algorithm>
#include <cmath>

using namespace std;

const int MAXN = 200001;  // 最大的线段数量
const int LIMIT = 18;      // 最大的2的幂次（对数最大值）
int n, m, power;           // n是线段数，m是点的数量，power是log2(n)的值
int line[MAXN * 2][3];     // 每条线段有3个属性：线段编号、左边界、右边界
int stjump[MAXN * 2][LIMIT];  // stjump[i][p]表示从第i条线段出发，跳跃2^p次能到达的最右线段
int ans[MAXN];             // 存储每个线段的结果

// 计算log2(n)，返回n最大为2的幂次的值
int log2(int n) {
    int result = 0;
    // 通过左移位判断最大的2的幂次
    while ((1 << result) <= (n >> 1)) {
        result++;
    }
    return result;
}

// 构建线段跳跃表
void build() {
    // 处理跨越的线段：如果左边界大于右边界，就把右边界加上m来模拟环状结构
    for (int i = 1; i <= n; i++) {
        if (line[i][1] > line[i][2]) {
            line[i][2] += m;
        }
    }
    // 按照线段的左边界排序线段
    sort(line + 1, line + n + 1, [](const int a[3], const int b[3]) {
        return a[1] < b[1];  // 根据左边界进行升序排序
    });
    // 复制一遍线段并处理跨越的部分
    for (int i = 1; i <= n; i++) {
        line[i + n][0] = line[i][0];
        line[i + n][1] = line[i][1] + m;
        line[i + n][2] = line[i][2] + m;
    }
    int e = n * 2;  // 扩展后的线段总数
    // 构建每条线段的跳跃表
    for (int i = 1, arrive = 1; i <= e; i++) {
        // 找到第i条线段能够跳到的最远线段
        while (arrive + 1 <= e && line[arrive + 1][1] <= line[i][2]) {
            arrive++;
        }
        stjump[i][0] = arrive;  // stjump[i][0]表示从i出发，跳跃1次能到达的最远线段
    }
    // 通过动态规划，构建更大的跳跃表，跳跃2^p次
    for (int p = 1; p <= power; p++) {
        for (int i = 1; i <= e; i++) {
            stjump[i][p] = stjump[stjump[i][p - 1]][p - 1];  // 跳跃2^p次，利用之前的跳跃信息
        }
    }
}

// 根据当前线段i，计算覆盖整个环所需的最小线段数
int jump(int i) {
    int aim = line[i][1] + m, cur = i, next, result = 0;
    // 从power次方开始跳跃，逐步减少跳跃次数，寻找最远的线段
    for (int p = power; p >= 0; p--) {
        next = stjump[cur][p];  // 获取跳跃2^p次能到达的线段
        if (line[next][2] < aim) {  // 如果跳跃后线段的右边界小于目标点aim
            result += (1 << p);  // 增加跳跃次数
            cur = next;  // 更新当前线段
        }
    }
    return result + 1 + 1;  // 加1表示当前线段，+1表示至少需要选择这条线段
}

// 计算所有线段的最小覆盖数
void compute() {
    power = log2(n);  // 计算log2(n)
    build();  // 构建跳跃表
    for (int i = 1; i <= n; i++) {
        ans[line[i][0]] = jump(i);  // 对每条线段，计算其最小覆盖数
    }
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cin >> n >> m;  // 输入线段数量n和点的数量m
    // 输入每条线段的信息
    for (int i = 1; i <= n; i++) {
        line[i][0] = i;  // 线段编号
        cin >> line[i][1] >> line[i][2];  // 输入左边界和右边界
    }
    compute();  // 计算每条线段的最小覆盖数
    // 输出结果
    cout << ans[1];
    for (in i = 2; i <= n; i++) {
        cout << " " << ans[i];
    }
    cout << "\n";
    return 0;
}
```

[ST表维护最大值和最小值](https://www.luogu.com.cn/problem/P2880)

给定一个长度为n的数组arr，一共有m次查询

每次查询arr[l~r]上的最大值和最小值

每次查询只需要打印最大值-最小值的结果

测试链接 : <https://www.luogu.com.cn/problem/P2880>

```c++
#include <iostream>
#include <vector>
#include <cmath>
using namespace std;

const int MAXN = 50001;
const int LIMIT = 16;

int arr[MAXN];
int log2[MAXN];
int stmax[MAXN][LIMIT];
int stmin[MAXN][LIMIT];

void build(int n) {
    log2[0] = -1;
    for (int i = 1; i <= n; i++) {
        log2[i] = log2[i >> 1] + 1;
        stmax[i][0] = arr[i];
        stmin[i][0] = arr[i];
    }
    for (int p = 1; p <= log2[n]; p++) {
        for (int i = 1; i + (1 << p) - 1 <= n; i++) {
            stmax[i][p] = max(stmax[i][p - 1], stmax[i + (1 << (p - 1))][p - 1]);
            stmin[i][p] = min(stmin[i][p - 1], stmin[i + (1 << (p - 1))][p - 1]);
        }
    }
}

int query(int l, int r) {
    int p = log2[r - l + 1];
    int a = max(stmax[l][p], stmax[r - (1 << p) + 1][p]);
    int b = min(stmin[l][p], stmin[r - (1 << p) + 1][p]);
    return a - b;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    
    int n, m;
    cin >> n >> m;
    for (int i = 1; i <= n; i++) {
        cin >> arr[i];
    }
    build(n);
    
    for (int i = 0, l, r; i < m; i++) {
        cin >> l >> r;
        cout << query(l, r) << '\n';
    }
    
    return 0;
}
```

ST表的适用范围

如果A区间和B区间可能有重叠的部分

但是并不影响A+B区间的答案，能通过 A区间答案 和 B区间答案 就加工出来

那么对应的区间询问，就是一个可重复贡献问题

例如：区间最大值，区间最小值、区间公约数等，但是区间求和就不符合这个要求

再例如：区间按位与、区间按位或，ST表都能高效地解决

ST表的优势和劣势

RMQ问题(Range Maximum/Minimum Query)可以用ST表维护，也可以用线段树等结构维护

ST表的优势：构建过程时间复杂度O(n * logn)，单次查询时间复杂度O(1)，代码量较小

ST表的劣势：需要空间较大，能维护的信息非常有限，不支持修改操作



## 树状数组

树状数组维护信息的类型

树状数组一般用来维护可差分的信息

比如：累加和、累乘积、或者出题人发现了某个可差分信息来出题考你

不可差分的信息，比如：最大值、最小值、除此之外的很多信息

不可差分的信息一般不用树状数组维护，会选择用线段树维护，因为线段树维护的方式思考难度更低

树状数组维护不可差分信息的内容不再讲述，后续会详细讲解线段树

大多数情况下，线段树可以替代树状数组，两者的时间复杂度差不多，单次调用都是O(log n)

线段树的优势：用法全面、思考难度低、维护信息类型多（包括可差分信息、不可差分信息）

线段树的劣势：代码较多、使用空间较大、常数时间稍差

树状数组优势：代码量少、使用空间少、常数时间优异

树状数组劣势：维护信息的类型少、维护某些不可差分的信息时思考难度大并且不易实现

[树状数组单点增加、范围查询模版](https://www.luogu.com.cn/problem/P3374)

测试链接 : https://www.luogu.com.cn/problem/P3374

这是使用树状数组最常见的方式，笔试、比赛中都大量出现

树状数组中信息的组织方式，课上重点图解

当i位置增加v时，如下位置都获得该值 : i += i & -i

当计算[1..i]范围的累加和时，把如下位置的值都加上 : i -= i & -i

神奇的lowbit，可以将上述的组织，优雅、方便的实现

单次调用时间复杂度O(log n)

```c++
#include <iostream>
#include <vector>
using namespace std;
const int MAXN = 500001;
vector<int> tree(MAXN, 0);
int n, m;
// 获取 i 的最低位 1
int lowbit(int i) {
    return i & -i;
}
// 单点增加
void add(int i, int v) {
    while (i <= n) {
        tree[i] += v;
        i += lowbit(i);
    }
}
// 计算前缀和 sum(1~i)
int sum(int i) {
    int ans = 0;
    while (i > 0) {
        ans += tree[i];
        i -= lowbit(i);
    }
    return ans;
}
// 计算区间和 range(l, r)
int range(int l, int r) {
    return sum(r) - sum(l - 1);
}
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    cin >> n >> m;
    for (int i = 1, v; i <= n; i++) {
        cin >> v;
        add(i, v);
    }
    for (int i = 0, a, b, c; i < m; i++) {
        cin >> a >> b >> c;
        if (a == 1) {
            add(b, c);
        } else {
            cout << range(b, c) << '\n';
        }
    }
    return 0;
}
```



## 线段树

线段树维护的信息类型

父范围上的某个信息，可以用O(1)的时间，从子范围的信息加工得到

满足的信息比如：累加和、最大值、最小值；不满足的信息比如：某范围上出现次数最多的数

线段树的经典功能，如下操作单次调用的时间复杂度为O(log n)

1，范围查询，包括范围内累加和、最大值、最小值等等信息

2，范围修改，包括范围内每个数都增加、重置等等操作

线段树的范围修改功能，想做到单次调用时间复杂度为O(log n)的要求：

一段范围上统一进行了某种修改操作，可以用O(1)的时间，就把这段范围维护的信息加工出来

满足的情况，比如：这段范围所有数字都加v，累加和可以快速的加工出来

不满足的情况，比如：这段范围上每个数字都逆序，累加和不能快速的加工出来

线段树非常灵活，维护信息的种类很多，支持范围修改的类型也很多，整个专题会讲述大量的题目和用法

线段树的组织，以最经典的累加和举例

1，线段树开始下标可以为1，也可以为0，下标从1开始是最经典的设定

2，线段树需要在初始化时，就指定范围的规模[1 ~ n]，一旦确定不能更改

3，任何一个大范围[l ~ r]，严格从中点mid，拆分成左范围[l ~ mid]、右范围[mid+1 ~ r]

4，每个范围的信息，填写在独立的、连续数组sum中，最大的范围[1 ~ n]，把信息填写在sum[1]

5，如果父范围把信息填写在sum[i]，那么左范围填写在sum[i*2]，右范围填写在sum[i*2+1]

6，范围[l ~ r]和i值的对应，是由公式限制死的，由递归参数维护，无需去记录对应关系

这种设定最为经典，并且为最佳实践，线段树整个专题的代码都遵守这种设定

如果线段树的范围是1 ~ n，那么记录信息的数组，开多大才够用？4倍的n！

为什么？课上重点图解，本节课代码HowManySpace，也展示了这一点

范围1 ~ n，形成的满二叉树高度会达到(log n + 2)，log以2为底

那么这棵满二叉树节点数会达到，2的(logn + 2)次方，log以2为底

2的(logn + 2)次方 = n * 4

范围修改操作，以最经典的范围内每个数字都增加来举例

sum数组 : 范围累加和(查询信息)    add数组 : 范围上每个数的增加值(懒信息)

范围内每个数字都增加 : void add(jobl, jobr, jobv, l, r, i)

前三个是任务参数，表示jobl ~ jobr范围上，每个数增加jobv，递归过程中这三个参数永远固定

后三个是范围参数，表示当前来到线段树的l ~ r范围上，信息存储位置是i，递归过程中这三个参数可变

开始时调用add(jobl, jobr, jobv, 1, n, 1)，范围增加的递归过程，懒更新机制！课上重点图解

1，如果发现任务范围(jobl, jobr)把当前范围(l,r)全覆盖了，不再向下传递任务，懒住！

   add[i] += jobv; sum[i] += jobv * (r - l + 1);

2，如果任务范围不能把当前范围全包，把该范围上积攒的懒信息，往下只下发一层，down过程

   然后决定当前任务是否要去往，左范围、右范围，继续调用子递归过程

   子递归完成后，利用左右范围的sum信息，把当前范围的sum[i]信息修改正确，up过程

3，退出当前递归过程

线段树支持范围增加、范围查询

[维护累加和](https://www.luogu.com.cn/problem/P3372)

```c++
#include <iostream>
#include <vector>

using namespace std;

const int MAXN = 100001; // 最大数组大小
vector<long long> arr(MAXN); // 原始数组
vector<long long> sum(MAXN << 2, 0); // 线段树数组，维护区间和
vector<long long> add(MAXN << 2, 0); // 懒标记数组，维护区间增量

// 更新父节点信息
void up(int i) {
    sum[i] = sum[i << 1] + sum[i << 1 | 1];
}

// 进行懒更新，将增量累加到当前区间
void lazy(int i, long long v, int n) {
    sum[i] += v * n; // 更新区间和
    add[i] += v; // 记录懒标记
}

// 向子节点下发懒标记
void down(int i, int ln, int rn) {
    if (add[i] != 0) { // 只有当懒标记不为 0 时才下发
        lazy(i << 1, add[i], ln); // 传递给左子区间
        lazy(i << 1 | 1, add[i], rn); // 传递给右子区间
        add[i] = 0; // 清空当前节点的懒标记
    }
}

// 构建线段树
void build(int l, int r, int i) {
    if (l == r) { // 叶子节点，直接存入原数组值
        sum[i] = arr[l];
    } else {
        int mid = (l + r) >> 1;
        build(l, mid, i << 1); // 递归构建左子树
        build(mid + 1, r, i << 1 | 1); // 递归构建右子树
        up(i); // 更新当前节点的区间和
    }
    add[i] = 0; // 初始化懒标记s
}

// 区间增加操作
void addRange(int jobl, int jobr, long long jobv, int l, int r, int i) {
    if (jobl <= l && r <= jobr) { // 完全覆盖当前区间
        lazy(i, jobv, r - l + 1); // 直接标记懒更新
    } else {
        int mid = (l + r) >> 1;
        down(i, mid - l + 1, r - mid); // 先下发懒标记
        if (jobl <= mid) addRange(jobl, jobr, jobv, l, mid, i << 1); // 递归左区间
        if (jobr > mid) addRange(jobl, jobr, jobv, mid + 1, r, i << 1 | 1); // 递归右区间
        up(i); // 更新当前区间的和
    }
}

// 区间查询操作
long long query(int jobl, int jobr, int l, int r, int i) {
    if (jobl <= l && r <= jobr) { // 当前区间完全包含在查询范围内
        return sum[i];
    }
    int mid = (l + r) >> 1;
    down(i, mid - l + 1, r - mid); // 先下发懒标记
    long long ans = 0;
    if (jobl <= mid) ans += query(jobl, jobr, l, mid, i << 1); // 递归查询左子区间
    if (jobr > mid) ans += query(jobl, jobr, mid + 1, r, i << 1 | 1); // 递归查询右子区间
    return ans;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n, m;
    cin >> n >> m;
    for (int i = 1; i <= n; i++) {
        cin >> arr[i];
    }
    build(1, n, 1); // 构建线段树
    for (int i = 0; i < m; i++) {
        int op, jobl, jobr;
        long long jobv;
        cin >> op;
        if (op == 1) { // 区间增加操作
            cin >> jobl >> jobr >> jobv;
            addRange(jobl, jobr, jobv, 1, n, 1);
        } else { // 区间查询操作
            cin >> jobl >> jobr;
            cout << query(jobl, jobr, 1, n, 1) << '\n';
        }
    }
    return 0;
}
```



## 树上倍增和LCA

每个节点只有一个头节点的图结构，就叫做树

如果所有节点形成一棵树，那么节点数为n，那么边的数量为n-1，也可能整体是森林结构

树上问题所指的树，包括一叉树、二叉树、多叉树

树上倍增

1）建立每个节点的深度表，deep数组

2）建立每个节点往上跳1步、跳2步、跳4步、跳8步..能到达的节点编号，ST表

3）给定任意节点i，可以快速查询，从i节点往上走的路径中位于第s层的节点编号

生成deep数组时间复杂度O(n)

生成ST表时间复杂度O(n * logn)

单次查询时间复杂度O(log n)

[树节点的第K个祖先](https://leetcode.cn/problems/kth-ancestor-of-a-tree-node/)

树上有n个节点，编号0 ~ n-1，树的结构用parent数组代表

其中parent[i]是节点i的父节点，树的根节点是编号为0

树节点i的第k个祖先节点，是从节点i开始往上跳k步所来到的节点

实现TreeAncestor类

TreeAncestor(int n, int[] parent) : 初始化

getKthAncestor(int i, int k) : 返回节点i的第k个祖先节点，不存在返回-1

测试链接 : https://leetcode.cn/problems/kth-ancestor-of-a-tree-node/

```c++
#include <vector>
#include <cstring>
using namespace std;

class TreeAncestor
{
public:
    static const int MAXN = 50001;
    static const int LIMIT = 16;
    
    int power;
    int head[MAXN], next[MAXN], to[MAXN], cnt = 0;
    int deep[MAXN];
    int stjump[MAXN][LIMIT];
    
    TreeAncestor(int n, vector<int> &parent)
    {
        power = log2(n);
        fill(head, head + MAXN, -1);
        for (int i = 1; i < parent.size(); i++)
        {
            addEdge(parent[i], i);
        }
        dfs(0, 0);
    }
    
    int log2(int n)
    {
        int ans = 0;
        while ((1 << ans) <= (n >> 1))
        {
            ans++;
        }
        return ans;
    }
    
    void addEdge(int u, int v)
    {
        next[cnt] = head[u];
        to[cnt] = v;
        head[u] = cnt++;
    }
    
    void dfs(int i, int f)
    {
        deep[i] = (i == 0) ? 1 : deep[f] + 1;
        stjump[i][0] = f;
        for (int p = 1; p <= power; p++)
        {
            stjump[i][p] = stjump[stjump[i][p - 1]][p - 1];
        }
        for (int e = head[i]; e != 0; e = next[e])
        {
            dfs(to[e], i);
        }
    }
    
    int getKthAncestor(int i, int k)
    {
        if (deep[i] <= k)
        {
            return -1;
        }
        int s = deep[i] - k;
        for (int p = power; p >= 0; p--)
        {
            if (deep[stjump[i][p]] >= s)
            {
                i = stjump[i][p];
            }
        }
        return i;
    }
};
```

[树上倍增解决LCA问题](https://www.luogu.com.cn/problem/P3379)

算法过程：

1）先让a和b往上跳到同一层

2）利用倍增算法找到a和b的最低公共祖先

如果节点数为n，建立预处理结构的时间复杂度O(n*logn)，单次查询的时间复杂度O(log n)

优势是可以在线查询，如果一共m条查询，那么查询的总复杂度O(m * logn)

```c++
#include <iostream>
#include <vector>
#include <cstring>
using namespace std;

const int MAXN = 500001;
const int LIMIT = 20;

int power;
int head[MAXN], nxt[MAXN << 1], to[MAXN << 1], cnt;
int deep[MAXN];
int stjump[MAXN][LIMIT];

void build(int n) {
    power = 0;
    while ((1 << power) <= (n >> 1)) power++;
    cnt = 1;
    memset(head, 0, sizeof(head));
}

void addEdge(int u, int v) {
    nxt[cnt] = head[u];
    to[cnt] = v;
    head[u] = cnt++;
}

// 递归 DFS 计算深度和倍增数组
void dfs(int u, int f) {
    deep[u] = deep[f] + 1;
    stjump[u][0] = f;
    for (int p = 1; p <= power; p++) {
        stjump[u][p] = stjump[stjump[u][p - 1]][p - 1];
    }
    for (int e = head[u]; e; e = nxt[e]) {
        if (to[e] != f) {
            dfs(to[e], u);
        }
    }
}

// 倍增法查询 LCA
int lca(int a, int b) {
    if (deep[a] < deep[b]) swap(a, b);
    for (int p = power; p >= 0; p--) {
        if (deep[stjump[a][p]] >= deep[b]) {
            a = stjump[a][p];
        }
    }
    if (a == b) return a;
    for (int p = power; p >= 0; p--) {
        if (stjump[a][p] != stjump[b][p]) {
            a = stjump[a][p];
            b = stjump[b][p];
        }
    }
    return stjump[a][0];
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int n, m, root;
    cin >> n >> m >> root;
    build(n);
    
    for (int i = 1, u, v; i < n; i++) {
        cin >> u >> v;
        addEdge(u, v);
        addEdge(v, u);
    }

    dfs(root, 0);

    for (int i = 1, a, b; i <= m; i++) {
        cin >> a >> b;
        cout << lca(a, b) << '\n';
    }

    return 0;
}
```



## 动态规划

### 递归一维动态规划

动态规划：用空间代替重复计算，包含一整套原理和技巧的总和
知道怎么算的算法 vs 知道怎么试的算法
有些递归在展开计算时，总是重复调用同一个子问题的解，这种重复调用的递归变成动态规划很有收益
如果每次展开都是不同的解，或者重复调用的现象很少，那么没有改动态规划的必要

任何动态规划问题都一定对应着一个有重复调用行为的递归
所以任何动态规划的题目都一定可以从递归入手，逐渐实现动态规划的方法

尝试策略 就是 转移方程，完全一回事！
推荐从尝试入手，因为代码好写，并且一旦发现尝试错误，重新想别的递归代价轻！

动态规划的大致过程：
想出设计优良的递归尝试(方法、经验、固定套路很多)，有关尝试展开顺序的说明
-> 记忆化搜索(从顶到底的动态规划) ，如果每个状态的计算枚举代价很低，往往到这里就可以了
-> 严格位置依赖的动态规划(从底到顶的动态规划) ，更多是为了下面说的 进一步优化枚举做的准备
-> 进一步优化空间（空间压缩），一维、二维、多维动态规划都存在这种优化
-> 进一步优化枚举也就是优化时间

解决一个问题，可能有很多尝试方法
众多的尝试方法中，可能若干的尝试方法有重复调用的情况，可以转化成动态规划
若干个可以转化成动态规划的方法中，又可能有优劣之分
判定哪个是最优的动态规划方法，依据来自题目具体参数的数据量
最优的动态规划方法实现后，后续又有一整套的优化技巧

##### 解码方法 II

一条包含字母 A-Z 的消息通过以下的方式进行了 编码 ：

'A' -> "1"、'B' -> "2" ...'Z' -> "26"

要 解码 一条已编码的消息，所有的数字都必须分组

然后按原来的编码方案反向映射回字母，可能存在多种方式。例如"11106" 可以映射为："AAJF"、"KJF"

注意，像 (1 11 06) 这样的分组是无效的，"06"不可以映射为'F'

除了上面描述的数字字母映射方案，编码消息中可能包含 '*' 字符

可以表示从 '1' 到 '9' 的任一数字（不包括 '0'）

例如，"1*" 可以表示 "11"、"12"、"13"、"14"、"15"、"16"、"17"、"18" 或 "19"

对 "1*" 进行解码，相当于解码该字符串可以表示的任何编码消息

给你一个字符串 s ，由数字和 '*' 字符组成，返回 解码 该字符串的方法 数目

由于答案数目可能非常大，返回10^9 + 7的模

测试链接 : https://leetcode.cn/problems/decode-ways-ii/

```c++
#include <iostream>
#include <vector>
#include <string>
#include <algorithm>

using namespace std;
// 解码方法 II
// 一条包含字母 A-Z 的消息通过以下的方式进行了 编码 ：
// 'A' -> "1"
// 'B' -> "2"
// ...
// 'Z' -> "26"
// 要 解码 一条已编码的消息，所有的数字都必须分组
// 然后按原来的编码方案反向映射回字母（可能存在多种方式）
// 例如，"11106" 可以映射为："AAJF"、"KJF"
// 注意，像 (1 11 06) 这样的分组是无效的，"06"不可以映射为'F'
// 除了上面描述的数字字母映射方案，编码消息中可能包含 '*' 字符
// 可以表示从 '1' 到 '9' 的任一数字（不包括 '0'）
// 例如，"1*" 可以表示 "11"、"12"、"13"、"14"、"15"、"16"、"17"、"18" 或 "19"
// 对 "1*" 进行解码，相当于解码该字符串可以表示的任何编码消息
// 给你一个字符串 s ，由数字和 '*' 字符组成，返回 解码 该字符串的方法 数目
// 由于答案数目可能非常大，答案对 1000000007 取模
// 测试链接 : https://leetcode.cn/problems/decode-ways-ii/
class DecodeWaysII {
public:
    static const long mod = 1000000007;

    // 方法1：暴力递归
    static int numDecodings1(const string& str) {
        return f1(str.c_str(), 0);
    }

    // 递归函数：计算从第i个字符开始的有效解码方式数目
    static int f1(const char* s, int i) {
        if (s[i] == '\0') {
            return 1; // 如果到达字符串末尾，返回1
        }
        if (s[i] == '0') {
            return 0; // 如果遇到'0'，无法解码，返回0
        }

        int ans = f1(s, i + 1) * (s[i] == '*' ? 9 : 1); // 当前位置单独解码
        if (s[i + 1] != '\0') {
            // 尝试将当前位置和下一个字符一起解码
            if (s[i] != '*') {
                if (s[i + 1] != '*') {
                    if ((s[i] - '0') * 10 + (s[i + 1] - '0') <= 26) {
                        ans += f1(s, i + 2);
                    }
                } else {
                    if (s[i] == '1') {
                        ans += f1(s, i + 2) * 9;
                    }
                    if (s[i] == '2') {
                        ans += f1(s, i + 2) * 6;
                    }
                }
            } else {
                if (s[i + 1] != '*') {
                    if (s[i + 1] <= '6') {
                        ans += f1(s, i + 2) * 2;
                    } else {
                        ans += f1(s, i + 2);
                    }
                } else {
                    ans += f1(s, i + 2) * 15; // '* *'的情况
                }
            }
        }
        return ans;
    }

    // 方法2：记忆化搜索（自顶向下的动态规划）
    static int numDecodings2(const string& str) {
        int n = str.size();
        vector<long> dp(n, -1);
        return (int)f2(str.c_str(), 0, dp);
    }

    // 递归函数：计算从第i个字符开始的解码方式数目，带记忆化
    static long f2(const char* s, int i, vector<long>& dp) {
        if (s[i] == '\0') {
            return 1; // 到达末尾，返回1
        }
        if (s[i] == '0') {
            return 0; // '0'无法解码，返回0
        }
        if (dp[i] != -1) {
            return dp[i]; // 如果已计算，直接返回缓存结果
        }

        long ans = f2(s, i + 1, dp) * (s[i] == '*' ? 9 : 1); // 当前位置单独解码
        if (s[i + 1] != '\0') {
            if (s[i] != '*') {
                if (s[i + 1] != '*') {
                    if ((s[i] - '0') * 10 + (s[i + 1] - '0') <= 26) {
                        ans += f2(s, i + 2, dp);
                    }
                } else {
                    if (s[i] == '1') {
                        ans += f2(s, i + 2, dp) * 9;
                    }
                    if (s[i] == '2') {
                        ans += f2(s, i + 2, dp) * 6;
                    }
                }
            } else {
                if (s[i + 1] != '*') {
                    if (s[i + 1] <= '6') {
                        ans += f2(s, i + 2, dp) * 2;
                    } else {
                        ans += f2(s, i + 2, dp);
                    }
                } else {
                    ans += f2(s, i + 2, dp) * 15; // '* *'的情况
                }
            }
        }
        ans %= mod;
        dp[i] = ans;
        return ans;
    }

    // 方法3：自底向上的动态规划
    static int numDecodings3(const string& str) {
        int n = str.size();
        vector<long> dp(n + 1);
        dp[n] = 1; // 最后一个字符之后的解码方式数目为1
        for (int i = n - 1; i >= 0; --i) {
            if (str[i] != '0') {
                dp[i] = (str[i] == '*' ? 9 : 1) * dp[i + 1]; // 当前位置单独解码
                if (i + 1 < n) {
                    if (str[i] != '*') {
                        if (str[i + 1] != '*') {
                            if ((str[i] - '0') * 10 + (str[i + 1] - '0') <= 26) {
                                dp[i] += dp[i + 2];
                            }
                        } else {
                            if (str[i] == '1') {
                                dp[i] += dp[i + 2] * 9;
                            }
                            if (str[i] == '2') {
                                dp[i] += dp[i + 2] * 6;
                            }
                        }
                    } else {
                        if (str[i + 1] != '*') {
                            if (str[i + 1] <= '6') {
                                dp[i] += dp[i + 2] * 2;
                            } else {
                                dp[i] += dp[i + 2];
                            }
                        } else {
                            dp[i] += dp[i + 2] * 15; // '* *'的情况
                        }
                    }
                }
                dp[i] %= mod;
            }
        }
        return (int)dp[0];
    }

    // 方法4：优化空间的动态规划
    static int numDecodings4(const string& str) {
        int n = str.size();
        long cur = 0, next = 1, nextNext = 0;
        for (int i = n - 1; i >= 0; --i) {
            if (str[i] != '0') {
                cur = (str[i] == '*' ? 9 : 1) * next; // 当前位置单独解码
                if (i + 1 < n) {
                    if (str[i] != '*') {
                        if (str[i + 1] != '*') {
                            if ((str[i] - '0') * 10 + (str[i + 1] - '0') <= 26) {
                                cur += nextNext;
                            }
                        } else {
                            if (str[i] == '1') {
                                cur += nextNext * 9;
                            }
                            if (str[i] == '2') {
                                cur += nextNext * 6;
                            }
                        }
                    } else {
                        if (str[i + 1] != '*') {
                            if (str[i + 1] <= '6') {
                                cur += nextNext * 2;
                            } else {
                                cur += nextNext;
                            }
                        } else {
                            cur += nextNext * 15; // '* *'的情况
                        }
                    }
                }
                cur %= mod;
            }
            nextNext = next;
            next = cur;
            cur = 0;
        }
        return (int)next;
    }
};

// 主函数，测试
int main() {
    string s = "1*";
    cout << "Method 1: " << DecodeWaysII::numDecodings1(s) << endl;
    cout << "Method 2: " << DecodeWaysII::numDecodings2(s) << endl;
    cout << "Method 3: " << DecodeWaysII::numDecodings3(s) << endl;
    cout << "Method 4: " << DecodeWaysII::numDecodings4(s) << endl;

    return 0;
}
```

##### 其他题目

1. 斐波那契数

   斐波那契数 （通常用 F(n) 表示）形成的序列称为 斐波那契数列

   该数列由 0 和 1 开始，后面的每一项数字都是前面两项数字的和。

   也就是：F(0) = 0，F(1) = 1

   F(n) = F(n - 1) + F(n - 2)，其中 n > 1

   给定 n ，请计算 F(n)

   测试链接 : https://leetcode.cn/problems/fibonacci-number/

2. 最低票价

   在一个火车旅行很受欢迎的国度，你提前一年计划了一些火车旅行

   在接下来的一年里，你要旅行的日子将以一个名为 days 的数组给出

   每一项是一个从 1 到 365 的整数

   火车票有 三种不同的销售方式

   一张 为期1天 的通行证售价为 costs[0] 美元

   一张 为期7天 的通行证售价为 costs[1] 美元

   一张 为期30天 的通行证售价为 costs[2] 美元

   通行证允许数天无限制的旅行

   例如，如果我们在第 2 天获得一张 为期 7 天 的通行证

   那么我们可以连着旅行 7 天(第2~8天)

   返回 你想要完成在给定的列表 days 中列出的每一天的旅行所需要的最低消费

   测试链接 : <https://leetcode.cn/problems/minimum-cost-for-tickets/>

3. 解码方法

   一条包含字母 A-Z 的消息通过以下映射进行了 编码 ：

   'A' -> "1"、'B' -> "2" ...'Z' -> "26"

   要 解码 已编码的消息，所有数字必须基于上述映射的方法，反向映射回字母（可能有多种方法）

   例如，"11106" 可以映射为："AAJF"、"KJF"

   注意，消息不能分组为(1 11 06)，因为 "06" 不能映射为 "F"

   这是由于 "6" 和 "06" 在映射中并不等价

   给你一个只含数字的 非空 字符串 s ，请计算并返回 解码 方法的 总数

   题目数据保证答案肯定是一个 32位 的整数

   测试链接 : <https://leetcode.cn/problems/decode-ways/>

4. 丑数 II

   丑数 就是只包含质因数 2、3 或 5 的正整数

   默认第1个丑数是1，前几项丑数为:

   1, 2, 3, 4, 5, 6, 8, 9, 10, 12, 15, 16, 18, 20,

   24, 25, 27, 30, 32, 36, 40, 45, 48, 50, 54, 60,

   64, 72, 75, 80, 81, 90, 96, 100, 108, 120, 125..

   给你一个整数n ，请你找出并返回第n个丑数

   比如，n = 37，返回125

   测试链接 : <https://leetcode.cn/problems/ugly-number-ii/>

5. 最长有效括号

   给你一个只包含 '(' 和 ')' 的字符串

   找出最长有效（格式正确且连续）括号子串的长度。

   测试链接 : <https://leetcode.cn/problems/longest-valid-parentheses/>

6. 环绕字符串中唯一的子字符串

   定义字符串 base 为一个 "abcdefghijklmnopqrstuvwxyz" 无限环绕的字符串

   所以 base 看起来是这样的：

   "..zabcdefghijklmnopqrstuvwxyzabcdefghijklmnopqrstuvwxyzabcd.."

   给你一个字符串 s ，请你统计并返回 s 中有多少 不同、非空子串 也在 base 中出现

   测试链接 : <https://leetcode.cn/problems/unique-substrings-in-wraparound-string/>

7. 不同的子序列 II

   给定一个字符串 s，计算 s 的 不同非空子序列 的个数

   因为结果可能很大，所以返回答案需要对 10^9 + 7 取余

   字符串的 子序列 是经由原字符串删除一些（也可能不删除）

   字符但不改变剩余字符相对位置的一个新字符串

   例如，"ace" 是 "abcde" 的一个子序列，但 "aec" 不是

   测试链接 : <https://leetcode.cn/problems/distinct-subsequences-ii/>
   
   

### 递归二维动态规划

动态规划表的大小：每个可变参数的可能性数量相乘
动态规划方法的时间复杂度：动态规划表的大小 * 每个格子的枚举代价

二维动态规划依然需要去整理 动态规划表的格子之间的依赖关系
找寻依赖关系，往往 通过画图来建立空间感，使其更显而易见
然后依然是 从简单格子填写到复杂格子 的过程，即严格位置依赖的动态规划(从底到顶)

能改成动态规划的递归，统一特征：

决定返回值的可变参数类型往往都比较简单，一般不会比int类型更复杂。从这个角度，可以解释 带路径的递归（可变参数类型复杂），不适合或者说没有必要改成动态规划

一定要 写出可变参数类型简单（不比int类型更复杂），并且 可以完全决定返回值的递归，保证做到 这些可变参数可以完全代表之前决策过程对后续过程的影响！再去改动态规划！

不管几维动态规划,经常从递归的定义出发，避免后续进行很多边界讨论

##### 最小路径和

给定一个包含非负整数的 m x n 网格 grid
请找出一条从左上角到右下角的路径，使得路径上的数字总和为最小。
说明：每次只能向下或者向右移动一步。
测试链接 : https://leetcode.cn/problems/minimum-path-sum/

```c++
#include <iostream>
#include <vector>
#include <algorithm>
#include <climits>

using namespace std;

// 暴力递归
int f1(const vector<vector<int>>& grid, int i, int j) {
    if (i == 0 && j == 0) {
        return grid[0][0];
    }
    int up = INT_MAX;
    int left = INT_MAX;
    if (i - 1 >= 0) {
        up = f1(grid, i - 1, j);
    }
    if (j - 1 >= 0) {
        left = f1(grid, i, j - 1);
    }
    return grid[i][j] + min(up, left);
}

int minPathSum1(const vector<vector<int>>& grid) {
    return f1(grid, grid.size() - 1, grid[0].size() - 1);
}

// 记忆化搜索
int f2(const vector<vector<int>>& grid, int i, int j, vector<vector<int>>& dp) {
    if (dp[i][j] != -1) {
        return dp[i][j];
    }
    int ans;
    if (i == 0 && j == 0) {
        ans = grid[0][0];
    } else {
        int up = INT_MAX;
        int left = INT_MAX;
        if (i - 1 >= 0) {
            up = f2(grid, i - 1, j, dp);
        }
        if (j - 1 >= 0) {
            left = f2(grid, i, j - 1, dp);
        }
        ans = grid[i][j] + min(up, left);
    }
    dp[i][j] = ans;
    return ans;
}

int minPathSum2(const vector<vector<int>>& grid) {
    int n = grid.size();
    int m = grid[0].size();
    vector<vector<int>> dp(n, vector<int>(m, -1));
    return f2(grid, n - 1, m - 1, dp);
}

// 严格位置依赖的动态规划
int minPathSum3(const vector<vector<int>>& grid) {
    int n = grid.size();
    int m = grid[0].size();
    vector<vector<int>> dp(n, vector<int>(m));
    dp[0][0] = grid[0][0];
    for (int i = 1; i < n; ++i) {
        dp[i][0] = dp[i - 1][0] + grid[i][0];
    }
    for (int j = 1; j < m; ++j) {
        dp[0][j] = dp[0][j - 1] + grid[0][j];
    }
    for (int i = 1; i < n; ++i) {
        for (int j = 1; j < m; ++j) {
            dp[i][j] = min(dp[i - 1][j], dp[i][j - 1]) + grid[i][j];
        }
    }
    return dp[n - 1][m - 1];
}

// 严格位置依赖的动态规划 + 空间压缩技巧
int minPathSum4(const vector<vector<int>>& grid) {
    int n = grid.size();
    int m = grid[0].size();
    vector<int> dp(m);
    dp[0] = grid[0][0];
    for (int j = 1; j < m; ++j) {
        dp[j] = dp[j - 1] + grid[0][j];
    }
    for (int i = 1; i < n; ++i) {
        dp[0] += grid[i][0];
        for (int j = 1; j < m; ++j) {
            dp[j] = min(dp[j - 1], dp[j]) + grid[i][j];
        }
    }
    return dp[m - 1];
}

int main() {
    vector<vector<int>> grid = {
        {1, 3, 1},
        {1, 5, 1},
        {4, 2, 1}
    };

    cout << "Minimum Path Sum (Recursion): " << minPathSum1(grid) << endl;
    cout << "Minimum Path Sum (Memoization): " << minPathSum2(grid) << endl;
    cout << "Minimum Path Sum (DP): " << minPathSum3(grid) << endl;
    cout << "Minimum Path Sum (DP + Space Optimization): " << minPathSum4(grid) << endl;

    return 0;
}
```

##### 单词搜索（无法改成动态规划）

给定一个 m x n 二维字符网格 board 和一个字符串单词 word

如果 word 存在于网格中，返回 true ；否则，返回 false 。

单词必须按照字母顺序，通过相邻的单元格内的字母构成

其中"相邻"单元格是那些水平相邻或垂直相邻的单元格

同一个单元格内的字母不允许被重复使用

测试链接 : <https://leetcode.cn/problems/word-search/>

##### 最长公共子序列

给定两个字符串text1和text2

返回这两个字符串的最长 公共子序列 的长度

如果不存在公共子序列，返回0

两个字符串的 公共子序列 是这两个字符串所共同拥有的子序列

测试链接 : <https://leetcode.cn/problems/longest-common-subsequence/>

##### 最长回文子序列

给你一个字符串 s ，找出其中最长的回文子序列，并返回该序列的长度

测试链接 : <https://leetcode.cn/problems/longest-palindromic-subsequence/>

##### 节点数为n高度不大于m的二叉树个数

现在有n个节点，计算出有多少个不同结构的二叉树

满足节点个数为n且树的高度不超过m的方案

因为答案很大，所以答案需要模上1000000007后输出

测试链接 : <https://www.nowcoder.com/practice/aaefe5896cce4204b276e213e725f3ea>

##### 矩阵中的最长递增路径

给定一个 m x n 整数矩阵 matrix ，找出其中 最长递增路径 的长度

对于每个单元格，你可以往上，下，左，右四个方向移动

不能在对角线方向上移动或移动到边界外（即不允许环绕）

测试链接 : <https://leetcode.cn/problems/longest-increasing-path-in-a-matrix/>



### **01背包、有依赖的背包**

01背包：每个物品 要和不要 两种可能性展开

有依赖的背包：多个物品变成一个复合物品（互斥），每件复合物品 不要和怎么要 多种可能性展开

时间复杂度O(物品个数 * 背包容量)，额外空间复杂度O(背包容量)

不能用01背包来解，但是非常重要的问题：非负数组前k个最小的子序列和问题

##### 01背包(模版)

给定一个正数t，表示背包的容量

有n个货物，每个货物可以选择1次

每个货物有自己体积costs[i]和价值values[i]

返回在不超过总容量的情况下，怎么挑选货物能达到价值最大

返回最大的价值

测试链接 : <https://www.luogu.com.cn/problem/P1048>

```c++
#include <iostream>
#include <vector>
#include <algorithm>
#include <cstring>
using namespace std;

const int MAXM = 101;  // 最大物品数量
const int MAXT = 1001; // 最大背包容量

int cost[MAXM]; // 每个物品的体积
int val[MAXM];  // 每个物品的价值
int dp[MAXT];   // 动态规划数组，用于存储当前容量下的最大价值

int t, n; // t为背包容量，n为物品数量
// 方法1: 严格位置依赖的动态规划
// 该方法使用二维数组来存储每个物品在每种容量下的最大价值
int compute1() {
    // dp[i][j] 表示前i个物品在容量为j时的最大价值
    vector<vector<int>> dp(n + 1, vector<int>(t + 1, 0));
    for (int i = 1; i <= n; ++i) { // 遍历每个物品
        for (int j = 0; j <= t; ++j) { // 遍历容量从0到t
            // 不选第i个物品
            dp[i][j] = dp[i - 1][j];
            // 如果容量允许，考虑选第i个物品
            if (j - cost[i] >= 0) {
                dp[i][j] = max(dp[i][j], dp[i - 1][j - cost[i]] + val[i]);
            }
        }
    }
    return dp[n][t]; // 返回使用所有物品且容量为t时的最大价值
}

// 方法2: 空间优化的动态规划
// 该方法利用滚动数组优化空间复杂度，将二维数组降为一维数组
int compute2() {
    // 初始化动态规划数组，全部置为0
    memset(dp, 0, sizeof(dp));
    // 遍历每个物品
    for (int i = 1; i <= n; ++i) {
        // 倒序遍历容量，从背包容量t开始，避免覆盖前面的状态
        for (int j = t; j >= cost[i]; --j) {
            // 更新当前容量j的最大价值
            dp[j] = max(dp[j], dp[j - cost[i]] + val[i]);
        }
    }
    return dp[t]; // 返回容量为t时的最大价值
}

int main() {
    ios::sync_with_stdio(false); // 加速输入输出
    cin.tie(0);
    // 输入多组数据直到EOF
    while (cin >> t >> n) {
        for (int i = 1; i <= n; ++i) {
            cin >> cost[i] >> val[i]; // 输入每个物品的体积和价值
        }
        // 输出使用空间优化的动态规划求解的结果
        cout << compute2() << endl;
    }
    return 0;
}
```

##### 有依赖的背包(模版)

物品分为两大类：主件和附件

主件购买没有限制，钱够就可以；附件购买有限制，该附件所归属的主件先购买，才能购买这个附件

例如，若想买打印机或扫描仪这样的附件，必须先购买电脑这个主件。以下是一些主件及其附件的展示：

电脑：打印机，扫描仪 | 书柜：图书 | 书桌：台灯，文具 | 工作椅：无附件

每个主件最多有2个附件，并且附件不会再有附件，主件购买后，怎么去选择归属附件完全随意，钱够就可以

所有的物品编号都在1~m之间，每个物品有三个信息：价格v、重要度p、归属q

价格就是花费，价格 * 重要度 就是收益，归属就是该商品是依附于哪个编号的主件

比如一件商品信息为[300,2,6]，花费300，收益600，该商品是6号主件商品的附件

再比如一件商品信息[100,4,0]，花费100，收益400，该商品自身是主件(q==0)

给定m件商品的信息，给定总钱数n，返回在不违反购买规则的情况下最大的收益

测试链接 : <https://www.luogu.com.cn/problem/P1064>

测试链接 : <https://www.nowcoder.com/practice/f9c6f980eeec43ef85be20755ddbeaf4>

```c++
#include <iostream>
#include <vector>
#include <algorithm>
#include <cstring> // For memset
using namespace std;

const int MAXN = 33001; // 最大预算
const int MAXM = 61;    // 最大物品数量

int cost[MAXM];            // 每件物品的价格
int val[MAXM];             // 每件物品的价值（价格 * 重要度）
bool king[MAXM];           // 标记是否为主件
int fans[MAXM];            // 每个主件的附属物品数量
int follows[MAXM][2];      // 每个主件的附属物品编号
int dp[MAXN];              // 动态规划数组

int n, m; // 总预算和物品数量

// 清理数据
// 初始化附属物品数量和动态规划数组
void clean() {
    memset(fans, 0, sizeof(fans));
    memset(dp, 0, sizeof(dp));
}

// 主函数
int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    // 持续读取输入
    while (cin >> n >> m) {
        clean(); // 每组输入需要清理数据

        // 读取每件物品的信息
        for (int i = 1; i <= m; i++) {
            int v, p, q;
            cin >> v >> p >> q;
            cost[i] = v;       // 设置物品价格
            val[i] = v * p;    // 计算 物品的价值
            king[i] = (q == 0); // 判断是否为主件

            // 如果是附件，记录其所属主件
            if (q != 0) {
                follows[q][fans[q]++] = i;
            }
        }
        
        // 动态规划求解最大收益
        for (int i = 1; i <= m; i++) {
            if (king[i]) { // 仅处理主件
                for (int j = n; j >= cost[i]; j--) { // 从后向前遍历预算，确保状态不被重复使用
                    // 情况1：仅选择主件
                    dp[j] = max(dp[j], dp[j - cost[i]] + val[i]);

                    // 获取该主件的附件编号
                    int fan1 = (fans[i] >= 1) ? follows[i][0] : -1; // 附件1
                    int fan2 = (fans[i] >= 2) ? follows[i][1] : -1; // 附件2

                    // 情况2：主件 + 附件1
                    if (fan1 != -1 && j >= cost[i] + cost[fan1]) {
                        dp[j] = max(dp[j], dp[j - cost[i] - cost[fan1]] + val[i] + val[fan1]);
                    }

                    // 情况3：主件 + 附件2
                    if (fan2 != -1 && j >= cost[i] + cost[fan2]) {
                        dp[j] = max(dp[j], dp[j - cost[i] - cost[fan2]] + val[i] + val[fan2]);
                    }

                    // 情况4：主件 + 附件1 + 附件2
                    if (fan1 != -1 && fan2 != -1 && j >= cost[i] + cost[fan1] + cost[fan2]) {
                        dp[j] = max(dp[j], dp[j - cost[i] - cost[fan1] - cost[fan2]] + val[i] + val[fan1] + val[fan2]);
                    }
                }
            }
        }

        // 输出最大收益
        cout << dp[n] << endl;
    }

    return 0;
}
```

##### 其它题目

1. 夏季特惠

   某公司游戏平台的夏季特惠开始了，你决定入手一些游戏

   现在你一共有X元的预算，平台上所有的 n 个游戏均有折扣

   标号为 i 的游戏的原价a_i元，现价只要b_i元

   也就是说该游戏可以优惠 a_i - b_i，并且你购买该游戏能获得快乐值为w_i

   由于优惠的存在，你可能做出一些冲动消费导致最终买游戏的总费用超过预算

   只要满足 : 获得的总优惠金额>=超过预算的总金额

   那在心理上就不会觉得吃亏

   现在你希望在心理上不觉得吃亏的前提下，获得尽可能多的快乐值。

   测试链接 : <https://leetcode.cn/problems/tJau2o/>

   ```c++
   #include <iostream>
   #include <vector>
   #include <algorithm>
   #include <cstring>
   using namespace std;
   
   const int MAXN = 501;  // 最大商品数量
   const int MAXX = 100001; // 最大预算
   
   int cost[MAXN]; // 需要考虑的商品的实际"坏处"
   long long val[MAXN]; // 需要考虑的商品的快乐值
   long long dp[MAXX];  // 动态规划数组，用于存储当前预算下的最大快乐值
   
   int n, m, x; // n为商品数量，m为需要考虑的商品数量，x为预算
   
   // 动态规划计算最大快乐值
   long long compute() {
       // 初始化动态规划数组
       memset(dp, 0, sizeof(dp));
       for (int i = 1; i <= m; ++i) {
           for (int j = x; j >= cost[i]; --j) {
               dp[j] = max(dp[j], dp[j - cost[i]] + val[i]);
           }
       }
       return dp[x];
   }
   
   int main() {
       ios::sync_with_stdio(false); // 加速输入输出
       cin.tie(0);
   
       // 输入多组数据直到EOF
       while (cin >> n >> x) {
           m = 1; // 初始化需要考虑的商品数量
           long long ans = 0; // 最终结果
   
           for (int i = 1; i <= n; ++i) {
               int pre, cur, happy, well;
               cin >> pre >> cur >> happy; // 输入原价、现价和快乐值
               well = pre - cur - cur;
   
               if (well >= 0) {
                   // "一定要买的商品"
                   x += well; // 增加预算
                   ans += happy; // 增加快乐值
               } else {
                   // "需要考虑的商品"
                   cost[m] = -well; // 记录坏处
                   val[m++] = happy; // 记录快乐值
               }
           }
   
           ans += compute(); // 计算需要考虑的商品的最大快乐值
           cout << ans << endl; // 输出结果
       }
   
       return 0;
   }
   ```

2. 目标和

   给你一个非负整数数组 nums 和一个整数 target 。

   向数组中的每个整数前添加 '+' 或 '-' ，然后串联起所有整数

   可以构造一个表达式

   例如nums=[2, 1]，可以在2之前添加'+' ，在1之前添加'-'

   然后串联起来得到表达式 "+2-1" 。

   返回可以通过上述方法构造的，运算结果等于 target 的不同表达式的数目

   测试链接 : <https://leetcode.cn/problems/target-sum/>

   ```c++
   #include <iostream>
   #include <vector>
   #include <unordered_map>
   #include <cstring>
   using namespace std;
   
   // 递归暴力求解
   template<typename T>
   int findTargetSumWays1(const vector<int>& nums, T target) {
       return f1(nums, target, 0, 0);
   }
   
   int f1(const vector<int>& nums, int target, int i, int sum) {
       if (i == nums.size()) {
           return sum == target ? 1 : 0;
       }
       return f1(nums, target, i + 1, sum + nums[i]) + f1(nums, target, i + 1, sum - nums[i]);
   }
   
   // 记忆化搜索求解
   int findTargetSumWays2(const vector<int>& nums, int target) {
       unordered_map<int, unordered_map<int, int>> dp;
       return f2(nums, target, 0, 0, dp);
   }
   
   int f2(const vector<int>& nums, int target, int i, int sum, unordered_map<int, unordered_map<int, int>>& dp) {
       if (i == nums.size()) {
           return sum == target ? 1 : 0;
       }
       if (dp[i].count(sum)) {
           return dp[i][sum];
       }
       int ans = f2(nums, target, i + 1, sum + nums[i], dp) + f2(nums, target, i + 1, sum - nums[i], dp);
       dp[i][sum] = ans;
       return ans;
   }
   
   // 动态规划求解（平移技巧）
   int findTargetSumWays3(const vector<int>& nums, int target) {
       int s = 0;
       for (int num : nums) {
           s += num;
       }
       if (target < -s || target > s) {
           return 0;
       }
       int n = nums.size();
       int m = 2 * s + 1;
       vector<vector<int>> dp(n + 1, vector<int>(m, 0));
       dp[n][target + s] = 1;
   
       for (int i = n - 1; i >= 0; --i) {
           for (int j = -s; j <= s; ++j) {
               if (j + nums[i] + s < m) {
                   dp[i][j + s] = dp[i + 1][j + nums[i] + s];
               }
               if (j - nums[i] + s >= 0) {
                   dp[i][j + s] += dp[i + 1][j - nums[i] + s];
               }
           }
       }
       return dp[0][s];
   }
   // 新思路，转化为01背包问题
   // 思考1:
   // 虽然题目说nums是非负数组，但即使nums中有负数比如[3,-4,2]
   // 因为能在每个数前面用+或者-号
   // 所以[3,-4,2]其实和[3,4,2]会达成一样的结果
   // 所以即使nums中有负数，也可以把负数直接变成正数，也不会影响结果
   // 思考2:
   // 如果nums都是非负数，并且所有数的累加和是sum
   // 那么如果target>sum，很明显没有任何方法可以达到target，可以直接返回0
   // 思考3:
   // nums内部的数组，不管怎么+和-，最终的结果都一定不会改变奇偶性
   // 所以，如果所有数的累加和是sum，并且与target的奇偶性不一样
   // 那么没有任何方法可以达到target，可以直接返回0
   // 思考4(最重要):
   // 比如说给定一个数组, nums = [1, 2, 3, 4, 5] 并且 target = 3
   // 其中一个方案是 : +1 -2 +3 -4 +5 = 3
   // 该方案中取了正的集合为A = {1，3，5}
   // 该方案中取了负的集合为B = {2，4}
   // 所以任何一种方案，都一定有 sum(A) - sum(B) = target
   // 现在我们来处理一下这个等式，把左右两边都加上sum(A) + sum(B)，那么就会变成如下：
   // sum(A) - sum(B) + sum(A) + sum(B) = target + sum(A) + sum(B)
   // 2 * sum(A) = target + 数组所有数的累加和
   // sum(A) = (target + 数组所有数的累加和) / 2
   // 也就是说，任何一个集合，只要累加和是(target + 数组所有数的累加和) / 2
   // 那么就一定对应一种target的方式
   // 比如非负数组nums，target = 1, nums所有数累加和是11
   // 求有多少方法组成1，其实就是求，有多少种子集累加和达到6的方法，(1+11)/2=6
   // 因为，子集累加和6 - 另一半的子集累加和5 = 1(target)
   // 所以有多少个累加和为6的不同集合，就代表有多少个target==1的表达式数量
   // 至此已经转化为01背包问题了
   // 转化为01背包问题
   int findTargetSumWays4(const vector<int>& nums, int target) {
       int sum = 0;
       for (int num : nums) {
           sum += num;
       }
       if (sum < target || ((target & 1) ^ (sum & 1)) == 1) {
           return 0;
       }
       return subsets(nums, (target + sum) / 2);
   }
   
   int subsets(const vector<int>& nums, int t) {
       if (t < 0) {
           return 0;
       }
       vector<int> dp(t + 1, 0);
       dp[0] = 1;
       for (int num : nums) {
           for (int j = t; j >= num; --j) {
               dp[j] += dp[j - num];
           }
       }
       return dp[t];
   }                                                                                                                                           
   
   int main() {
       vector<int> nums = {1, 1, 1, 1, 1}; // 示例数据
       int target = 3; // 示例目标
   
       cout << "暴力递归求解: " << findTargetSumWays1(nums, target) << endl;
       cout << "记忆化搜索求解: " << findTargetSumWays2(nums, target) << endl;
       cout << "动态规划求解: " << findTargetSumWays3(nums, target) << endl;
       cout << "转化为01背包求解: " << findTargetSumWays4(nums, target) << endl;
   
       return 0;
   }
   ```

3. 最后一块石头的重量 II

   有一堆石头，用整数数组 stones 表示，其中 stones[i] 表示第 i 块石头的重量

   每一回合，从中选出任意两块石头，然后将它们一起粉碎

   假设石头的重量分别为 x 和 y，且 x <= y

   那么粉碎的可能结果如下：

   如果 x == y，那么两块石头都会被完全粉碎；

   如果 x != y，那么重量为 x 的石头将会完全粉碎，而重量为 y 的石头新重量为 y-x

   最后，最多只会剩下一块 石头，返回此石头 最小的可能重量

   如果没有石头剩下，就返回 0

   测试链接 : <https://leetcode.cn/problems/last-stone-weight-ii/>

   ```c++
   #include <vector>
   #include <algorithm>
   #include <numeric> // for accumulate
   #include <iostream>
   
   class LastStoneWeightII {
   public:
       // 主函数：计算最后剩下石头的最小重量
       int lastStoneWeightII(std::vector<int>& stones) {
           // 计算石头总重量
           int sum = std::accumulate(stones.begin(), stones.end(), 0);
   
           // 计算最接近 sum / 2 的子集重量
           int nearSum = findClosestSubsetSum(stones, sum / 2);
   
           // 返回最后剩下石头的最小可能重量
           return sum - 2 * nearSum;
       }
   
   private:
       // 辅助函数：找到不超过 t 的子集最大重量
       int findClosestSubsetSum(const std::vector<int>& stones, int t) {
           // 定义 dp 数组，dp[j] 表示不超过重量 j 的最大子集重量
           std::vector<int> dp(t + 1, 0);
   
           // 遍历每块石头
           for (int stone : stones) {
               // 倒序遍历，以避免重复使用同一块石头
               for (int j = t; j >= stone; --j) {
                   // 更新 dp[j]: 要么不选当前石头，要么选当前石头
                   dp[j] = std::max(dp[j], dp[j - stone] + stone);
               }
           }
   
           // 返回不超过 t 的最大子集重量
           return dp[t];
       }
   };
   
   // 测试用例
   int main() {
       LastStoneWeightII solution;
   
       // 示例输入
       std::vector<int> stones = {2, 7, 4, 1, 8, 1};
   
       // 输出结果
       std::cout << "Minimum possible weight of the last stone: " 
                 << solution.lastStoneWeightII(stones) << std::endl;
   
       return 0;
   }`
   ```

4. 非负数组前k个最小的子序列累加和

   给定一个数组nums，含有n个数字，都是非负数

   给定一个正数k，返回所有子序列中累加和最小的前k个累加和

   子序列是包含空集的

   1 <= n <= 10^5

   1 <= nums[i] <= 10^6

   1 <= k <= 10^5

   注意这个数据量，用01背包的解法是不行的，时间复杂度太高了

   对数器验证

   时间复杂度O(n*logn) + O(k*logk)，额外空间复杂度O(k)

   ```c++
   #include <iostream>
   #include <vector>
   #include <queue>
   #include <algorithm>
   #include <numeric>
   #include <cstdlib>
   #include <ctime>
   using namespace std;
   
   // 暴力方法
   // 用于验证结果
   void generateSubsequences(const vector<int>& nums, int index, int sum, vector<int>& allSubsequences) {
       if (index == nums.size()) {
           allSubsequences.push_back(sum);
           return;
       }
       // 不选择当前元素
       generateSubsequences(nums, index + 1, sum, allSubsequences);
       // 选择当前元素
       generateSubsequences(nums, index + 1, sum + nums[index], allSubsequences);
   }
   
   vector<int> topKSum1(const vector<int>& nums, int k) {
       vector<int> allSubsequences;
       generateSubsequences(nums, 0, 0, allSubsequences);
       sort(allSubsequences.begin(), allSubsequences.end());
       vector<int> result(k);
       for (int i = 0; i < k; ++i) {
           result[i] = allSubsequences[i];
       }
       return result;
   }
   
   // 01背包方法
   // 这种方法在过大的输入下不适用
   vector<int> topKSum2(const vector<int>& nums, int k) {
       int sum = accumulate(nums.begin(), nums.end(), 0);
       vector<int> dp(sum + 1, 0);
       dp[0] = 1;
       for (int num : nums) {
           for (int j = sum; j >= num; --j) {
               dp[j] += dp[j - num];
           }
       }
       vector<int> result;
       for (int j = 0; j <= sum && result.size() < k; ++j) {
           for (int i = 0; i < dp[j] && result.size() < k; ++i) {
               result.push_back(j);
           }
       }
       return result;
   }
   
   // 使用优先级队列的最优解
   vector<int> topKSum3(const vector<int>& nums, int k) {
       vector<int> sortedNums = nums;
       sort(sortedNums.begin(), sortedNums.end());
   
       // 优先级队列以累加和从小到大排序
       priority_queue<pair<int, int>, vector<pair<int, int>>, greater<>> heap;
   
       heap.push({sortedNums[0], 0}); // 将最小的元素加入队列
       vector<int> result;
   
       for (int i = 0; i < k; ++i) {
           auto [sum, index] = heap.top();
           heap.pop();
           result.push_back(sum);
   
           // 如果仍有正序元素可选择，则继续增加
           if (index + 1 < sortedNums.size()) {
               heap.push({sum + sortedNums[index + 1] - sortedNums[index], index + 1});
               heap.push({sum + sortedNums[index + 1], index + 1});
           }
       }
       return result;
   }
   
   // 生成随机数组，用于测试
   vector<int> randomArray(int len, int value) {
       vector<int> result(len);
       for (int i = 0; i < len; ++i) {
           result[i] = rand() % value;
       }
       return result;
   }
   
   // 比较两个数组是否相等
   bool equals(const vector<int>& a, const vector<int>& b) {
       if (a.size() != b.size()) {
           return false;
       }
       for (size_t i = 0; i < a.size(); ++i) {
           if (a[i] != b[i]) {
               return false;
           }
       }
       return true;
   }
   
   // 为了测试
   int main() {
       srand(time(0));
       int n = 15;
       int v = 40;
       int testTime = 5000;
       cout << "\u6d4b\u8bd5\u5f00\u59cb" << endl;
       for (int i = 0; i < testTime; ++i) {
           int len = rand() % n + 1;
           vector<int> nums = randomArray(len, v);
           int k = rand() % ((1 << len) - 1) + 1;
           vector<int> ans1 = topKSum1(nums, k);
           vector<int> ans2 = topKSum2(nums, k);
           vector<int> ans3 = topKSum3(nums, k);
           if (!equals(ans1, ans2) || !equals(ans1, ans3)) {
               cout << "\u51fa\u9519\u4e86!" << endl;
           }
       }
       cout << "\u6d4b\u8bd5\u7ed3\u675f" << endl;
       return 0;
   }
   ```

###  多重背包

```c++
#include <bits/stdc++.h>
using namespace std;
const int N = 0x3f3f3f3f;
int n, m;
int v[N], w[N], s[N];
int main() {
    cin >> n >> m
    for(int i = 1; i <= n; i++) cin >> v[i] >> w[i] >> s[i];
    for(int i = 1; i <= n; i++) {
        for(int j = 0; j <= m; j++) {
            for(int k = 0; k <= s[i] && w[i] * k <= j; k++) {
                dp[i][j] = max(dp[i][j], dp[i - 1][j - k * w[i]] + k * v[i]);
            }
        }
    }
    cout <<  dp[n][m] << endl;
    return 0;
}
```

#### 二进制优化

```c++
#include <bits/stdc++.h>
using namespace std;

const int N = 12010, M = 2010;
int n, m;
int v[N], w[N];
int dp[M];

int main()
{
    cin >> n >> m;
    int cnt = 1; 
    for (int i = 1; i <= n; i++)
    {
        int a, b, c; 
        cin >> a >> b >> c;

        int s = 1;
        while (s <= c)
        {
            v[cnt] = a * s;
            w[cnt] = b * s;
            cnt++;
            c -= s;
            s *= 2;
        }
        if (c > 0)
        {
            v[cnt] = a * c;
            w[cnt] = b * c;
            cnt++; 
        }
    }
    n = cnt - 1; 
    for (int i = 1; i <= n; i++)
    {
        for (int j = m; j >= w[i]; j--) 
        {
            dp[j] = max(dp[j], dp[j - w[i]] + v[i]);
        }
    }
    cout << dp[m] << endl;
    return 0;
}

```

### 分组背包

- 总共有 `n` 组物品。
- 每组内有若干个物品（第 `i` 组有 `s[i]` 个）。
- 每组最多只能选一个物品（或者不选）。
- 给定背包容量 `m`，问能获得的最大价值是多少。

```cpp
using namespace std;
const int N = 110;
int f[N]; // f[j] 表示容量为 j 时的最大价值
int v[N][N], w[N][N], s[N]; // v[i][j] 表示第 i 组第 j 件物品的体积，w 是价值，s[i] 是第 i 组的物品数量
int n, m; // n 是组数，m 是背包容量

int main() {
    cin >> n >> m;
    for(int i = 0; i < n; i++) {
        cin >> s[i]; // 每组物品数量
        for(int j = 0; j < s[i]; j++) {
            cin >> v[i][j] >> w[i][j]; // 输入每个物品的体积和价值
        }
    }

    // 分组背包核心 DP
    for(int i = 0; i < n; i++) { // 枚举每一组
        for(int j = m; j >= 0; j--) { // 背包容量从大到小枚举
            for(int k = 0; k < s[i]; k++) { // 枚举第 i 组中的每个物品
                if(j >= v[i][k]) {
                    f[j] = max(f[j], f[j - v[i][k]] + w[i][k]);
                }
            }
        }
    }

    cout << f[m] << endl; // 输出最大价值
}
```

### 完全背包

完全背包每件物品可以装无限次

```c++
using namespace std;
const int N = 1010; // 假设最大容量为 1000，可以根据需要调整
int v[N], w[N];     // v[i] 是第 i 个物品体积，w[i] 是价值
int dp[N];          // dp[j] 表示容量为 j 时的最大价值

int main() {
    int n, m;
    cin >> n >> m;
    for (int i = 1; i <= n; i++) {
        cin >> v[i] >> w[i];
        // 关键点：完全背包必须是正序遍历
        for (int j = v[i]; j <= m; j++) {
            dp[j] = max(dp[j], dp[j - v[i]] + w[i]);
        }
    }
    cout << dp[m] << endl; // 输出容量为 m 时的最大价值
    return 0;
}

```

### 区间DP

每堆石子有一定的质量，可以用一个整数来描述，现在要将这 N堆石子合并成为一堆。 每次只能合并相邻的两堆，合并的代价为这两堆石子的质量之和，合并后与这两堆石子相邻的石子 将和新堆相邻，合并时由于选择的顺序不同，合并的总代价也不相同。

```c++
const int N = 310;
int n;
int s[N];       // 前缀和数组，用来快速求区间和 s[r] - s[l-1]
int f[N][N];    // f[l][r] 表示把区间 [l, r] 的石子合并成一堆的最小代价

int main() {
    scanf("%d", &n);
    for (int i = 1; i <= n; i++) scanf("%d", &s[i]);
    // 构建前缀和数组，s[i] 表示前 i 堆石子的总数量
    for (int i = 1; i <= n; i++) s[i] += s[i - 1];

    // 枚举区间长度
    for (int len = 2; len <= n; len++) {
        for (int i = 1; i + len - 1 <= n; i++) {
            int l = i, r = i + len - 1;
            f[l][r] = 1e9; // 初始化为无穷大
            // 枚举分割点 k，把 [l, r] 分成 [l, k] 和 [k+1, r]
            for (int k = l; k < r; k++) {
                f[l][r] = min(f[l][r], f[l][k] + f[k + 1][r] + s[r] - s[l - 1]);
            }
        }
    }

    printf("%d\n", f[1][n]); // 输出最小合并代价
    return 0;
}
```

### 计数类DP

整数划分(完全背包写法)

一个正整数 n 可以表示成若干个正整数之和，我们将这样的一种表示称为正整数 n 的一种划分。 现在给定一个正整数 n，请你求出 n共有多少种不同的划分方法。

```c++
//完全背包的写法
using namespace std;
const int M=1e9+7;
int f[1010],n;
int main()
{
	cin>>n;
	f[0]=1;
	for (int i = 1; i <= n; i ++ )
		for (int j = i; j <= n; j ++ ){
			f[j]=(f[j-i]+f[j])%M;
		}
	cout<<f[n]<<endl;
	return 0;
}
```

### 数位统计DP

```c++
#include <iostream>
#include <vector>
using namespace std;

// 获取数字数组 num 从第 r 位到第 l 位组成的整数（高位在前，低位在后）
int get(vector<int> num, int l, int r) {
    int res = 0;
    for (int i = l; i >= r; i--)
        res = res * 10 + num[i];
    return res;
}

// 返回 10 的 i 次方
int power10(int i) {
    int res = 1;
    while (i--) res *= 10;
    return res;
}

// 统计从 1 到 n 中数字 x 出现了多少次
int count(int n, int x) {
    if (!n) return 0;
    vector<int> num;
    // 将 n 拆成各个位的数字，低位在前，高位在后
    while (n) {
        num.push_back(n % 10);
        n /= 10;
    }
    n = num.size();  // 数字的位数
    int res = 0;
    for (int i = n - 1 - !x; i >= 0; i--) {
        // 枚举第 i 位（从高位向低位）
        if (i < n - 1) {
            res += get(num, n - 1, i + 1) * power10(i);
            if (!x) res -= power10(i);  // 去除前导 0 的非法情况
        }
        if (num[i] == x)
            res += get(num, i - 1, 0) + 1;
        else if (num[i] > x)
            res += power10(i);
    }
    return res;
}

int main() {
    int a, b;
    while (cin >> a >> b, a || b) {
        if (a > b) swap(a, b);  // 保证 a <= b
        for (int i = 0; i < 10; i++)
            cout << count(b, i) - count(a - 1, i) << ' ';
        cout << endl;
    }
    return 0;
}
```

### 状态压缩DP

[题目链接](https://www.luogu.com.cn/problem/U204941)

[视频讲解](https://www.bilibili.com/video/BV1cv411b7EG/)

```c++
#include <iostream>
#include <cstring>
using namespace std;

const int N = 12, M = 1 << N;
long long f[N][M]; // f[i][j] 表示第 i 列状态为 j 的合法方案数
bool st[M];        // st[i] 表示状态 i 是否合法
int main() {
    int n, m;
    while (cin >> n >> m, n || m) {
        memset(f, 0, sizeof f);
        // 预处理所有状态是否合法
        for (int i = 0; i < 1 << n; i++) {
            st[i] = true;
            int cnt = 0; // 连续0的个数
            for (int j = 0; j < n; j++) {
                if (i >> j & 1) { // 当前位为1，代表放了横向的骨牌
                    if (cnt & 1) { // 如果前面有奇数个连续0
                        st[i] = false;
                        break;
                    }
                    cnt = 0; // 重置
                } else {
                    cnt++;
                }
            }
            if (cnt & 1) st[i] = false; // 检查末尾是否也有奇数个0
        }
        f[0][0] = 1; // 初始状态：第0列为空，是一种合法方案
        // 状态转移
        for (int i = 1; i <= m; i++) { // 第 i 列
            for (int j = 0; j < 1 << n; j++) { // 当前列的状态
                for (int k = 0; k < 1 << n; k++) { // 前一列的状态
                    if ((j & k) == 0 && st[j | k]) {
                        f[i][j] += f[i - 1][k];
                    }
                }
            }
        }
        cout << f[m][0] << endl; // 最终目标：第 m 列不放横向骨牌（全由竖放拼接）
    }
    return 0;
}
```

### [树型DP](https://www.bilibili.com/video/BV1eK411N7Ly/)

```c++
#include <iostream>
#include <cstring>
using namespace std;
const int N = 6010;

int n;
int w[N];          // 每个节点（员工）的高兴度
int h[N], e[N], ne[N], idx; // 邻接表存储树
bool st[N];        // 判断是否有父节点（用于找根节点）
int f[N][2];       // f[i][0]: 不选i的最大高兴度, f[i][1]: 选i的最大高兴度

// 添加一条边 a -> b，表示 b 是 a 的子节点
void add(int a, int b) {
    e[idx] = b;
    ne[idx] = h[a];
    h[a] = idx++;
}

// 树形DP核心函数
void dfs(int u) {
    f[u][0] = 0;
    f[u][1] = w[u];
    for (int i = h[u]; i != -1; i = ne[i]) {
        int j = e[i];
        dfs(j);  // 遍历子节点
        // 状态转移
        f[u][0] += max(f[j][0], f[j][1]); // 不选当前节点，可以选或不选子节点
        f[u][1] += f[j][0];               // 选当前节点，不能选子节点
    }
}

int main() {
    cin >> n;
    // 读取每个员工的高兴度
    for (int i = 1; i <= n; i++) {
        cin >> w[i];
    }

    memset(h, -1, sizeof h); // 邻接表初始化

    // 输入边（子父关系），并建立反向边（从父指向子）
    for (int i = 0; i < n - 1; i++) {
        int a, b;
        cin >> a >> b;
        add(b, a);     // b 是 a 的上司 => 建边 b->a
        st[a] = true;  // a 有父节点
    }

    // 找根节点（没有父节点的那个）
    int root = 1;
    while (st[root]) root++;

    // 从根节点开始DFS
    dfs(root);

    // 输出结果：选或不选根节点的最大值
    cout << max(f[root][0], f[root][1]) << endl;

    return 0;
}
```



## 贪心算法

狭义的贪心:每一步都做出在当前状态下最好或最优的选择，从而希望最终的结果是最好或最优的算法

广义的贪心:通过分析题目自身的特点和性质，只要发现让求解答案的过程得到加速的结论，都算广义的贪心

贪心是最符合自然智慧的思想，一般分析门槛不高

理解基本的排序、有序结构，有基本的逻辑思维就能理解

但是贪心的题目，千题千面，极难把握

有关贪心的若干现实 & 提醒
1，不要去纠结严格证明，每个题都去追求严格证明，浪费时间、收益很低，而且千题千面。玄学！
2，一定要掌握用对数器验证的技巧，这是解决贪心问题的关键
3，解法几乎只包含贪心思路的题目，代码量都不大
4，大量累积贪心的经验，重点不是证明，而是题目的特征，以及贪心方式的特征，做好总结方便借鉴
5，关注题目数据量，题目的解可能来自贪心，也很可能不是，如果数据量允许，能不用贪心就不用（稳）
6，贪心在笔试中出现概率不低，但是面试中出现概率较低，原因是 淘汰率 vs 区分度
7，广义的贪心无所不在，可能和别的思路结合，一般都可以通过自然智慧想明白，依然不纠结证明

##### 题目

- 最大数

  给定一组非负整数nums

  重新排列每个数的顺序（每个数不可拆分）使之组成一个最大的整数

  测试链接 : <https://leetcode.cn/problems/largest-number/>

```c++
#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
#include <random>

// 最大数
// 给定一组非负整数nums，重新排列每个数的顺序（每个数不可拆分）
// 使之组成一个最大的整数

using namespace std;

// 全排列代码：生成所有排列，返回字典序最小的拼接结果（暴力方法）
string way1(vector<string>& strs) {
    vector<string> ans;
    f(strs, 0, ans);
    sort(ans.begin(), ans.end());  // 字典序最小排序
    return ans[0];
}

// 全排列的递归函数
void f(vector<string>& strs, int i, vector<string>& ans) {
    if (i == strs.size()) {
        string path = "";
        for (const string& s : strs) {
            path += s;
        }
        ans.push_back(path);
    } else {
        for (int j = i; j < strs.size(); j++) {
            swap(strs[i], strs[j]);  // 交换位置
            f(strs, i + 1, ans);
            swap(strs[i], strs[j]);  // 回溯，恢复原状
        }
    }
}

// 生成一个字符串数组，包含最大数字字典序最小的拼接结果（正式方法，时间复杂度O(n*logn)）
string way2(vector<string>& strs) {
    sort(strs.begin(), strs.end(), [](const string& a, const string& b) {
        return (a + b) < (b + a);  // 字符串拼接排序
    });

    string result = "";
    for (const string& s : strs) {
        result += s;
    }
    return result;
}

// 生成随机字符串数组用于测试
vector<string> randomStringArray(int n, int m, int v) {
    random_device rd;
    mt19937 gen(rd());
    uniform_int_distribution<> dist(1, n);  // 数组长度

    int length = dist(gen);
    vector<string> ans(length);
    
    for (int i = 0; i < length; i++) {
        ans[i] = randomString(m, v);
    }
    return ans;
}

// 生成随机字符串
string randomString(int m, int v) {
    random_device rd;
    mt19937 gen(rd());
    uniform_int_distribution<> dist(1, m);  // 字符串长度
    uniform_int_distribution<> char_dist(0, v - 1);  // 字符种类

    int len = dist(gen);
    string str = "";
    
    for (int i = 0; i < len; i++) {
        char c = 'a' + char_dist(gen);  // 随机字符
        str += c;
    }
    return str;
}

// 测试代码：验证两种方法的正确性
void test() {
    int n = 8;  // 数组中最多几个字符串
    int m = 5;  // 字符串长度最大多长
    int v = 4;  // 字符的种类有几种
    int testTimes = 2000;

    cout << "测试开始" << endl;
    for (int i = 1; i <= testTimes; i++) {
        vector<string> strs = randomStringArray(n, m, v);
        string ans1 = way1(strs);
        string ans2 = way2(strs);
        if (ans1 != ans2) {
            cout << "出错了！" << endl;
        }
        if (i % 100 == 0) {
            cout << "测试到第" << i << "组" << endl;
        }
    }
    cout << "测试结束" << endl;
}

// 将整数数组转换为最大的拼接数
string largestNumber(vector<int>& nums) {
    vector<string> strs;
    for (int num : nums) {
        strs.push_back(to_string(num));
    }

    sort(strs.begin(), strs.end(), [](const string& a, const string& b) {
        return (a + b) > (b + a);  // 字符串拼接排序
    });

    // 如果排序后最前面的元素是"0"，则说明所有数字都是0，直接返回"0"
    if (strs[0] == "0") {
        return "0";
    }

    string result = "";
    for (const string& s : strs) {
        result += s;
    }
    return result;
}

// 主函数：用于测试
int main() {
    // 测试用例
    vector<int> nums = {10, 2};
    cout << "最大数为: " << largestNumber(nums) << endl;  // 输出: 210
    // 调用测试函数
    test();
    return 0;
}
```

- 题目2

  两地调度

  公司计划面试2n个人，给定一个数组 costs

  其中costs[i]=[aCosti, bCosti]

  表示第i人飞往a市的费用为aCosti，飞往b市的费用为bCosti

  返回将每个人都飞到a、b中某座城市的最低费用

  要求每个城市都有n人抵达

  测试链接 : <https://leetcode.cn/problems/two-city-scheduling/>

```c++
#include <iostream>
#include <vector>
#include <algorithm>

// 两地调度
// 公司计划面试2n个人，给定一个二维数组costs，其中costs[i] = [aCosti, bCosti]
// 表示第i个人飞往a市的费用为aCosti，飞往b市的费用为bCosti
// 返回将每个人都飞到a、b中某座城市的最低费用
// 要求每个城市都有n人抵达

using namespace std;

// 两地调度的核心方法
int twoCitySchedCost(vector<vector<int>>& costs) {
    int n = costs.size();
    vector<int> arr(n);
    int sum = 0;

    // 计算每个人飞往a市的费用之和，并计算costs[i][1] - costs[i][0]的差值存入arr数组
    for (int i = 0; i < n; i++) {
        arr[i] = costs[i][1] - costs[i][0];
        sum += costs[i][0];  // 先将所有人飞往a市的费用加到总费用中
    }

    // 对arr数组进行排序，找出最小的差值
    sort(arr.begin(), arr.end());

    // 计算选择飞往b市的最小费用
    int m = n / 2;
    for (int i = 0; i < m; i++) {
        sum += arr[i];  // 将差值添加到总费用中，最小的差值使得费用最小
    }

    return sum;
}

// 主函数
int main() {
    // 测试用例：给定一个二维数组costs
    vector<vector<int>> costs = {{10, 20}, {30, 50}, {50, 30}, {10, 10}};
    
    // 调用函数并输出结果
    cout << "最小调度费用为: " << twoCitySchedCost(costs) << endl;  // 输出: 140

    return 0;
}
```

- 题目3

  吃掉N个橘子的最少天数

  厨房里总共有 n 个橘子，你决定每一天选择如下方式之一吃这些橘子

  1）吃掉一个橘子

  2）如果剩余橘子数 n 能被 2 整除，那么你可以吃掉 n/2 个橘子

  3）如果剩余橘子数 n 能被 3 整除，那么你可以吃掉 2*(n/3) 个橘子

  每天你只能从以上 3 种方案中选择一种方案

  请你返回吃掉所有 n 个橘子的最少天数

  测试链接 : 

  <https://leetcode.cn/problems/minimum-number-of-days-to-eat-n-oranges/>

```c++
#include <iostream>
#include <unordered_map>
#include <algorithm>

// 吃掉N个橘子的最少天数
// 厨房里总共有 n 个橘子，每天你可以选择以下方式之一吃掉橘子：
// 1）吃掉一个橘子
// 2) 如果剩余橘子数 n 能被 2 整除，可以吃掉 n/2 个橘子
// 3) 如果剩余橘子数 n 能被 3 整除，可以吃掉 2*(n/3) 个橘子
// 你需要返回吃掉所有 n 个橘子的最少天数

using namespace std;

// 哈希表用于缓存计算结果，避免重复计算
unordered_map<int, int> dp;

// 最少天数计算函数
int minDays(int n) {
    // 基本情况：如果剩余橘子数量为 0 或 1，直接返回对应的天数
    if (n <= 1) {
        return n;
    }
    
    // 如果已计算过这个值，直接返回缓存中的结果
    if (dp.find(n) != dp.end()) {
        return dp[n];
    }

    // 计算三种情况的最少天数：
    // 1) 吃掉一个橘子，剩下 n-1 个橘子，调用递归
    // 2) 如果 n 能被 2 整除，先吃掉一半，再递归
    // 3) 如果 n 能被 3 整除，先吃掉三分之二，再递归
    int ans = min(n % 2 + 1 + minDays(n / 2), n % 3 + 1 + minDays(n / 3));

    // 将计算结果存入缓存
    dp[n] = ans;
    return ans;
}

// 主函数
int main() {
    // 测试示例
    int n = 10;  // 输入橘子的数量
    cout << "吃掉 " << n << " 个橘子的最少天数是: " << minDays(n) << endl;  // 输出结果
    
    return 0;
}
```

- 题目4

  会议室II

  给你一个会议时间安排的数组 intervals

  每个会议时间都会包括开始和结束的时间intervals[i]=[starti, endi]

  返回所需会议室的最小数量

  测试链接 : https://leetcode.cn/problems/meeting-rooms-ii/

```c++
#include <iostream>
#include <vector>
#include <algorithm>
#include <queue>
using namespace std;
// 会议室 II
// 给定一个会议时间安排的数组 intervals
// 每个会议时间都包含开始和结束的时间 intervals[i] = [starti, endi]
// 返回所需会议室的最小数量

// 会议室最小数量计算函数
int minMeetingRooms(vector<vector<int>>& meeting) {
    int n = meeting.size();

    // 按照会议的开始时间排序
    sort(meeting.begin(), meeting.end(), [](const vector<int>& a, const vector<int>& b) {
        return a[0] < b[0];
    });

    // 使用优先队列（最小堆）来追踪正在使用的会议室的结束时间
    priority_queue<int, vector<int>, greater<int>> heap;

    int ans = 0;
    
    // 遍历所有会议
    for (int i = 0; i < n; i++) {
        // 如果当前会议的开始时间 >= 堆中最早结束的会议的结束时间
        // 说明可以重用这间会议室
        while (!heap.empty() && heap.top() <= meeting[i][0]) {
            heap.pop();  // 释放已经结束的会议室
        }

        // 为当前会议分配一个会议室，加入堆中，堆顶为最早结束时间
        heap.push(meeting[i][1]);

        // 更新会议室的最小数量
        ans = max(ans, (int)heap.size());
    }

    return ans;
}

// 主函数
int main() {
    // 测试示例：会议时间安排
    vector<vector<int>> meetings = {{0, 30}, {5, 10}, {15, 20}};
    
    // 输出所需的最小会议室数量
    cout << "所需最少会议室数量: " << minMeetingRooms(meetings) << endl;  

    return 0;
}
```

- 题目5

  课程表III

  这里有n门不同的在线课程，按从1到n编号

  给你一个数组courses

  其中courses[i]=[durationi, lastDayi]表示第i门课将会持续上durationi天课

  并且必须在不晚于lastDayi的时候完成

  你的学期从第 1 天开始

  且不能同时修读两门及两门以上的课程

  返回你最多可以修读的课程数目

  测试链接 : <https://leetcode.cn/problems/course-schedule-iii/>

```c++
#include <iostream>
#include <vector>
#include <algorithm>
#include <queue>
using namespace std;
// 课程表 III
// 给定 n 门不同的在线课程，每个课程有两个参数：持续时间和最迟结束日期。
// 我们要选择最多的课程，要求每门课程在它的截止日期前完成，且每次只能选一门课程。
int scheduleCourse(vector<vector<int>>& courses) {
    // 0: 课程持续时间
    // 1: 课程最迟结束时间
    // 按照课程的截止日期排序
    sort(courses.begin(), courses.end(), [](const vector<int>& a, const vector<int>& b) {
        return a[1] < b[1];  // 按照截止日期排序
    });
    // 大根堆：用来存储已选课程的持续时间
    priority_queue<int> heap;
    int time = 0;  // 当前已经上过的课程的总时间
    // 遍历所有课程
    for (const auto& c : courses) {
        // 如果当前课程的持续时间可以在其截止日期前完成
        if (time + c[0] <= c[1]) {
            heap.push(c[0]);  // 将课程的持续时间加入堆中
            time += c[0];  // 增加已修课程的时间
        } else {
            // 如果当前课程的持续时间超过了截止日期
            // 只有在已选课程中有比当前课程持续时间更长的课程时，才进行替换
            if (!heap.empty() && heap.top() > c[0]) {
                // 取消最耗时的课程，加入当前课程
                time += c[0] - heap.top();
                heap.pop();  // 移除堆顶课程
                heap.push(c[0]);  // 将当前课程加入堆
            }
        }
    }
    // 堆的大小即为能够完成的课程数
    return heap.size();
}

// 主函数
int main() {
    // 测试示例：每个课程的 [持续时间, 最迟结束时间]
    vector<vector<int>> courses = {{100, 200}, {200, 1300}, {300, 1200}, {400, 1000}};
    // 输出最多可以修读的课程数量
    cout << "最多可以修读的课程数量: " << scheduleCourse(courses) << endl;  // 输出: 3
    return 0;
}
```

- 题目6

  连接棒材的最低费用

  你有一些长度为正整数的棍子

  这些长度以数组sticks的形式给出

  sticks[i]是第i个木棍的长度

  你可以通过支付x+y的成本将任意两个长度为x和y的棍子连接成一个棍子

  你必须连接所有的棍子，直到剩下一个棍子

  返回以这种方式将所有给定的棍子连接成一个棍子的最小成本

  测试链接 : <https://leetcode.cn/problems/minimum-cost-to-connect-sticks/>

  测试链接 : <https://www.luogu.com.cn/problem/P1090>

  ```c++
  #include <iostream>
  #include <vector>
  #include <queue>
  
  using namespace std;
  
  // 连接棒材的最低费用
  // 给定若干长度的棍子，通过连接棍子来最小化总成本
  // 每次连接两个棍子，成本是这两个棍子长度的和
  // 目标是将所有棍子连接成一个，返回最小的连接成本
  
  int connectSticks(vector<int>& sticks) {
      // 小根堆：用于每次获取最短的两根棍子进行连接
      priority_queue<int, vector<int>, greater<int>> minHeap;
  
      // 将所有棍子的长度加入小根堆中
      for (int stick : sticks) {
          minHeap.push(stick);
      }
  
      int totalCost = 0;  // 用于记录总的连接成本
  
      // 只要堆中有超过一根棍子，就继续连接
      while (minHeap.size() > 1) {
          // 取出最小的两根棍子
          int first = minHeap.top();  // 第一根棍子
          minHeap.pop();
          int second = minHeap.top();  // 第二根棍子
          minHeap.pop();
  
          // 连接这两根棍子，连接成本是它们的和
          int cost = first + second;
          totalCost += cost;
  
          // 将连接后的新棍子加入堆中
          minHeap.push(cost);
      }
  
      return totalCost;
  }
  
  // 主函数
  int main() {
      // 测试示例：棍子长度数组
      vector<int> sticks = {2, 4, 3};
  
      // 输出连接所有棍子的最小成本
      cout << "最小连接成本: " << connectSticks(sticks) << endl;  // 输出: 14
  
      return 0;
  }
  ```




## **建图、链式前向星、拓扑排序**

有向 vs 无向、不带权 vs 带权。入参一般为 节点数量n和所有的边 或者 直接给图

##### 建图的三种方式

1，邻接矩阵（适合点的数量不多的图）

2，邻接表（最常用的方式）

3，链式前向星（空间要求严苛情况下使用。比赛必用，大厂笔试、面试不常用）

```c++
#include <iostream>
#include <vector>
#include <array>
#include <cstring>
using namespace std;

const int MAXN = 11;  // 点的最大数量
const int MAXM = 21;  // 边的最大数量

// 邻接矩阵方式建图
int graph1[MAXN][MAXN];

// 邻接表方式建图
vector<vector<pair<int, int>>> graph2(MAXN);

// 链式前向星方式建图
int head[MAXN], next[MAXM], to[MAXM], weight[MAXM];
int cnt;

// 清空和准备图
void build(int n) {
    // 邻接矩阵清空
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= n; j++) {
            graph1[i][j] = 0;
        }
    }
    // 邻接表清空和准备
    graph2.clear();
    graph2.resize(n + 1);  // 支持1~n的下标
    // 链式前向星清空
    cnt = 1;
    memset(head, 0, sizeof(int) * (n + 1));
}

// 链式前向星加边
void addEdge(int u, int v, int w) {
    // u -> v , 边权重是w
    next[cnt] = head[u];
    to[cnt] = v;
    weight[cnt] = w;
    head[u] = cnt++;
}

// 三种方式建立有向图带权图
void directGraph(const vector<array<int, 3>>& edges) {
    // 邻接矩阵建图
    for (const auto& edge : edges) {
        graph1[edge[0]][edge[1]] = edge[2];
    }
    // 邻接表建图
    for (const auto& edge : edges) {
        graph2[edge[0]].emplace_back(edge[1], edge[2]);
    }
    // 链式前向星建图
    for (const auto& edge : edges) {
        addEdge(edge[0], edge[1], edge[2]);
    }
}

// 三种方式建立无向图带权图
void undirectGraph(const vector<array<int, 3>>& edges) {
    // 邻接矩阵建图
    for (const auto& edge : edges) {
        graph1[edge[0]][edge[1]] = edge[2];
        graph1[edge[1]][edge[0]] = edge[2];
    }
    // 邻接表建图
    for (const auto& edge : edges) {
        graph2[edge[0]].emplace_back(edge[1], edge[2]);
        graph2[edge[1]].emplace_back(edge[0], edge[2]);
    }
    // 链式前向星建图
    for (const auto& edge : edges) {
        addEdge(edge[0], edge[1], edge[2]);
        addEdge(edge[1], edge[0], edge[2]);
    }
}

// 三种方式遍历图
void traversal(int n) {
    cout << "邻接矩阵遍历:" << endl;
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= n; j++) {
            cout << graph1[i][j] << " ";
        }
        cout << endl;
    }

    cout << "邻接表遍历:" << endl;
    for (int i = 1; i <= n; i++) {
        cout << i << "(邻居、边权): ";
        for (const auto& edge : graph2[i]) {
            cout << "(" << edge.first << "," << edge.second << ") ";
        }
        cout << endl;
    }

    cout << "链式前向星遍历:" << endl;
    for (int i = 1; i <= n; i++) {
        cout << i << "(邻居、边权): ";
        for (int ei = head[i]; ei > 0; ei = next[ei]) {
            cout << "(" << to[ei] << "," << weight[ei] << ") ";
        }
        cout << endl;
    }
}

int main() {
    // 理解了带权图的建立过程，也就理解了不带权图
    // 点的编号为1...n
    // 例子1自己画一下图，有向带权图，然后打印结果
    int n1 = 4;
    vector<array<int, 3>> edges1 = { { 1, 3, 6 }, { 4, 3, 4 }, { 2, 4, 2 }, { 1, 2, 7 }, { 2, 3, 5 }, { 3, 1, 1 } };
    build(n1);
    directGraph(edges1);
    traversal(n1);
    cout << "==============================" << endl;
    
    // 例子2自己画一下图，无向带权图，然后打印结果
    int n2 = 5;
    vector<array<int, 3>> edges2 = { { 3, 5, 4 }, { 4, 1, 1 }, { 3, 4, 2 }, { 5, 2, 4 }, { 2, 3, 7 }, { 1, 5, 5 }, { 4, 2, 6 } };
    build(n2);
    undirectGraph(edges2);
    traversal(n2);

    return 0;
}
```

##### 拓扑排序

每个节点的前置节点都在这个节点之前

要求：有向图、没有环

拓扑排序的顺序可能不只一种。拓扑排序也可以用来判断有没有环。

1）在图中找到所有入度为0的点

2）把所有入度为0的点在图中删掉，重点是删掉影响！继续找到入度为0的点并删掉影响

3）直到所有点都被删掉，依次删除的顺序就是正确的拓扑排序结果

4）如果无法把所有的点都删掉，说明有向图里有环

拓扑排序模版

邻接表建图（动态方式）

链式前向星建图（静态方式）

测试链接 : https://leetcode.cn/problems/course-schedule-ii

测试链接 : <https://www.nowcoder.com/practice/88f7e156ca7d43a1a535f619cd3f495c>

```c++
#include <iostream>
#include <vector>
#include <queue>
using namespace std;

vector<int> findOrder(int numCourses, vector<vector<int>>& prerequisites) {
    // 邻接表建图
    vector<vector<int>> graph(numCourses);
    // 入度表
    vector<int> indegree(numCourses, 0);

    // 构建图和入度表
    for (const auto& edge : prerequisites) {
        graph[edge[1]].push_back(edge[0]);
        indegree[edge[0]]++;
    }

    // 队列初始化，存储入度为 0 的课程
    queue<int> q;
    for (int i = 0; i < numCourses; i++) {
        if (indegree[i] == 0) {
            q.push(i);
        }
    }

    // 存储课程的学习顺序
    vector<int> order;
    while (!q.empty()) {
        int current = q.front();
        q.pop();
        order.push_back(current);

        // 更新入度，并将入度为 0 的课程加入队列
        for (int next : graph[current]) {
            if (--indegree[next] == 0) {
                q.push(next);
            }
        }
    }

    // 如果能够学习所有课程，返回课程顺序，否则返回空数组
    if (order.size() == numCourses) {
        return order;
    } else {
        return {};
    }
}

int main() {
    int numCourses = 4;
    vector<vector<int>> prerequisites = {{1, 0}, {2, 0}, {3, 1}, {3, 2}};
    
    vector<int> result = findOrder(numCourses, prerequisites);
    
    if (result.empty()) {
        cout << "No valid order" << endl;
    } else {
        cout << "Valid order: ";
        for (int course : result) {
            cout << course << " ";
        }
        cout << endl;
    }

    return 0;
}
```

##### 题目

- 字典序最小的拓扑排序

  要求返回所有正确的拓扑排序中 字典序最小 的结果

  建图请使用链式前向星方式，因为比赛平台用其他建图方式会卡空间

  测试链接 : <https://www.luogu.com.cn/problem/U107394>

```c++
#include <iostream>
#include <vector>
#include <queue>
#include <cstring>
#include <algorithm>

using namespace std;

const int MAXN = 100001;
const int MAXM = 100001;

int head[MAXN], next[MAXM], to[MAXM];
int cnt = 1;  // 边的计数器
int heap[MAXN], heapSize = 0;  // 小根堆
int indegree[MAXN];  // 入度表
int ans[MAXN];  // 存储拓扑排序结果
int n, m;  // 顶点数和边数

// 清理之前的脏数据
void build(int n) {
    cnt = 1;
    heapSize = 0;
    memset(head, 0, sizeof(int) * (n + 1));
    memset(indegree, 0, sizeof(int) * (n + 1));
}

// 用链式前向星建图
void addEdge(int f, int t) {
    next[cnt] = head[f];
    to[cnt] = t;
    head[f] = cnt++;
}

// 小根堆里加入数字
void push(int num) {
    int i = heapSize++;
    heap[i] = num;
    // heapInsert的过程
    while (i > 0 && heap[i] < heap[(i - 1) / 2]) {
        swap(heap[i], heap[(i - 1) / 2]);
        i = (i - 1) / 2;
    }
}

// 小根堆里弹出最小值
int pop() {
    int ans = heap[0];
    heap[0] = heap[--heapSize];
    // heapify的过程
    int i = 0;
    int l = 1;
    while (l < heapSize) {
        int best = l + 1 < heapSize && heap[l + 1] < heap[l] ? l + 1 : l;
        best = heap[best] < heap[i] ? best : i;
        if (best == i) {
            break;
        }
        swap(heap[best], heap[i]);
        i = best;
        l = i * 2 + 1;
    }
    return ans;
}

// 判断小根堆是否为空
bool isEmpty() {
    return heapSize == 0;
}

// 拓扑排序
void topoSort() {
    // 把入度为0的节点加入堆中
    for (int i = 1; i <= n; i++) {
        if (indegree[i] == 0) {
            push(i);
        }
    }
    int fill = 0;
    while (!isEmpty()) {
        int cur = pop();
        ans[fill++] = cur;
        // 用链式前向星的方式，遍历cur的相邻节点
        for (int ei = head[cur]; ei != 0; ei = next[ei]) {
            if (--indegree[to[ei]] == 0) {
                push(to[ei]);
            }
        }
    }
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    while (cin >> n >> m) {
        build(n);
        for (int i = 0, from, to; i < m; i++) {
            cin >> from >> to;
            addEdge(from, to);
            indegree[to]++;
        }

        topoSort();

        for (int i = 0; i < n - 1; i++) {
            cout << ans[i] << " ";
        }
        cout << ans[n - 1] << "\n";
    }

    return 0;
}
```

- 火星词典

  现有一种使用英语字母的火星语言

  这门语言的字母顺序对你来说是未知的。

  给你一个来自这种外星语言字典的字符串列表 words

  words 中的字符串已经 按这门新语言的字母顺序进行了排序 。

  如果这种说法是错误的，并且给出的 words 不能对应任何字母的顺序，则返回 ""

  否则，返回一个按新语言规则的 字典递增顺序 排序的独特字符串

  如果有多个解决方案，则返回其中任意一个

  words中的单词一定都是小写英文字母组成的

  测试链接 : https://leetcode.cn/problems/alien-dictionary

```c++
#include <iostream>
#include <vector>
#include <string>
#include <queue>
#include <unordered_map>
#include <unordered_set>

using namespace std;

class Solution {
public:
    string alienOrder(vector<string>& words) {
        // 入度表，26种字符
        unordered_map<char, int> indegree;
        unordered_map<char, unordered_set<char>> graph;

        // 初始化每个字符的入度
        for (const string& word : words) {
            for (char c : word) {
                if (indegree.find(c) == indegree.end()) {
                    indegree[c] = 0;
                }
            }
        }

        // 构建图和更新入度
        for (int i = 0; i < words.size() - 1; ++i) {
            string cur = words[i];
            string next = words[i + 1];
            int len = min(cur.size(), next.size());
            for (int j = 0; j < len; ++j) {
                if (cur[j] != next[j]) {
                    // 发现不同字符，建立图的边 cur[j] -> next[j]
                    if (graph[cur[j]].find(next[j]) == graph[cur[j]].end()) {
                        graph[cur[j]].insert(next[j]);
                        indegree[next[j]]++;
                    }
                    break;
                }
            }
            // 如果前一个单词是后一个单词的前缀，则返回空字符串
            if (cur.size() > next.size() && cur.substr(0, next.size()) == next) {
                return "";
            }
        }

        // 使用队列进行拓扑排序
        queue<char> q;
        for (const auto& entry : indegree) {
            if (entry.second == 0) {
                q.push(entry.first);
            }
        }

        string result;
        while (!q.empty()) {
            char cur = q.front();
            q.pop();
            result.push_back(cur);
            for (char neighbor : graph[cur]) {
                if (--indegree[neighbor] == 0) {
                    q.push(neighbor);
                }
            }
        }

        // 如果结果的字符数与字典中的字符数不相等，则说明存在环
        if (result.size() != indegree.size()) {
            return "";
        }

        return result;
    }
};

// 测试
int main() {
    Solution solution;
    vector<string> words = {"wrt", "wrf", "er", "ett", "rftt"};
    string result = solution.alienOrder(words);
    if (result.empty()) {
        cout << "No valid order exists!" << endl;
    } else {
        cout << "Alien dictionary order: " << result << endl;
    }
    return 0;
}
```

- 戳印序列

  你想最终得到"abcbc"，认为初始序列为"?????"。印章是"abc"

  那么可以先用印章盖出"??abc"的状态，

  然后用印章最左字符和序列的0位置对齐，就盖出了"abcbc"

  这个过程中，"??abc"中的a字符，被印章中的c字符覆盖了

  每次盖章的时候，印章必须完全盖在序列内

  给定一个字符串target是最终的目标，长度为n，认为初始序列为n个'?'

  给定一个印章字符串stamp，目标是最终盖出target，但是印章的使用次数必须在10*n次以内

  返回一个数组，该数组由每个回合中被印下的最左边字母的索引组成

  上面的例子返回[2,0]，表示印章最左字符依次和序列2位置、序列0位置对齐盖下去，就得到了target

  如果不能在10*n次内印出序列，就返回一个空数组

  测试链接 : https://leetcode.cn/problems/stamping-the-sequence

```c++
#include <iostream>
#include <vector>
#include <queue>
#include <cstring>
using namespace std;

class Solution {
public:
    vector<int> movesToStamp(string stamp, string target) {
        int m = stamp.size();
        int n = target.size();
        vector<int> indegree(n - m + 1, m);  // 初始化每个位置的indegree
        vector<vector<int>> graph(n);  // 图的表示
        queue<int> q;  // 队列，用来记录已完成的戳印位置
        vector<bool> visited(n, false);  // 用来标记每个位置是否被访问
        vector<int> path;  // 最终结果路径

        // O(n * m)
        for (int i = 0; i <= n - m; ++i) {
            for (int j = 0; j < m; ++j) {
                if (target[i + j] == stamp[j]) {
                    if (--indegree[i] == 0) {
                        q.push(i);
                    }
                } else {
                    graph[i + j].push_back(i);
                }
            }
        }

        while (!q.empty()) {
            int cur = q.front();
            q.pop();
            path.push_back(cur);

            for (int i = 0; i < m; ++i) {
                if (!visited[cur + i]) {
                    visited[cur + i] = true;
                    for (int next : graph[cur + i]) {
                        if (--indegree[next] == 0) {
                            q.push(next);
                        }
                    }
                }
            }
        }

        if (path.size() != n - m + 1) {
            return {};  // 如果不能按要求戳印出目标字符串
        }

        // 结果逆序
        reverse(path.begin(), path.end());
        return path;
    }
};

int main() {
    Solution sol;
    string stamp = "abc";
    string target = "abcbc";
    vector<int> result = sol.movesToStamp(stamp, target);

    for (int idx : result) {
        cout << idx << " ";
    }
    cout << endl;

    return 0;
}
```



## 常用语法&函数

#### 字符串去重

使用 `std::sort` 和 `std::unique` 去除字符串中的重复字符。

std::unique返回去重后的末尾迭代器，可以通过 erase 删除重复部分。

```cpp
// 字符串去重
void removeDuplicateCharacters(string &a) {
    sort(a.begin(), a.end());  // 排序字符串
    a.erase(unique(a.begin(), a.end()), a.end());  // 去重并删除重复区间
}
```

示例用法：

```cpp
string a = "11322";
removeDuplicateCharacters(a);
cout << a;  // 输出：123
```



#### Vector 去重

使用 `std::sort` 和 `std::unique` 删除 `vector` 中的重复元素。

```cpp
// Vector 去重
void removeDuplicateInVector(vector<int> &q) {
    sort(q.begin(), q.end());  // 排序
    q.erase(unique(q.begin(), q.end()), q.end());  // 去重并删除重复区间
}
```

**示例用法**：

```cpp
vector<int> q = {1, 1, 2, 3};
removeDuplicateInVector(q);
for (auto i : q) cout << i << ' ';  // 输出：1 2 3
```



#### 字符串和数字互相转换

```c++
int num1 = stoi(str1);  // 使用 stoi 转换
long long num2 = stoll(str2);  // 使用 stoll 转换
float num3 = stof(str3);  // 使用 stof 转换
string str1 = to_string(num1);  // 使用 to_string 转换
```



#### 读取整行字符串

使用 `getline()` 来读取包含空格的字符串。

```cpp
// 使用 getline() 读取整行字符串
void readLineExample() {
    string s;
    getline(cin, s);  // 从标准输入中读取一行
    cout << s << endl;  // 输出输入的内容
}
```



#### 字符串替换

使用 `replace()` 方法替换字符串中的某些部分。

```cpp
// 替换字符串内容
void replaceExample() {
    string s = "hello,world!";
    s.replace(6, 5, "girl");  // 结果："hello,girl!"
    s.replace(s.begin() + 6, s.begin() + 10, "boy");  // 结果："hello,boy!"
}
```



#### 插入字符串或字符：

使用 `insert()` 方法在字符串中指定位置插入字符或子字符串。

```cpp
// 插入字符串或字符
void insertExample() {
    string s = "12345";
    s.insert(2, "abc");  // 结果："12abc345"
    s.insert(s.begin() + 1, 'x');  // 结果："1x2abc345"
}
```



#### 删除字符串内容

使用 `erase()` 删除指定位置的字符或子串。

```cpp
// 删除字符串内容
void eraseExample() {
    string s = "123456789";
    s.erase(s.begin() + 1);  // 结果："13456789"
    s.erase(1, 3);  // 结果："16789",删除3个字符
    s.erase(s.begin() + 2, s.end() - 2);  // 结果："1289"
}
```



#### 字符串反转

使用 `reverse()` 反转字符串。

```cpp
// 字符串反转
void reverseExample() {
    string s = "12345";
    reverse(s.begin(), s.end());  // 结果："54321"
}
```



#### 子串截取

使用 `substr()` 截取字符串的部分。

```cpp
// 子串截取
void substringExample() {
    string s = "12345";
    string sub = s.substr(3, 1);  // 结果："4"
    // 提取从索引 1 到字符串末尾的子字符串
    string sub = s.substr(1);
}
```

s.substr(pos) 返回从位置 pos开始直到字符串末尾的子字符串。

s.substr(pos, len) 返回从位置 pos 开始的子字符串，长度为 len



#### 字符串查找

使用 `find()` 查找子串或字符。

```cpp
// 字符串查找
void findExample() {
    string s = "dog bird chicken bird cat";
    size_t pos = s.find("chicken");  // 结果：9
    pos = s.find('i', 6);  // 结果：11
    pos = s.rfind("bird");  // 结果：16
    pos = s.rfind('i');  // 结果：18
}
    int pos = s.find(target, 0); // 从索引 0 开始查找字符 'z'

    if (pos == std::string::npos) {
        std::cout << "未找到字符 " << target << std::endl;
    } else {
        std::cout << "字符 " << target << " 在索引 " << pos << " 处" << std::endl;
    }
```



#### max_element 和 min_element

使用 `std::max_element` 和 `std::min_element` 查找最大值和最小值。

```cpp
// 查找最大最小值
int position = max_element(a, a + n) - a;
int data = *max_element(a, a + n);

int position = max_element(v.begin(), v.end()) - v.begin();
int it = *max_element(v.begin(), v.end());
```



#### stable_sort 保序排序

`stable_sort` 保证排序时相等元素的相对顺序不变。



#### map 和 unordered_map 

`map` 根据键值自动排序，`unordered_map` 无序。

用 `find()` 查找键值对，`erase()` 删除元素。

```cpp
// 使用 map 查找和删除
map<int, int> mp;
mp[1] = 2;
mp[2] = 3;
auto it = mp.find(2);
cout << it->second;  // 输出 3
mp.erase(2);  // 删除键 2 的元素
```



#### 字符串与字符的转换

使用 `c_str()` 将 `string` 转换为 C 风格字符串。

```cpp
const char* c;
string s = "1234";
c = s.c_str();
cout << c;  // 输出：1234
```



#### 结构体与排序

定义结构体并使用重载运算符或自定义排序函数对结构体数组进行排序。

```cpp
struct Student {
    string name;
    int grade;
    float high;
    bool operator < (const Student &tmp) const {
        return grade < tmp.grade;  // 按成绩从小到大排序
    }
};

struct Student stu[100];
sort(stu, stu + 100);
```



#### 自定义排序函数

使用 `cmp1` 自定义排序规则，按成绩排序，如果成绩相同则按身高排序。

```cpp
bool cmp1(Student a, Student b) {
    if (a.grade != b.grade)
        return a.grade < b.grade;
    else
        return a.high < b.high;
}

sort(stu, stu + 100, cmp1);  // 使用自定义排序
```



#### atoi 与 atof 

`atoi` 将字符串转换为整数，`atof` 将字符串转换为浮点数。

```cpp
const char* c;
string s;
cin >> s;
c = s.c_str();
int n = atoi(c);  // 将字符串转换为整数
cout << n << endl;  // 输出 -100
```



#### find 与 erase 

`find` 查找子串，`erase` 删除字符串的部分内容。

```cpp
// 查找字符串
string s = "ifisfs";
if (s.find("is") != string::npos)
    cout << s.find("is");  // 输出 2
```

------



#### 容器操作

- #### Set

  **set** 是一个自动排序的容器，不允许重复元素。

  访问元素时使用迭代器。

  `insert()` 用于插入元素，返回一个 pair，其中第二个元素表示插入是否成功。

```cpp
set<int> s;
s.insert(1);  // 插入元素 1
s.insert(2);  // 插入元素 2
s.insert(1);  // 不插入重复元素

for (auto it = s.begin(); it != s.end(); ++it) {
    cout << *it << " ";  // 输出：1 2
}
for(auto i : s) {
    cout << i << " ";
}
```

`find()` 用于查找元素，返回一个迭代器。

`erase()` 用于删除元素。

```cpp
auto it = s.find(2);  // 查找元素 2
if (it != s.end()) {
    s.erase(it);  // 删除元素 2
}
```



- #### Map

  **map** 是一个按键自动排序的容器，键值对存储。

  `find()` 返回指向元素的迭代器，若找不到返回 `map::end()`。

  `operator[]` 用于访问和插入元素。

```cpp
map<int, string> m;
m[1] = "one";  // 插入键值对
m[2] = "two";  // 插入键值对
cout << m[1] << endl;  // 输出 "one"

auto it = m.find(2);
if (it != m.end()) {
    cout << it->second << endl;  // 输出 "two"
}
for (auto &i : m) {
    cout << i.first << " " << i.second << endl;
}
for (auto [a, b] : m) {
	cout << a << " " << b << endl;
}
```



- #### Vector

  **vector** 是一个动态数组，允许随机访问。

  可以通过 `push_back()` 插入元素，通过 `size()` 获取元素个数。

  支持索引访问元素。


```cpp
vector<int> v;
v.push_back(1);  // 插入元素 1
v.push_back(2);  // 插入元素 2

for (int i = 0; i < v.size(); ++i) {
    cout << v[i] << " ";  // 输出：1 2
}
```



- #### Queue

  **queue** 是一个先进先出（FIFO）的容器，常用于任务排队。

  使用 `push()` 插入元素，`front()` 访问队头元素，`pop()` 移除队头元素。


```cpp
queue<int> q;
q.push(1);  // 插入元素 1
q.push(2);  // 插入元素 2
cout << q.front() << endl;  // 输出：1
q.pop();  // 移除队头元素

cout << q.front() << endl;  // 输出：2
```



- #### Priority Queue

  **priority_queue** 是一个优先队列，元素根据优先级自动排序（默认最大堆）。

  `push()` 插入元素，`top()` 返回堆顶元素，`pop()` 移除堆顶元素。


```cpp
priority_queue<int> pq;
pq.push(1);  // 插入元素 1
pq.push(3);  // 插入元素 3
pq.push(2);  // 插入元素 2

cout << pq.top() << endl;  // 输出：3 (最大元素)
pq.pop();  // 移除堆顶元素

cout << pq.top() << endl;  // 输出：2
```

------



#### 字符串格式输出

- #### printf 格式化输出

  支持多种格式说明符： 

  %d：输出整数（int）。

  `%f`：输出浮点数（`float` 或 `double`）。

  `%s`：输出字符串（`char*` 或 `const char*`）。

  `%c`：输出字符（`char`）。

  `%x`：输出十六进制（`int`）。

  `%o`：输出八进制（`int`）。

  `%.2f`：输出浮点数，保留 2 位小数。

```cpp
int a = 10;
float pi = 3.14159;
const char* str = "Hello, World!";

printf("Integer: %d\n", a);  // 输出：Integer: 10
printf("Float: %.2f\n", pi);  // 输出：Float: 3.14
printf("String: %s\n", str);  // 输出：String: Hello, World!
```



- #### cout 格式化输出

  **setprecision()**：设置浮点数输出的精度。

  **fixed 和 scientific**：控制浮点数输出的格式（定点表示或科学计数法）。


```cpp
#include <iostream>
#include <iomanip>  // 包含 setprecision 等格式控制函数

double pi = 3.14159265358979;

cout << fixed << setprecision(2) << pi << endl;  // 输出：3.14
cout << scientific << setprecision(2) << pi << endl;  // 输出：3.14e+00
```

​	setw()：设置输出的字段宽度（对齐）。

​	left, right 和 internal：控制输出的对齐方式。

```cpp
cout << setw(10) << left << "Hello" << endl;  // 输出：Hello     
cout << setw(10) << right << "Hello" << endl;  // 输出：     Hello
cout << setw(10) << internal << 123 << endl;  // 输出：    123
```



- #### cin 输入格式

  **cin >> variable**：标准输入，用于读取数据。

  **getline()**：读取整行数据，包括空格。


```cpp
int num;
string str;

cin >> num;  // 读取整数
cout << num << endl;  // 输出整数

cin.ignore();  // 清除输入缓冲区，防止 getline() 读取到残留的换行符
getline(cin, str);  // 读取一行字符串
cout << str << endl;  // 输出字符串
```

**跳过空格**：`cin` 会自动跳过空格、制表符和换行符，直到遇到有效字符。

```cpp
int x, y;
cin >> x >> y;  // 跳过空格并读取整数 x 和 y
```

------



- #### 其他常见输出

  **setfill() 与 setw()**：填充空格

```cpp
int num = 42;
cout << setw(6) << setfill('0') << num << endl;  // 输出：000042
```

​	**boolalpha 与 noboolalpha**：输出布尔值

```cpp
bool flag = true;
cout << boolalpha << flag << endl;  // 输出：true
cout << noboolalpha << flag << endl;  // 输出：1
```

​	**showbase 与 noshowbase**：显示或不显示数字基数（如 `0x` 或 `0`）

```cpp
int num = 255;
cout << showbase << hex << num << endl;  // 输出：0xff
cout << noshowbase << hex << num << endl;  // 输出：ff
```

​	**uppercase 与 nouppercase**：控制十六进制字母的大小写

```cpp
cout << uppercase << hex << 255 << endl;  // 输出：FF
cout << nouppercase << hex << 255 << endl;  // 输出：ff
```



#### priority_queue

- ##### **priority_queue 基本用法**


```cpp
#include <iostream>
#include <queue>
using namespace std;

int main() {
    priority_queue<int> pq;  // 默认最大堆
    pq.push(10);  // 插入元素 10
    pq.push(30);  // 插入元素 30
    pq.push(20);  // 插入元素 20

    // 打印并移除堆顶元素
    while (!pq.empty()) {
        cout << pq.top() << " ";  // 打印堆顶元素
        pq.pop();  // 移除堆顶元素
    }

    // 输出：30 20 10
    return 0;
}
```

------



- ##### **最小堆（greater<type>）**


```cpp
#include <iostream>
#include <queue>
#include <functional>  // greater
using namespace std;

int main() {
    priority_queue<int, vector<int>, greater<int>> pq;  // 最小堆

    pq.push(10);
    pq.push(30);
    pq.push(20);

    // 打印并移除堆顶元素
    while (!pq.empty()) {
        cout << pq.top() << " ";  // 打印堆顶元素
        pq.pop();  // 移除堆顶元素
    }

    // 输出：10 20 30
    return 0;
}
```

------



- ##### 使用自定义结构体的优先队列

假设我们有一个 `Person` 结构体，包含 `name` 和 `age`，并且我们希望按照年龄（`age`）的大小来进行优先级排序。下面是如何实现自定义结构体优先队列的步骤。

```cpp
#include <iostream>
#include <queue>
#include <vector>
#include <string>
using namespace std;

// 自定义结构体
struct Person {
    string name;
    int age;

    // 构造函数
    Person(string n, int a) : name(n), age(a) {}
};

// 自定义比较器：按年龄升序排序（最小堆）
struct CompareAge {
    bool operator()(const Person& p1, const Person& p2) {
        return p1.age > p2.age;  // 如果 p1.age > p2.age，p1 的优先级低
    }
};

int main() {
    // 创建优先队列，使用自定义比较器
    priority_queue<Person, vector<Person>, CompareAge> pq;

    // 插入元素
    pq.push(Person("Alice", 25));
    pq.push(Person("Bob", 30));
    pq.push(Person("Charlie", 20));

    // 打印并移除堆顶元素
    while (!pq.empty()) {
        Person top = pq.top();  // 获取堆顶元素
        cout << top.name << " (" << top.age << ")" << endl;
        pq.pop();  // 移除堆顶元素
    }

    // 输出：
    // Charlie (20)
    // Alice (25)
    // Bob (30)

    return 0;
}
```



- ##### 使用 `std::pair` 的 `first` 元素的优先队列


```cpp
int main() {
    // 定义一个优先队列，使用 std::less 来构造最大堆
    auto cmp = [](const pair<int, int>& a, const pair<int, int>& b) {
        return a.first < b.first; // 根据 first 元素排序
    };

    std::priority_queue<pair<int, int>, vector<pair<int, int>>, decltype(cmp)> pq(cmp);

    // 插入元素
    pq.push({1, 10});
    pq.push({3, 30});
    pq.push({2, 20});

    // 访问并删除元素
    while (!pq.empty()) {
        pair<int, int> top = pq.top();
        cout << "(" << top.first << ", " << top.second << ")" << endl;
        pq.pop();
    }

    return 0;
}
```



#### **std::lower_bound**

在 **有序序列** 中查找 **第一个大于或等于（≥）目标值** 的元素位置。

```cpp
#include <iostream>
#include <vector>
#include <algorithm>

int main() {
    std::vector<int> v = {1, 3, 5, 7, 9};

    auto it = lower_bound(v.begin(), v.end(), 5);
    std::cout << "lower_bound(5) 位置: " << (it - v.begin()) << std::endl; // 输出 2

    it = lower_bound(v.begin(), v.end(), 6);
    std::cout << "lower_bound(6) 位置: " << (it - v.begin()) << std::endl; // 输出 3

    return 0;
}
```

------



#### **std::upper_bound**

在 **有序序列** 中查找 **第一个大于（>）目标值** 的元素位置。

```cpp
#include <iostream>
#include <vector>
#include <algorithm>

int main() {
    std::vector<int> v = {1, 3, 5, 7, 9};

    auto it = std::upper_bound(v.begin(), v.end(), 5);
    std::cout << "upper_bound(5) 位置: " << (it - v.begin()) << std::endl; // 输出 3

    it = std::upper_bound(v.begin(), v.end(), 6);
    std::cout << "upper_bound(6) 位置: " << (it - v.begin()) << std::endl; // 输出 3

    return 0;
}
```



# 算法模版

## 一、基础算法

### 快速排序

```c++
void quick_sort(int q[], int l, int r) {
    //递归的终止情况
    if(l >= r) return;
    
     //选取分界线。这里选数组中间那个数
	int i = l - 1, j = r + 1,  x = q[l + r >> 1];
    //划分成左右两个部分
    while (i < j) {
        do i++; while(q[i] < x);
        do j--; while(q[j] > x);
        if(i < j) swap(q[i], q[j]);
    }
    //对左右部分排序
    quick_sort(q, l, j); quick_sort(q, j + 1, r);
}
```

### 归并排序

```c++
void merge_sort(int q[], int l, int r) {
    if (l >= r) return;
    int mid = l + r >> 1;
    merge_sort(q, l, mid);
    merge_sort(q, mid + 1, r);
    
    int k = 0; i = l; j = mid + 1;
    while(i <= mid && j <= r) {
        if(q[i] <= q[j]) tmp[k++] = q[i++];
        else tmp[k++] = q[j++];
    }
    while (i <= mid) tmp[k++] = q[i++];
    while (j <= r) tmp[k++] = q[j++];
    for (i = l, j = 0; i <= r; i++, j++) q[i] = tmp[j];
}
```

### 高精度加法

```c++
vector<int> add(vector<int> &a, vector<int> &b) {
    vector<int> c;
    int t = 0;
    for(int i = 0; i < a.size() || i < b.size(); i++) {
        if(i < a.size()) t += a[i];
        if(i < b.size()) t += b[i];
        c.push_bach(t % 10);
        t /= 10;
    }
    if(t) c.push_back(1);
    return c;
}
```

### 高精度减法

```c++
// C = A - B, 满足A >= B, A >= 0, B >= 0
vector<int> sub(vector<int> &a, vector<int> &b) {
    vector<int> c;
    for(int i = 0, t = 0; i < a.size(); i++) {
        t = a[i] - t;
        if(i < b.size()) t -= b[i];
        c.push_back((t + 10) % 10);
        if (t < 0) t = 1;
        else t = 0;
    }
    while (c.size() > 1 && c.back() == 0) c.pop_back();
    return c;
}
```

### 高精度比大小

```c++
bool cmp(vector<int> &a, vector<int> &b) {
    if(a.size() != b.size()) {
        return a.size() > b.size();
    } 
    for(int i = a.size() - 1; i >= 0; i--) {
        if(a[i] != b[i]){
            return a[i] > b[i];
        }
    }
    return true;
}
```

### 高精度乘低精度

```c++
vector<int> mul(vector<int> &a, int b) {
	vector<int> c;
    int t = 0;
    for(int i = 0; i < a.size() || t; i++) {
        if(i < a.size()) t += a[i] * b;
        c.puch_back(t%10);
        t /= 10;
    }
    while(c.size() > 1 && c.back() == 0) c.pop_back();
    return c;
}
```

### 高精度乘高精度

```c++
vector<int> mul(vector<int> &a, vector<int> &b) {
	vector<int> c(a.size() + b.size());
    for(int i = 0; i < a.size(); i++) {
		for(int j = 0; j < b.size(); j++) {
			c[i + j] += a[i] * b[j];
        }
    }
    for(int i = 0, t = 0; i < c.size(); i++) {
		t += c[i];
        c[i] = t % 10;
        t /= 10;
    }
    while(c.size() > 1 && c.back() == 0) c.pop_back();
    return c;
}
```

### 高精度除低精度

```c++
vector<int> div(vector<int> &a, int b, int &r) {
    vector<int> c;
    r = 0;
    for(int i = a.size() - 1; i >= 0; i++) {
        r = r*10 + a[i];
        c.push_back(r/b);
        r %= b;
    }
    reverse(c.begin(), c.end());
    while(c.size() > 1 && c.back() == 0) .
}
```

### 高精度除高精度

```c++
vector<int> div(vector<int> &a, vector<int> &b, vector<int> &r) {
    vector<int> c;
    if(!cmp(a, b)) {
		c.push_back(0);
        r.assign(a.begin(), a.end());
        reture c;
    }
    int j = b.size();
    r.assign(a.end() - j, a.end());
    while(j <= a.size()) {
        int k = 0;
        while(cmp(r, b)){
            r = sub(r, b);
            k++;
        }
        c.push_back(k);
        if(j < a.size()) {
            r.insert(r.begin(), a[a.size() - j - 1]);
        }
        if(r.size() > 1 && r.back() == 0) {
            r.pop_back();
        }
        j++;
    }
    reverse(c.begin(), c.end());
    while(c.size() > 1 && c.back() ==  0) {
        c.pop_back();
    }
    return c;
}
```

### 一维前缀和

前缀和可以用于快速计算一个序列的区间和，也有很多问题里不是直接用前缀和，但是借用了前缀
和的思想。

```
预处理:s[i]=a[i]+a[i-1]
求区间[l,r]:sum=s[r]-s[l-1]
"前缀和数组"和"原数组"可以合二为一
```

```c++
const int N=100010;
 int a[N];
 int main(){
    int n,m;
    scanf("%d",&n);
    for(int i=1;i<=n;i++)scanf("%d",&a[i]);
    for(int i=1;i<=n;i++)a[i]=a[i-1]+a[i];
    scanf("%d",&m);
    while(m--){
        int l,r;
        scanf("%d%d",&l,&r);
        printf("%d\n",a[r]-a[l-1]);
    }
    return 0;
 }
```

### 二维前缀和

```c++
int s[1010][1010];
int n,m,q;
int main(){
    scanf("%d%d%d",&n,&m,&q);
    for(int i=1;i<=n;i++)
        for(int j=1;j<=m;j++)
            scanf("%d",&s[i][j]);
    for(int i=1;i<=n;i++)
        for(int j=1;j<=m;j++)
            s[i][j]+=s[i-1][j]+s[i][j-1]-s[i-1][j-1];
    while(q--){
        int x1,y1,x2,y2;
        scanf("%d%d%d%d",&x1,&y1,&x2,&y2);
        printf("%d\n",s[x2][y2]-s[x2][y1-1]-s[x1-1][y2]+s[x1-1][y1-1]);
    }
    return 0;
 }
```

### 一维差分

```c++
using namespace std;
int a[100010],s[100010];
int main(){
   int n,m;
   cin>>n>>m;
   for(int i=1;i<=n;i++)cin>>a[i];   
   for(int i=1;i<=n;i++)s[i]=a[i]-a[i-1];// 读入并计算差分数组
   while(m--){
       int l,r,c;
       cin>>l>>r>>c;
       s[l]+=c;
       s[r+1]-=c;// 在原数组中将区间[l, r]加上c
   }
   for(int i=1;i<=n;i++){
       s[i]+=s[i-1];
       cout<<s[i]<<' ';
   }// 给差分数组计算前缀和，就求出了原数组
   return 0;
}
```

### 二维差分

```c++
const int N = 1e3 + 10;
int a[N][N], b[N][N];
void insert(int x1, int y1, int x2, int y2, int c)
{
   b[x1][y1] += c;
   b[x2 + 1][y1] -= c;
   b[x1][y2 + 1] -= c;
   b[x2 + 1][y2 + 1] += c;
}
int main()
{
   int n, m, q;
   cin >> n >> m >> q;
   for (int i = 1; i <= n; i++)
       for (int j = 1; j <= m; j++)
           cin >> a[i][j];
   for (int i = 1; i <= n; i++)
   {
       for (int j = 1; j <= m; j++)
       {
           insert(i, j, i, j, a[i][j]);      //构建差分数组
       }
   }
   while (q--)
   {
       int x1, y1, x2, y2, c;
       cin >> x1 >> y1 >> x2 >> y2 >> c;
       insert(x1, y1, x2, y2, c);//加c
   }
   for (int i = 1; i <= n; i++)
   {
       for (int j = 1; j <= m; j++)
       {
           b[i][j] += b[i - 1][j] + b[i][j - 1] - b[i - 1][j - 1];  //二维前缀和
       }
   }
   for (int i = 1; i <= n; i++)
   {
       for (int j = 1; j <= m; j++)
       {
           printf("%d ", b[i][j]);
       }
       printf("\n");
   }
   return 0;
}
```

### 位运算

```
求n的第k位数字: n >> k & 1
返回n的最后一位1：lowbit(n) = n & -n
```

### 离散化

离散化的本质是建立了一段数列到自然数之间的映射关系（value -> index)，通过建立新索引，来缩
小目标区间，使得可以进行一系列连续数组可以进行的操作比如二分，前缀和等…

```c++
vector<int> alls; // 存储所有待离散化的值
sort(alls.begin(), alls.end()); // 将所有值排序
alls.erase(unique(alls.begin(), alls.end()), alls.end());   // 去掉重复元素
// 二分求出x对应的离散化的值
int find(int x)  // 找到第一个大于等于x的位置
{
    int l = 0, r = alls.size() - 1, ans = alls.size(); // ans 设为 alls.size() 以防 x 比所有元素都大
    while (l <= r) { 
        int mid = l + ((r - l) >> 1); 
        if (alls[mid] >= x) { 
            ans = mid; 
            r = mid - 1; 
        } else { 
            l = mid + 1; 
        } 
    } 
    return ans;
}

```

```c++
typedef pair<int, int> PII;
const int N = 300010;
int n, m;
int a[N], s[N];
vector<int> alls;       // 存入下标容器
vector<PII> add, query; // add增加容器，存入对应下标和增加的值的大小
// query存入需要计算下标区间和的容器
int find(int x)  // 找到第一个大于等于x的位置
{
    int l = 0, r = alls.size() - 1, ans = alls.size(); 
    while (l <= r) { 
        int mid = l + ((r - l) >> 1); 
        if (alls[mid] >= x) { 
            ans = mid; 
            r = mid - 1; 
        } else { 
            l = mid + 1; 
        } 
    } 
    return ans;
}

int main()
{
    cin >> n >> m;
    for (int i = 0; i < n; i++)
    {
        int x, c;
        cin >> x >> c;
        add.push_back({x, c}); // 存入下标即对应的数值c
        alls.push_back(x);     // 存入数组下标x=add.first
    }
    for (int i = 0; i < m; i++)
    {
        int l, r;
        cin >> l >> r;
        query.push_back({l, r}); // 存入要求的区间
        alls.push_back(l);       // 存入区间左右下标
        alls.push_back(r);
    }
    // 区间去重
    sort(alls.begin(), alls.end());
    alls.erase(unique(alls.begin(), alls.end()), alls.end());
    // 处理插入
    for (auto item : add)
    {
        int x = find(item.first); // 将add容器的add.secend值存入数组a[]当中，
        a[x] += item.second;      // 在去重之后的下标集合alls内寻找对应的下标并添加数值
    }
    // 预处理前缀和
    for (int i = 1; i <= alls.size(); i++)
        s[i] = s[i - 1] + a[i];
    // 处理询问
    for (auto item : query)
    {
        int l = find(item.first), r = find(item.second); 
        // 在下标容器中查找对应的左右两端[l ~r] 下标，然后通过下标得到前缀和相减再得到区间a[l ~r] 的和
        cout<< s[r] - s[l - 1] << endl;
    }
    return 0;
}
```

### 区间合并

```c++
void merge(vector<PII> &segs)
{
    vector<PII> res;
    sort(segs.begin(), segs.end());
    int st = -2e9, ed = -2e9;
    for (auto seg : segs)
        if (ed < seg.first)
        {
            if (st != -2e9)
                res.push_back({st, ed});
            st = seg.first, ed = seg.second;
        }
        else
            ed = max(ed, seg.second);
    if (st != -2e9)
        res.push_back({st, ed});
    segs = res;
}
```

## 二、数据结构

### 单链表

```C++
const int N = 100010;
int head, e[N], ne[N], idx;
// 初始化
void init()
{
    head = -1;
    idx = 0;
}
// 在链表头部添加节点
void add_to_head(int x)
{
    e[idx] = x, ne[idx] = head, head = idx++;
}
// 在位置k添加节点x
void add(int k, int x)
{
    e[idx] = x, ne[idx] = ne[k], ne[k] = idx++;
}
// 删除位置k的节点
void remove(int k)
{
    ne[k] = ne[ne[k]];
}
```

```c++
int main()
{
    int m;
    init();
    cin >> m;
    while (m--)
    {
        int k, x;
        char op;
        cin >> op;
        if (op == 'H')
        {
            cin >> x;
            add_to_head(x);
        }
        else if (op == 'D')
        {
            cin >> k;
            if (!k)
                head = ne[head];
            remove(k - 1);
        }
        else
        {
            cin >> k >> x;
            add(k - 1, x);
        }
    }
    for (int i = head; i != -1; i = ne[i])
        cout << e[i] << ' ';
    cout << endl;
    return 0;
}
```

### 双链表

```c++
const int N=100010;
int e[N],l[N],r[N],idx;
 //初始化
void init(){
    l[1]=0;
    r[0]=1;
    idx=2;
 }
 //在节点a的右边插入一个数x
 void insert(int a,int x){
    e[idx]=x;
    l[idx]=a,r[idx]=r[a];
    l[r[a]]=idx,r[a]=idx++;
 }
 //删除节点a
 void remove(int a){
    l[r[a]]=l[a];
    r[l[a]]=r[a];
 }
```

```c++
int main()
{
    int m;
    cin >> m;
    init();
    while (m--)
    {
        string op;
        cin >> op;
        int k, x;
        if (op == "L")
        { // 在最左端插入数x
            cin >> x;
            insert(0, x);
        }
        else if (op == "R")
        { // 在最右端插入数x
            cin >> x;
            insert(l[1], x);
        }
        else if (op == "D")
        { // 删除第k个插入的数
            cin >> k;
            remove(k + 1);
        }
        else if (op == "IL")
        { // 在第k个位置的左侧插入一个数
            cin >> k >> x;
            insert(l[k + 1], x);
        }
        else if (op == "LR")
        { // 在第k个位置的右侧插入一个数
            cin >> k >> x;
            insert(k + 1, x);
        }
    }
    for (int i = r[0]; i != 1; i = r[i])
        printf("%d ", e[i]);
    cout << endl;
    return 0;
}
```

### 栈

```c++
const int N = 100010;
int stk[N], tt;
int main()
{
    int m;
    cin >> m;
    while (m--)
    {
        string op;
        int x;
        cin >> op;
        if (op == "push")
        {
            cin >> x;
            stk[tt++] = x;
        }
        else if (op == "pop")
        {
            tt--;
        }
        else if (op == "query")
        {
            cout << stk[tt - 1] << endl;
        }
        else
        {
            if (!tt)
                cout << "YES" << endl;
            else
                cout << "NO" << endl;
        }
    }
    return 0;
}
```

### 队列

```c++
const int MAXN = 100;
int q[MAXN];
int h = 0, t = 0;

void push(int x) {
    if ((t + 1) % MAXN == h) {  // 判断队列是否已满
        cout << "Queue is full!\n";
        return;
    }
    q[t] = x;
    t = (t + 1) % MAXN;  // 更新尾指针
}

int pop() {
    if (t == h) {  // 判断队列是否为空
        cout << "Queue is empty!\n";
        return -1;  // 返回错误标志
    }
    int res = q[h];
    h = (h + 1) % MAXN;  // 更新头指针
    return res;
}

bool isempty() {
    return h == t;
}
```

### 单调栈

单调栈最经典的用法是解决如下问题：

每个位置都求：

0）当前位置的 左侧比当前位置的数字小，且距离最近的位置 在哪

1）当前位置的 右侧比当前位置的数字小，且距离最近的位置 在哪

或者

每个位置都求：

0）当前位置的 左侧比当前位置的数字大，且距离最近的位置 在哪

1）当前位置的 右侧比当前位置的数字大，且距离最近的位置 在哪

```c++
const int MAXN = 1000001;
vector<int> arr(MAXN);
vector<pair<int, int>> ans(MAXN);
int n;

void compute() {
    stack<int> s;
    // 遍历阶段
    for (int i = 0; i < n; i++) {
        while (!s.empty() && arr[s.top()] >= arr[i]) {
            int cur = s.top();
            s.pop();
            ans[cur].first = s.empty() ? -1 : s.top();
            ans[cur].second = i;
        }
        s.push(i);
    }
    // 清算阶段
    while (!s.empty()) {
        int cur = s.top();
        s.pop();
        ans[cur].first = s.empty() ? -1 : s.top();
        ans[cur].second = -1;
    }
    // 修正阶段
    for (int i = n - 2; i >= 0; i--) {
        if (ans[i].second != -1 && arr[ans[i].second] == arr[i]) {
            ans[i].second = ans[ans[i].second].second;
        }
    }
}
```

### 单调队列

[滑动窗口最大值](https://leetcode.cn/problems/sliding-window-maximum/)

给你一个整数数组 nums，有一个大小为 k 的滑动窗口从数组的最左侧移动到数组的最右侧

你只可以看到在滑动窗口内的 k 个数字。滑动窗口每次只向右移动一位。

返回 滑动窗口中的最大值 。

```c++
#include <iostream>
#include <vector>
#include <deque>
using namespace std;

class SlidingWindowMaximum {
public:
    static vector<int> maxSlidingWindow(vector<int>& nums, int k) {
        deque<int> dq; // 存下标，用于维护单调递减的队列
        vector<int> res;

        for (int i = 0; i < nums.size(); ++i) {
            // 移除队头已经滑出窗口范围的元素
            if (!dq.empty() && dq.front() <= i - k) {
                dq.pop_front();
            }
            // 保证队列单调递减（从大到小）
            while (!dq.empty() && nums[dq.back()] <= nums[i]) {
                dq.pop_back();
            }
            dq.push_back(i); // 当前元素加入队尾

            // 当窗口大小满足 k 时，记录最大值
            if (i >= k - 1) {
                res.push_back(nums[dq.front()]);
            }
        }

        return res;
    }
};

// 示例测试
int main() {
    vector<int> nums = {1, 3, -1, -3, 5, 3, 6, 7};
    int k = 3;
    vector<int> result = SlidingWindowMaximum::maxSlidingWindow(nums, k);
    for (int num : result) {
        cout << num << " ";
    }
    cout << endl;
    return 0;
}
```

### KMP字符串匹配

```c++
void get_nextval(const string &t, int nextval[]) {
    int i = 0, j = -1;
    nextval[0] = -1; 
    while (i < t.size() - 1) {
        if (j == -1 || t[i] == t[j]) {
            i++; j++;
            if (t[i] != t[j]) {
                nextval[i] = j;
            } else {
                nextval[i] = nextval[j];  
            }
        } else {
            j = nextval[j];  
        }
    }
}
int kmp(const string &s, const string &t) {
    int nextval[t.size()];
    get_nextval(t, nextval);
    int i = 0, j = 0;

    while (i < s.size()) {
        if (j == -1 || s[i] == t[j]) {
            i++; j++;
        } else {
            j = nextval[j];  
        }

        if (j == t.size()) {  
            return i - j;
        }
    }
    return -1;  
}
```

### Trie树

```c++
const int MAXN = 150001; // 最大节点数，可以根据数据量调整
int tree[MAXN][26];      // 用于存储前缀树的结构，每个节点有26个子节点
int end[MAXN];           // 用于记录以某个节点为结尾的单词数量
int pass[MAXN];          // 用于记录经过某个节点的路径数量
int cnt;                 // 节点计数器

// 初始化前缀树
void build() {
    cnt = 1;
    fill(&tree[0][0], &tree[0][0] + MAXN * 26, 0);
    fill(end, end + MAXN, 0);
    fill(pass, pass + MAXN, 0);
}
// 初始化前缀树2
void build() {
    cnt = 1;
    fill(&tree[0][0], &tree[0][0] + MAXN * 26, 0);
    fill(end, end + MAXN, 0);
    fill(pass, pass + MAXN, 0);
}
// 插入单词
void insert(const string& word) {
    int cur = 1; // 从根节点开始
    pass[cur]++;
    for (char c : word) {
        int path = c - 'a'; // 计算路径（字符到'a'的偏移量）
        if (tree[cur][path] == 0) {
            tree[cur][path] = ++cnt; // 如果路径不存在，创建新节点
        }
        cur = tree[cur][path];
        pass[cur]++; // 更新路径计数
    }
    end[cur]++; // 更新单词结尾计数
}

// 查询单词是否存在
int search(const string& word) {
    int cur = 1; // 从根节点开始
    for (char c : word) {
        int path = c - 'a';
        if (tree[cur][path] == 0) {
            return 0; // 如果路径不存在，单词不存在
        }
        cur = tree[cur][path];
    }
    return end[cur]; // 返回单词结尾的计数
}

// 查询以某个前缀开头的单词数量
int prefixNumber(const string& prefix) {
    int cur = 1; // 从根节点开始
    for (char c : prefix) {
        int path = c - 'a';
        if (tree[cur][path] == 0) {
            return 0; // 如果路径不存在，前缀不存在
        }
        cur = tree[cur][path];
    }
    return pass[cur]; // 返回路径计数
}

// 删除单词
void deleteWord(const string& word) {
    if (search(word) > 0) { // 确保单词存在
        int cur = 1; // 从根节点开始
        for (char c : word) {
            int path = c - 'a';
            if (--pass[tree[cur][path]] == 0) { // 如果路径计数为0，删除路径
                tree[cur][path] = 0;
                return;
            }
            cur = tree[cur][path];
        }
        end[cur]--; // 减少单词结尾计数
    }
}
```

### 并查集

```c++
//(1)朴素并查集：
int pre[N];
for (int i = 1; i <= n; i ++ ) pre[i] = i;
int find(int x) {
    if (x != pre[x]) pre[x] = find[pre[x]];
    return pre[x];
}
void merge(int x, int y) {
    int fx = find(x);
    int fy = find(y);
    if(fx != fy) pre[fx] = fy;
}
//(2)维护size的并查集：
int pre[N], size[N];
for (int i = 1; i <= n; i ++ ) {
    pre[i] = i;
    size[i] = 1;
}
int find(int x) {
    if (x != pre[x]) pre[x] = find[pre[x]];
    return pre[x];
}
void merge(int x, int y) {
    int fx = find(x);
    int fy = find(y);
    if(fx != fy) {
        pre[fx] = fy;
        size[fy] += size[fx];
    }
}
```

### 堆

```c++
const int MAXN = 100001;
vector<int> arr(MAXN);
int n;

void swap(int &a, int &b) {
    int temp = a;
    a = b;
    b = temp;
}

void heapInsert(int i) {
    while (i > 0 && arr[i] > arr[(i - 1) / 2]) {
        swap(arr[i], arr[(i - 1) / 2]);
        i = (i - 1) / 2;
    }
}

void heapify(int i, int size) {
    int l = i * 2 + 1;
    while (l < size) {
        int best = (l + 1 < size && arr[l + 1] > arr[l]) ? l + 1 : l;
        best = (arr[best] > arr[i]) ? best : i;
        if (best == i) break;
        swap(arr[best], arr[i]);
        i = best;
        l = i * 2 + 1;
    }
}

void heapSort() {
    for (int i = n / 2 - 1; i >= 0; i--) {
        heapify(i, n);
    }
    int size = n;
    while (size > 1) {
        swap(arr[0], arr[--size]);
        heapify(0, size);
    }
}
```

### 一般hash

```c++
//拉链法
int h[N], e[N], ne[N], idx;
fill(h, h + N, -1);
void insert(int x) {
    int k = (x % N + N) % N;
    e[idx] = x;
    ne[idx] = h[k];
    h[k] = idx++;
}
bool find(int x) {
    int k = (x % N + N) % N;
    for(int i = h[k]; i != -1; i = ne[i]) {
        if (e[i] == x) {
            return true;
        }
    }
    return false;
}
//开放寻址法
int h[N];
int find(int x) {
    int k = (x % N + N) % N;  
    int start = k;  
    while (h[k] != -1 && h[k] != x) {
        k++;  
        if (k == N) k = 0; 
        if (k == start) break;  
    }
    return k;  
}
```

### 字符串哈希

```c++
typedef unsigned long long ULL;
const int N = 100005; 
const ULL P = 131;    
ULL h[N], p[N]; 

void init_hash(const string &str) {
    int n = str.size();
    p[0] = 1;
    for (int i = 1; i <= n; i++) {
        h[i] = h[i - 1] * P + str[i - 1]; 
        p[i] = p[i - 1] * P;
    }
}

ULL get_hash(int l, int r) {
    return h[r] - h[l - 1] * p[r - l + 1];
}
```

### STL

| 中文名称           | 容器类型       | 头文件            | 创建语法                       | 添加操作（时间复杂度）                                      | 删除操作（时间复杂度）                                  | 访问操作（时间复杂度）                                       |
| ------------------ | -------------- | ----------------- | ------------------------------ | ----------------------------------------------------------- | ------------------------------------------------------- | ------------------------------------------------------------ |
| 变长数组           | vector         | `<vector>`        | `vector<int> v`                | `v.push_back(x)` <br> O(1)                                  | `v.pop_back()` <br> O(1)                                | `v[i]` <br> O(1)                                             |
| 双端队列           | deque          | `<deque>`         | `deque<int> d`                 | `d.push_front(x)` <br> O(1) <br> `d.push_back(x)` <br> O(1) | `d.pop_front()` <br> O(1) <br> `d.pop_back()` <br> O(1) | `d.front()` <br> O(1) <br> `d.back()` <br> O(1)              |
| 栈                 | stack          | `<stack>`         | `stack<int> s`                 | `s.push(x)` <br> O(1)                                       | `s.pop()` <br> O(1)                                     | `s.top()` <br> O(1)                                          |
| 队列               | queue          | `<queue>`         | `queue<int> q`                 | `q.push(x)` <br> O(1)                                       | `q.pop()` <br> O(1)                                     | `q.front()` <br> O(1)                                        |
| 优先队列（大根堆） | priority_queue | `<queue>`         | `priority_queue<int> q`        | `q.push(x)` <br> O(logn)                                    | `q.pop()` <br> O(logn)                                  | `q.top()` <br> O(1)                                          |
| 有序集合           | set            | `<set>`           | `set<int> s`                   | `s.insert(x)` <br> O(logn)                                  | `s.erase(it)` <br> O(logn)                              | `s.count(x)` <br> O(k+logn)                                  |
| 无序集合           | unordered_set  | `<unordered_set>` | `unordered_set<int> s`         | `s.insert(x)` <br> O(1)（平均）<br> O(n)（最坏）            | `s.erase(it)` <br> O(1)（平均）<br> O(n)（最坏）        | `s.count(x)` <br> O(1)（平均）<br> O(n)（最坏）              |
| 有序键值对映射     | map            | `<map>`           | `map<string, int> h`           | `h[str] = x` <br> O(logn)                                   | `h.erase(it)` <br> O(logn)                              | `h.count(str)` <br> O(logn) <br> `h[str]` <br> O(logn)       |
| 无序键值对映射     | unordered_map  | `<unordered_map>` | `unordered_map<string, int> h` | `h[str] = x` <br> O(1)（平均）<br> O(n)（最坏）             | `h.erase(it)` <br> O(1)（平均）<br> O(n)（最坏）        | `h.count(str)` <br> O(1)（平均）<br> O(n)（最坏） <br> `h[str]` <br> O(1)（平均）<br> O(n)（最坏） |

| 容器/工具          | 类型/描述                | 核心操作                                                     | 时间复杂度                 | 特殊特性/注意事项                                            |
| ------------------ | ------------------------ | ------------------------------------------------------------ | -------------------------- | ------------------------------------------------------------ |
| **vector**         | 变长数组（动态数组）     | `push_back()` `pop_back()` `[]` `size()` `empty()` `clear()` `begin()` `end()` | 尾部操作O(1), 随机访问O(1) | 支持倍增扩容；可比较（按字典序）                             |
| **pair**           | 二元组                   | `first` `second`                                             | -                          | 支持比较运算（先比较`first`，后`second`）                    |
| **string**         | 字符串                   | `substr(start, len)` `c_str()` `size()` `empty()` `clear()`  | 子串操作O(len)             | 支持类似vector的操作；`c_str()`返回C风格字符串               |
| **queue**          | 队列（FIFO）             | `push()` `pop()` `front()` `back()` `size()` `empty()`       | 插入/删除O(1)              | 底层默认用deque实现                                          |
| **priority_queue** | 优先队列（默认大根堆）   | `push()` `pop()` `top()` `size()` `empty()`                  | 插入/删除O(logn)           | 定义小根堆：`priority_queue<int, vector<int>, greater<int>> q` |
| **stack**          | 栈（LIFO）               | `push()` `pop()` `top()` `size()` `empty()`                  | 栈顶操作O(1)               | 底层默认用deque实现                                          |
| **deque**          | 双端队列                 | `push_front()` `push_back()` `pop_front()` `pop_back()` `[]` `begin()` `end()` | 两端操作O(1), 随机访问O(1) | 支持快速头尾操作，但内存非连续                               |
| **set/multiset**   | 有序集合（红黑树实现）   | `insert(x)` `erase(x/it)` `find(x)` `count(x)` `lower_bound(x)` `upper_bound(x)` | 插入/删除/查找O(logn)      | `multiset`允许重复元素；`count(x)`返回匹配数（O(k+logn)）    |
| **map/multimap**   | 有序键值对（红黑树实现） | `insert({k,v})` `erase(k/it)` `find(k)` `[]` `lower_bound(k)` `upper_bound(k)` | 插入/删除/查找O(logn)      | `multimap`允许多个相同键；`multimap`不支持`[]`操作符         |
| **unordered_**系列 | 哈希表实现（无序）       | `insert()` `erase()` `find()` `count()`                      | 平均O(1)，最坏O(n)         | 不支持`lower_bound()`和`++`/`--`迭代器操作                   |
| **bitset**         | 位操作容器               | `set(k,v)` `reset(k)` `flip(k)` `count()` `any()` `none()` `[]` | 位操作O(1)                 | 固定大小；支持位运算（& \| ^ ~）                             |



## 三、搜索与图论

### 深度优先遍历

```c++
int dfs(int u)
{
	st[u] = true;
    for (int i = h[u]; i != -1; i = ne[i])
    {
        int j = e[i];
        if (!st[j]) dfs(j);
    }
}
```

#### -应用：数字全排列

```c++
#include <iostream>
using namespace std;
int res[10],b[10],n;
void dfs(int k){
	if(k==n){//k==n则输出n个数字
        for(int i=0;i<n;i++)printf("%d ",res[i]);
        cout<<endl;
    }
    for(int i=1;i<=n;i++){
        if(!b[i]){//判断是否被用过
            res[k]=i;//当前k位存入位置
            b[i]=1;//表示被占用
            dfs(k+1);
            b[i]=0;//恢复现场
        }
    }
}
int main(){
	cin>>n;
    dfs(0);//从0开始枚举
    return 0;
}
```

#### -应用：树的重心

```c++
#include <cstdio>
#include <cstring>
#include <iostream>
#include <algorithm>
using namespace std;

const int N = 100010, M = N * 2;

int n, h[N], e[M], ne[M], idx;
int ans = N;
bool st[N];

void add(int a, int b) {
    e[idx] = b, ne[idx] = h[a], h[a] = idx++;
}

int dfs(int u) {
    st[u] = true;
    int size = 0, sum = 0;
    for (int i = h[u]; i != -1; i = ne[i]) {
        int j = e[i];
        if (st[j]) continue;
        int s = dfs(j);
        size = max(size, s);
        sum += s;
    }
    size = max(size, n - sum - 1);
    ans = min(ans, size);
    return sum + 1;
}

int main() {
    scanf("%d", &n);
    memset(h, -1, sizeof h);
    for (int i = 0; i < n - 1; i++) {
        int a, b;
        scanf("%d%d", &a, &b);
        add(a, b), add(b, a);
    }
	dfs(1);
    printf("%d\n", ans);
    return 0;
}
```

#### -应用:  n皇后问题

```c++
#include <iostream>
using namespace std;

const int N = 11;
char q[N][N]; 
bool dg[N * 2], udg[N * 2], col[N]; 

int n;

void dfs(int r) {
    if (r == n) {
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < n; j++)
                cout << q[i][j];
            cout << endl;
        }
        cout << endl;
        return;
    }
    for (int i = 0; i < n; i++) { 
        if (!col[i] && !dg[i + r] && !udg[n - i + r]) { 
            q[r][i] = 'Q';
            col[i] = dg[i + r] = udg[n - i + r] = true; 
            dfs(r + 1);
            col[i] = dg[i + r] = udg[n - i + r] = false; 
            q[r][i] = '.';
        }
    }
}

int main() {
    cin >> n;
    for (int i = 0; i < n; i++)
        for (int j = 0; j < n; j++)
            q[i][j] = '.';
    
    dfs(0);
    return 0;
}
```



### 宽度优先遍历

```c++
#include <queue>
using namespace std;
queue<int> q;
st[1] = true; 
q.push(1);
while (!q.empty()) {
    int t = q.front();
    q.pop();
    for (int i = h[t]; i != -1; i = ne[i]) {
        int j = e[i];
        if (!st[j]) {
            st[j] = true; 
            q.push(j);
        }
    }
}
```

#### -应用：走迷宫

```c++
typedef pair<int,int> PII;
const int N=110;
int g[N][N],f[N][N],n,m;
int bfs(int x,int y){
	queue<PII> que;
    que.push({x,y});
    int dx[4]={0,1,0,-1},dy[4]={1,0,-1,0};
    while(!que.empty()){
        PII t=que.front();
        que.pop();
        g[t.first][t.second]=1;
        for(int i=0;i<4;i++){
            int a=t.first+dx[i],b=t.second+dy[i];
            if(a>=0&&b>=0&&a<n&&b<m&&!g[a][b]){
                g[a][b]=1;
                f[a][b]=f[t.first][t.second]+1;
                que.push({a,b});
            }
        }
    }
    return f[n-1][m-1];
}
int main(){
    scanf("%d%d",&n,&m);
    for(int i=0;i<n;i++)
        for(int j=0;j<m;j++)
            scanf("%d",&g[i][j]);
    cout<<bfs(0,0)<<endl;
    return 0;
}
```

#### -应用：八数码

```c++
#include <iostream>
#include <queue>
#include <unordered_map>
#include <string>
using namespace std;
int bfs(string state) {
    queue<string> q;  // 队列用于 BFS
    unordered_map<string, int> d;  // 哈希表记录每个状态的步数
    int dx[4] = {-1, 0, 1, 0}, dy[4] = {0, 1, 0, -1};  // 4个方向：上、右、下、左
    string ed = "12345678x";  // 目标状态
    q.push(state);
    d[state] = 0;
    while (q.size()) {
        auto t = q.front();  // 获取队头元素
        q.pop();
        if (t == ed)  // 如果当前状态是目标状态，返回步数
            return d[t];
        int distance = d[t];  // 当前状态的步数
        int k = t.find('x');  // 查找空白格子 'x' 的位置
        int x = k / 3, y = k % 3;  // 计算 'x' 在 3x3 网格中的位置（行和列）
        for (int i = 0; i < 4; i++) {
            int a = x + dx[i], b = y + dy[i];  // 计算新位置
            if (a >= 0 && a < 3 && b >= 0 && b < 3) {  // 如果新位置有效
                swap(t[a * 3 + b], t[k]);  // 交换空白格和目标位置
                if (!d.count(t)) {  // 如果新的状态未被访问过
                    d[t] = distance + 1;  // 更新步数
                    q.push(t);  // 将新状态入队
                }
                swap(t[a * 3 + b], t[k]);  // 还原交换，继续尝试其他方向
            }
        }
    }
    return -1;  // 如果没有找到解，返回 -1
}

int main() {
    char s[2];
    string state;
    // 输入 9 个数字构建初始状态
    for (int i = 0; i < 9; i++) {
        cin >> s;
        state += *s;
    }
    // 输出从初始状态到目标状态的最短步数
    cout << bfs(state) << endl;
    return 0;
}
```



### 拓扑排序

```c++
using namespace std;
const int N = 100010;
int e[N], ne[N], idx; //邻接表存储图
int h[N];//邻接表的每个头链表
int q[N], hh = 0, tt = -1; //队列保存入度为0的点，也就是能够输出的点
int n, m; //保存图的点数和边数
int d[N];//保存各个点的入度
void add(int a, int b) {
    e[idx] = b, ne[idx] = h[a], h[a] = idx++;
}
void topsort() {
    for (int i = 1; i <= n; i++) {//遍历一遍顶点的入度。
        if (!d[i])//如果入度为0，则可以入队列
            q[++tt] = i;
    }
    while (tt >= hh) { //循环处理队列中点的
        int a = q[hh++];
        for (int i = h[a]; i != -1; i = ne[i]) {
            int b = e[i]; //a 有一条边指向b
            d[b]--;//删除边后，b的入度减1
            if (!d[b])//如果b的入度减为 0,则 b 可以输出，入队列
                q[++tt] = b;
        }
    }
    if (tt == n - 1) {//如果队列中的点的个数与图中点的个数相同，则可以进行拓扑排序
        for (int i = 0; i < n; i++)//队列中保存了所有入度为0的点，依次输出
            printf("%d ", q[i]);
    } else//如果队列中的点的个数与图中点的个数不相同，则可以进行拓扑排序
        cout << -1;
}
int main() {
	cin >> n >> m; //保存点的个数和边的个数
    memset(h, -1, sizeof h); //初始化领接矩阵
    while (m--) { //依次读入边
        int a, b;
        cin >> a >> b;
        d[b]++;//顶点b的入度+1
        add(a, b); //添加到邻接矩阵
    }
    topsort();//进行拓扑排序
    return 0;
}
```

### Dijkstra算法

```c++
const int INF = 0x7fffffff;
int G[N][N], pre[N], dis[N];
bool vis[N];

fill(G[0], G[0] + N * N, INF); 
fill(dis, dis + N, INF);      
dis[s] = 0;                    

for (int i = 0; i < n; i++) {
    int min = INF, u = -1;
    for (int j = 0; j < n; j++) {
        if (!vis[j] && dis[j] < min) {
            u = j;
            min = dis[j];
        }
    }
    if (u == -1) break;        
    vis[u] = true;
    for (int v = 0; v < n; v++) {
        if (!vis[v] && G[u][v] != INF) {
            if (G[u][v] + dis[u] < dis[v]) {
                dis[v] = G[u][v] + dis[u];
                pre[v] = u;    
            }
        }
    }
}
```

#### -堆优化版

```c++
const int INF = 0x3f3f3f3f;
const int N = 100010;  
const int M = 200010;  
int h[N], e[M], ne[M], w[M], idx;
int dis[N], pre[N];
bool vis[N];
void add(int a, int b, int c) {
    e[idx] = b, w[idx] = c, ne[idx] = h[a], h[a] = idx++;
}
void dijkstra(int s) {
    memset(dis, 0x3f, sizeof dis);
    dis[s] = 0;
    priority_queue<pair<int, int>, vector<pair<int, int>>, greater<pair<int, int>>> pq;
    pq.push({0, s});  
	while (!pq.empty()) {
	    int u = pq.top().second;
	    pq.pop(); 
        if (vis[u]) continue;  
    	vis[u] = true;
    	for (int i = h[u]; i != -1; i = ne[i]) {
        	int v = e[i];
        	if (dis[u] + w[i] < dis[v]) { 
            	dis[v] = dis[u] + w[i];
            	pre[v] = u;
            	pq.push({dis[v], v});
       		}
    	}
	}
}
int main() {
    int n, m, s;
    cin >> n >> m >> s;
    memset(h, -1, sizeof h);  
    for (int i = 0; i < m; i++) {
        int a, b, c;
        cin >> a >> b >> c;
        add(a, b, c);
    }
    dijkstra(s);
    for (int i = 1; i <= n; i++) {
        if (dis[i] == INF) cout << "INF ";
        else cout << dis[i] << " ";
    }
    cout << endl;
    return 0;
}
```

### floyd算法

```c++
//初始化
for (int i = 1; i <= n; i ++ )
	for (int j = 1; j <= n; j ++ )
        if (i == j) d[i][j] = 0;
        else d[i][j] = INF;
 // 算法结束后，d[a][b]表示a到b的最短距离
void floyd()
 {
    for (int k = 1; k <= n; k ++ )//k为中转节点
        for (int i = 1; i <= n; i ++ )
            for (int j = 1; j <= n; j ++ )
                d[i][j] = min(d[i][j], d[i][k] + d[k][j]);
 }
```

### prim算法

```c++
int n;      
int g[N][N];        
int dist[N];        
bool st[N];     

int prim()
{
    memset(dist, 0x3f, sizeof dist);
    int res = 0;
    for (int i = 0; i < n; i ++ )
    {
        int t = -1;
        for (int j = 1; j <= n; j ++ )
            if (!st[j] && (t == -1 || dist[t] > dist[j]))
                t = j;
        if (i && dist[t] == INF) return INF;                         
        if (i) res += dist[t];
        st[t] = true;
        for (int j = 1; j <= n; j ++ ) dist[j] = min(dist[j], g[t][j]);
    }
    return res;
}
```

### Kruskal算法

```c++
int n, m;       // n是点数，m是边数
int p[N];       // 并查集的父节点数组
struct Edge     // 存储边
{
    int a, b, w;
    bool operator< (const Edge &W)const
    {
        return w < W.w;
    }
}edges[M];
int find(int x)     // 并查集核心操作
{
    if (p[x] != x) p[x] = find(p[x]);
    return p[x];
 }
int kruskal()
{
    sort(edges, edges + m);
    for (int i = 1; i <= n; i ++ ) p[i] = i;    // 初始化并查集
    int res = 0, cnt = 0;
    for (int i = 0; i < m; i ++ )
    {
        int a = edges[i].a, b = edges[i].b, w = edges[i].w;
        a = find(a), b = find(b);
        if (a != b)     // 如果两个连通块不连通，则将这两个连通块合并
        {
            p[a] = b;
            res += w;
            cnt ++ ;
        }
    }
    if (cnt < n - 1) return INF;
    return res;
}
```

### Bellman-Ford

```c++
int n, m;               // n表示点数，m表示边数
int dist[N], backup[N]; // dist[x]存储1到x的最短路距离
struct Edge             // 边，a表示出点，b表示入点，w表示边的权重
{
    int a, b, w;
} edges[M];
// 求1到n的最短路距离，如果无法从1走到n，则返回-1。
int bellman_ford()
{
    memset(dist, 0x3f, sizeof dist);
    dist[1] = 0;
    // 如果第n次迭代仍然会松弛三角不等式，就说明存在一条长度是n+1的最短路径，由抽屉原理，路径中至少存在两个相同的点，说明图中存在负权回路。
    for (int i = 0; i < n; i++)
    {
        memcpy(backup, dist, sizeof dist);
        for (int j = 0; j < m; j++)
        {
            int a = edges[j].a, b = edges[j].b, w = edges[j].w;
            if (dist[b] > backup[a] + w)
                dist[b] = backup[a] + w;
        }
    }
    if (dist[n] > 0x3f3f3f3f / 2)
        return -1;
    return dist[n];
}
```



### 染色法判别二分图

```c++
int n;                      // 图中的点数
int h[N], e[M], ne[M], idx; // 邻接表，h[i] 是点 i 的邻接链表头，e 存储目标点，ne 存储下一个边的索引
int color[N];               // color[i] 表示点 i 的颜色，初始为 -1，染色后为 0 或 1
bool dfs(int u, int c)
{
    color[u] = c;  // 给当前节点染色（0 或 1）
    for (int i = h[u]; i != -1; i = ne[i])  // 遍历所有相邻边
    {
        int j = e[i]; // j 是 u 的邻接点

        if (color[j] == -1)  // 如果 j 还没染色
        {
            if (!dfs(j, !c)) // 给 j 染上相反颜色，如果染色失败（矛盾），返回 false
                return false;
        }
        else if (color[j] == c) // 如果 j 和 u 同色，说明不是二分图
            return false;
    }
    return true;  // 当前分支染色成功
}
bool check()
{
    memset(color, -1, sizeof color);  // 所有点初始化为未染色
    bool flag = true;

    for (int i = 1; i <= n; i++)
        if (color[i] == -1) // 如果点 i 没染色
            if (!dfs(i, 0)) // 从 i 出发尝试染色
            {
                flag = false;  // 发现矛盾，非二分图
                break;
            }

    return flag;
}
```



### 匈牙利算法

```c++
// 遍历自己喜欢的女孩int n1, n2; // n1表示第一个集合中的点数，n2表示第二个集合中的点数 
int h[N], e[M], ne[M], idx; // 邻接表存储所有边，匈牙利算法中只会用到从第一个集合指向第二个集合的边，所以这里只用存一个方向的边
int match[N]; // 存储第二个集合中的每个点当前匹配的第一个集合中的点是哪个
bool st[N];   // 表示第二个集合中的每个点是否已经被遍历过
void add(int a, int b)
{
    e[idx] = b, ne[idx] = h[a], h[a] = idx++;
}
bool find(int x)
{
    // 遍历自己喜欢的女孩
    for (int i = h[x]; i != -1; i = ne[i])
    {
        int j = e[i];
        if (!st[j]) // 如果在这一轮模拟匹配中,这个女孩尚未被预定
        {
            st[j] = true; // 那x就预定这个女孩了
            // 如果女孩j没有男朋友，或者她原来的男朋友能够预定其它喜欢的女孩。配对成功
            if (match[j] == 0 || find(match[j]))
            {
                match[j] = x;
                return true;
            }
        }
    }
    // 自己中意的全部都被预定了。配对失败。
    return false;
}

// 求最大匹配数，依次枚举第一个集合中的每个点能否匹配第二个集合中的点
int res = 0;
for (int i = 1; i <= n1; i++)
{
    memset(st, false, sizeof st);
    if (find(i))
        res++;
}
```



## 四、数学知识

### 试除法判定质数

```c++
bool is_prime(int x)
{
    if (x < 2) return false;
    for (int i = 2; i <= x / i; i ++ )
        if (x % i == 0)
            return false;
    return true;
}
```



### 试除法分解质因数

```c++
void divide(int x)
{
    for (int i = 2; i <= x / i; i ++ )
        if (x % i == 0)//i 一定是质数
        {
            int s = 0;
            while (x % i == 0) x /= i, s ++ ;
            cout << i << ' ' << s << endl;
        }
    if (x > 1) cout << x << ' ' << 1 << endl;
    cout << endl;
}
```



### 埃氏筛法求质数

```c++
int primes[N], cnt;     // primes[]存储所有素数
bool st[N];         // st[x]存储x是否被筛掉
void get_primes(int n)
{
    for (int i = 2; i <= n; i ++ )
    {
        if (st[i]) continue;
        primes[cnt ++ ] = i;
        for (int j = i + i; j <= n; j += i)
            st[j] = true;
    }
}
```



### 线性筛法求质数

```c++
int primes[N], cnt;     // primes[]存储所有素数
bool st[N];         // st[x]存储x是否被筛掉
void get_primes(int n)
{
    for (int i = 2; i <= n; i ++ )
	{
        if (!st[i]) primes[cnt ++ ] = i;
        for (int j = 0; j < cnt && primes[j] <= n / i; j ++ )
        {
            st[primes[j] * i] = true;
            if (i % primes[j] == 0) break;
        }
    }
}
```



### 试除法求所有约数

```c++
vector<int> get_divisors(int x)
{
    vector<int> res;
    for (int i = 1; i <= x / i; i ++ )
        if (x % i == 0)
        {
            res.push_back(i);
            if (i != x / i) res.push_back(x / i);
        }
    sort(res.begin(), res.end());
    return res;
}
```



### 约数个数

```c++
using namespace std;
typedef long long LL;
const int N = 110, mod = 1e9 + 7;
int main()
{
    int n;
    cin >> n;
    unordered_map<int, int> primes;
    while (n -- )
    {
        int x;
        cin >> x;
        for (int i = 2; i <= x / i; i ++ )
            while (x % i == 0)
            {
                x /= i;
               primes[i] ++ ;
            }
        if (x > 1) primes[x] ++ ;
    }
    LL res = 1;
    for (auto p : primes) res = res * (p.second + 1) % mod;
    cout << res << endl;
    return 0;
}
```



### 容斥原理应用

给定一个整数 n 和 m 个不同的质数p1,p2,…,pm
请你求出 1∼n 中能被 p1,p2,…,pm中的至少一个数整除的整数有多少个。

输入格式
第一行包含整数 n 和 m。

第二行包含 m 个质数。

输出格式
输出一个整数，表示满足条件的整数的个数。

数据范围
1≤m≤16,
1≤n,pi≤109

输入样例：
10 2
2 3
输出样例：
7

```c++
#include <iostream>
using namespace std;
typedef long long LL;
const int N = 20;
int p[N], n, m;
int main() {
    cin >> n >> m;
    for (int i = 0; i < m; i++) 
        cin >> p[i];
    int res = 0;
    // 枚举从 1 到 2^m - 1 的每一个集合状态（至少选中一个集合）
    for (int i = 1; i < (1 << m); i++) {
        LL t = 1;  // 选中集合对应质数的乘积
        int s = 0;  // 选中的集合数量
        // 枚举当前状态的每一位
        for (int j = 0; j < m; j++) {
            if (i >> j & 1) {  // 选中一个集合
                // 乘积大于 n，则 n / t = 0，直接跳过这轮计算
                if (t * p[j] > n) {  
                    t = -1;
                    break;
                }
                s++;  // 选中的集合数量 +1
                t *= p[j];
            }
        }
        if (t == -1) continue;  
        // 使用容斥原理进行计数
        if (s & 1) res += n / t;  // 加 n / t
        else res -= n / t;        // 选中偶数个集合，减 n / t
    }
    cout << res << endl;
    return 0;
}
```


#### Dijkstra (L2-001)

```c++
const int INF = 0x7fffffff;
int G[N][N], pre[N], dis[N];
bool vis[N];

fill(G[0], G[0] + N * N, INF); // 初始化图为无穷大
fill(dis, dis + N, INF);       // 初始化距离为无穷大
dis[s] = 0;                    // 起点到自身距离为0

for (int i = 0; i < n; i++) {
    int min = INF, u = -1;
    for (int j = 0; j < n; j++) {
        if (!vis[j] && dis[j] < min) {
            u = j;
            min = dis[j];
        }
    }
    if (u == -1) break;        // 所有点都访问过，退出
    vis[u] = true;
    for (int v = 0; v < n; v++) {
        if (!vis[v] && G[u][v] != INF) {
            if (G[u][v] + dis[u] < dis[v]) {
                dis[v] = G[u][v] + dis[u];
                pre[v] = u;    // 记录前驱节点
            }
        }
    }
}
```

#### 打印最短路径

```c++
void printpath(int v) {
    if (v == s) {
        cout << v;
        return;
    }
    printpath(pre[v]);
    cout << " " << v;
}
```

------

#### 常用数据结构和语法

- `map`

  - 查找：`mp.find(key) == mp.end();`

- `vector`

  - 初始化：`vector<double> pa(n, 0.0);`
  - 清空：`vec.clear();`

- `set`

  - 插入：`st.insert();`

- `queue`

  - 获取队首：`q.front();`
  - 弹出：`q.pop();`

- 二分查找

  - 使用：`upper_bound(a, a + n, i);`

- `struct` 的定义

  ```c++
  struct node {
      node() {}
  };
  ```

- `memset`

  - 初始化：`memset(h, -1, sizeof h);`

- 输入输出和常用函数

  - 输出：`puts("Yes");`
  - 字符串操作：`string ss = b.substr(0, b.size() - 4);`

------

#### 并查集 (Union-Find)

```c++
int find(int x) {
    if (x != pre[x]) pre[x] = find(pre[x]);
    return pre[x];
}

void merge(int x, int y) {
    int fx = find(x);
    int fy = find(y);
    if (fx < fy) pre[fy] = fx;
    else pre[fx] = fy;
}

for (int i = 0; i < N; i++) pre[i] = i; // 初始化
```

------

#### 堆 (Heap)

```c++
int cnt = 0; // 堆的元素个数

void create(int x) {
    ans[++cnt] = x;
    int t = cnt;
    while (t > 1 && ans[t / 2] > ans[t]) {
        swap(ans[t / 2], ans[t]);
        t /= 2;
    }
}
```

------

#### 最长递增子序列 (L2-014)

```c++
#include <iostream>
#include <algorithm>
#include <cstring>
using namespace std;

const int N = 1e5 + 5;
int dp1[N], a[N], n;

int main() {
    cin >> n;
    for (int i = 1; i <= n; i++) cin >> a[i];
    int ans = 1;
    dp1[ans] = a[1];
    for (int i = 2; i <= n; i++) {
        if (a[i] > dp1[ans]) {
            dp1[++ans] = a[i];
        } else {
            int temp = upper_bound(dp1 + 1, dp1 + ans + 1, a[i]) - dp1;
            dp1[temp] = a[i];
        }
    }
    cout << ans << endl;
    return 0;
}
```

------

#### 前序遍历 (L2-004)

```c++
void f(int l, int r) {
    if (l > r) return;
    int tl = r, tr = l + 1;

    while (tl > l && a[tl] >= a[l]) tl--;
    while (tr <= r && a[tr] < a[r]) tr++;

    if (tr - tl != 1) return;
    f(l + 1, tl);
    f(tr, r);
    ans.push_back(a[l]);
}
```

------

#### 根据前序、中序输出层序遍历 (L2-006)

```c++
int build(int al, int ar, int il, int ir) {
    if (al > ar) return 0;
    int root = ir;
    while (in[root] != after[ar]) root--;
    int cnt = ir - root;

    l[after[ar]] = build(al, ar - cnt - 1, il, root - 1);
    r[after[ar]] = build(ar - cnt, ar - 1, root + 1, ir);
    return after[ar];
}

void print(int root) {
    queue<int> q;
    q.push(root);
    cout << root;
    while (!q.empty()) {
        int node = q.front();
        q.pop();
        if (l[node]) {
            cout << " " << l[node];
            q.push(l[node]);
        }
        if (r[node]) {
            cout << " " << r[node];
            q.push(r[node]);
        }
    }
}
```

------

#### 链式前向星 (L2-23)

```c++
int h[N], ne[N], e[N], idx;
memset(h, -1, sizeof h);

void add(int a, int b) {
    e[idx] = b;
    ne[idx] = h[a];
    h[a] = idx++;
}

bool dfs(int u) {
    st[u] = true;
    for (int i = h[u]; i != -1; i = ne[i]) {
        int j = e[i];
        if (!st[j]) {
            if (!dfs(j)) return false;
        } else if (color[u] == color[j]) {
            return false;
        }
    }
    return true;
}
```

------

#### 判断质数

```c++
bool is_prime(int x) {
    if (x < 2) return false;
    for (int i = 2; i <= sqrt(x); i++) {
        if (x % i == 0) return false;
    }
    return true;
}
```

------

#### 注意：

1. **多项式的除法** (L2-18) 处理细节。
2. **L2-28, L2-02, L2-22**: 对应的代码待补充或结合题目具体分析。

_____



当存图的数组大于1e5 * 1e5, 时, 可以用string来存图


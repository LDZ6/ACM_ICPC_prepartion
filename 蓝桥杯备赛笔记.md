## 知识点汇总

- **基础算法**：
  - 枚举、模拟、进制转换、前缀和、差分、贪心、双指针、二分、位运算、递归等。
- **排序**：
  - 冒泡排序、选择排序、插入排序、快速排序、归并排序等。
- **搜索**：
  - DFS基础&回溯、DFS-剪枝、记忆化搜索、折半搜索、三分搜索、模拟退火、最小圆覆盖等。
- **字符串**：
  - KMP&字符串哈希、Manacher、字典树基础、01tire、后缀数组、回文自动机等。
- **初等数论**：
  - 矩阵乘法、整除、同余、GCD、LCM、扩展欧几里得、素数判定、埃氏筛法、欧拉筛、唯一分解定理、快速幂、费马小定理、逆元、欧拉函数、欧拉降幂等。
- **组合数学**：
  - 计数原理、组合数常用计算方法、排列组合专项、集合与朴素容斥等。
- **复杂计数问题**：
  - FFT-快速傅里叶变换、NTT-快速数论变换、FWT-快速沃尔什变换、Burnside引理和Polya定理等。
- **高级数据结构**：
  - 并查集基础、单调栈&单调队列、ST表、树状数组基础、线段树、二维树状数组等。
- **数据结构—提高**：
  - 主席树、线段树标记永久化、线段树维护矩阵、线段树维护哈希、线段树分治、线段树合并等。
- **数据结构—进阶**：
  - FHQ-Treap、树的哈希、莫队、CDQ分治、可并堆、动态树LCT、Dsu On Tree等。
- **动态规划—线性与背包问题**：
  - 线性DP、LIS、LCS、二维DP、01背包、完全背包、多重背包、二维费用背包&分组背包等。
- **动态规划—提高**：
  - 自下而上树形DP、换根DP、区间DP、状压DP、数位DP等。
- **动态规划—进阶**：
  - 决策单调性优化DP、后效性DP、概率DP、数据结构优化DP、插头DP、凹凸性DP优化等。
- **图论—提高**：
  - 基础拓扑排序、Floyd & Dijkstra、Johnson、反图&虚拟源点技巧、Kruskal & Prim、倍增求LCA、Tarjan离线求LCA等。
- **图论—进阶**：
  - 二分图最大匹配、二分图最大权匹配、Dinic、最小费用最大流、模拟费用流、拆点等网络流建图技巧、差分约束系统等。
- **计算几何**：
  - 计算几何基础知识、点积和叉积、点和线的关系、线和线的关系、任意多边形面积的计算、皮克定理、Graham扫描法、极角排序等。



## 对应知识点题目

---

### **基础算法**
- [x] DNA序列修正
- [x] 三带一
- [x] 蘑菇炸弹
- [x] 依依的画作
- [x] 计算函数值
- [x] 穿越时空之门
- [ ] 帕鲁服务器崩坏【算法赛】
- [ ] 求和
- [ ] 子串简写
- [ ] 闪耀的灯光
- [ ] 重新排序
- [ ] 字符迁移【算法赛】
- [ ] 食堂
- [x] X进制减法
- [ ] 巧克力
- [x] 管道
- [ ] 平摊购买费用【算法赛】
- [x] 商品库存管理
- [ ] 成绩统计
- [ ] 拔河
- [ ] 最大子矩阵
- [ ] 异或和之和
- [x] 与或异或
- [x] 卓儿金字塔
- [ ] 小小蓝的异或消除

---

### **排序**
- [ ] 奇偶排序【算法赛】
- [x] 阿坤老师的独特瓷器
- [x] 封闭图形个数
- [x] 二进制王国【算法赛】
- [x] 星际争霸
- [ ] 宝石塔的亮度差异
- [ ] 摆玩具
- [x] 肖恩的排序
- [ ] 小蓝的零花钱
- [ ] 第k个数

---

### **搜索**
- [ ] 寻找食物储量
- [ ] 求函数零点
- [x] 特定准则
- [x] 模拟退火模板
- [x] 七段码
- [x] 皮诺亚曲线距离
- [ ] 迷宫与陷阱
- [ ] 小朋友崇拜圈
- [ ] 小怂爱水洼
- [ ] 数和游戏
- [ ] 01游戏
- [ ] 团建
- [ ] 五子棋对弈
- [ ] LITS游戏
- [ ] 黑白皇后
- [x] 子集之和
- [x] 点点之遥
- [x] 神奇花园
- [ ] 星际指挥官
- [ ] 用圆覆盖点

---

### **字符串**
- [ ] 幸运字符串
- [ ] 串的前缀
- [ ] boarder
- [x] 你也喜欢幸运字符串吗？
- [ ] 小蓝的01串
- [x] 回文字符串
- [ ] 最长回文前后缀
- [x] 诗歌双联
- [x] 异或和之差
- [ ] XOR最大值
- [ ] 反异或01串
- [ ] 小蓝的神秘图书馆
- [x] 暗号
- [ ] 最长公共前缀的和
- [x] 恰好出现最多的最长
- [x] 双回文
- [ ] 双倍回文
- [ ] 双双回文
- [ ] 小蓝的最大异或和
- [ ] 后缀数组模板

---

### **初等数论**
- [ ] 既约分数
- [ ] 阶乘约数
- [x] 逆元
- [x] 最大质因子个数【算法赛】
- [ ] 最小质因子之和(EasyVersion)
- [ ] 最小质因子之和(HardVersion)
- [ ] 小蓝的金牌梦【算法赛】
- [x] 阶乘的约数和
- [x] 寻找整数
- [x] 召唤数学精灵
- [ ] 拔苗助长【算法赛】
- [ ] 选素数
- [ ] 数的拆分
- [ ] GCD
- [ ] 宝石组合
- [ ] exgcd求逆元
- [x] exgcd进阶
- [x] 青蛙的约会
- [ ] 矩阵快速幂
- [x] 斐波那契数列
- [x] 获胜的概率2
- [x] 多项式的所有零点
- [x] 挑选武器
- [ ] 最大异或和
- [ ] 得到异或k的方案数
- [ ] 四转轮回，缘起缘灭
- [ ] 取模
- [ ] 同余方程组的最小解

---

### **组合数学**
- [x] 9面数字墙【算法赛】
- [x] 数字求和【算法赛】
- [ ] 智力测试
- [x] 涂涂画画【算法赛】
- [ ] 求解组合数
- [ ] 求解组合数2
- [x] 匹配二元组的数量【算法赛】
- [x] 困局
- [x] 机器人的选法总数
- [ ] 2023
- [ ] 互质
- [ ] 魔法森林的秘密

---

### **复杂计数问题**
- [x] 多项式卷积
- [x] 子集的和与积
- [ ] 子集卷积
- [ ] 小浩的染色图问题

---

### **高级数据结构**
- [x] 皇家守卫【算法赛】
- [x] 附近最小
- [x] 龙骑士军团【算法赛】
- [x] 最大区间
- [x] 四元组问题
- [ ] 百亿富翁
- [ ] 游戏
- [ ] 子矩阵
- [ ] 修改数组
- [ ] 合根植物
- [x] 封印宝石
- [x] 植物生命力
- [x] 双向排序
- [x] 最优清零方案
- [x] 蓝桥田地

---

### **数据结构—提高**
- [ ] 升级电缆【算法赛】
- [x] 三角序列
- [x] 选糖果【算法赛】
- [x] 小蓝的逆序对问题【算法赛】
- [x] 标记永久化练习题
- [x] 相似字符串
- [x] 线段树维护哈希
- [x] 回文带修判断
- [x] period判断
- [x] 蓝桥地图

---

### **数据结构—进阶**
- [ ] 冰山
- [ ] 重复的数
- [ ] HH的项链
- [x] 拦截导弹（加强）
- [ ] 最大差值
- [ ] 序列维护
- [ ] 弹飞绵羊
- [x] 魔法森林
- [x] 探险之旅
- [x] 树形植物的颜色统计
- [x] 机器人工厂的组织结构
- [x] 狮子王国的聚会
- [ ] 神秘森林的亲戚问题
- [ ] 森林守护者

---

### **动态规划—线性与背包问题**
- [x] 接龙数列
- [ ] 火车运输
- [ ] 简单的LIS问题【算法赛】
- [ ] 砝码称重
- [ ] 划分
- [ ] 明日方舟大作战！
- [x] 冒险岛的魔法果实
- [x] 2022
- [x] 火柴棒数字
- [ ] 背包与魔法
- [ ] 魔法巡游
- [ ] 赤壁之战【算法赛】
- [ ] 数字三角形
- [ ] 积木画
- [ ] 数组切分

---

### **动态规划—提高**
- [x] 左孩子右兄弟
- [x] 取气球【算法赛】
- [x] 大臣的旅费
- [ ] 蓝桥舞会
- [x] 树的连边II
- [ ] 回路计数
- [ ] 糖果
- [x] 矩阵计数
- [x] 星球
- [x] 覆盖
- [ ] 再遇白骨精【算法赛】
- [x] 二进制问题
- [x] 好数之和
- [x] 十六夜咲夜的题目
- [x] 数字迷宫
- [ ] 石子合并
- [ ] 更小的数
- [ ] 课上小游戏

---

### **动态规划—进阶**
- [ ] 最长不下降子序列
- [x] 单词分类
- [x] 作物杂交
- [x] 分果果
- [x] 居民集会
- [x] 小蓝玩游戏
- [x] 苏苏的01字符串
- [x] 小蓝的飞行棋
- [ ] 期望次数【算法赛】
- [ ] 神奇游乐园

---

### **图论—提高**
- [ ] 路径
- [ ] 出租车
- [ ] 出差
- [x] 环境治理
- [x] 染色时间
- [x] 小蓝组网
- [x] 魔法阵
- [ ] 限高杆
- [ ] 走多远
- [x] 小朋友崇拜圈
- [x] 城邦
- [ ] 通电
- [ ] 电动车
- [ ] 砍树
- [ ] 外卖员的小爱好【算法赛】

---

### **图论—进阶**
- [ ] 二分图的判定
- [ ] 婚礼
- [ ] 月老
- [x] 黑白图染色方案
- [x] 二分图最大匹配
- [x] 战士与巫师
- [ ] 矩阵游戏
- [ ] 长方形的覆盖
- [ ] 二分图最大权匹配
- [x] 最短的线段匹配
- [ ] 组队游戏
- [ ] 网格覆盖
- [ ] 网络最大流
- [x] 网格染色最小代价
- [x] 最大权闭合子图
- [x] 方格取数
- [ ] 最小费用流
- [ ] 最小组队费用
- [ ] 扩充网络的最小代价
- [ ] 货物调配

---

### **计算几何**
- [ ] 叉积模板
- [ ] 点到线段的距离
- [ ] 直线相交的判断
- [x] 飞机轰炸
- [x] 线段相交的判断
- [ ] 视力检查
- [x] 二维凸包模板
- [x] 最大的三角形
- [x] 凸包的包含
- [x] 最远的点

---



## 真题

##### 飞机降落

[蓝桥杯2023年第十四届省赛真题-飞机降落 - C语言网](https://www.dotcpp.com/oj/problem3151.html)(dfs)

```c++
#include <iostream>
using namespace std;

const int MAXN = 100010;
struct plant
{
    int t, d, l;
} p[MAXN];
bool used[MAXN];
int n, t;
bool flag;
void dfs(int x, int last)
{
    if (x == n)
    {
        flag = true;
        return;
    }
    for (int i = 0; i < n; i++)
    {
        if (!used[i])
        {
            if (p[i].t + p[i].d < last)
                continue;
            used[i] = true;
            dfs(x + 1, max(last, p[i].t) + p[i].l);
            used[i] = false;
            if (flag)
                return;
        }
    }
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    scanf("%d", &t);
    while (t--)
    {
        scanf("%d", &n);
        flag = false;
        for (int i = 0; i < n; i++)
        {
            scanf("%d %d %d", &p[i].t, &p[i].d, &p[i].l);
        }
        dfs(0, 0);
        if (flag)
            printf("YES\n");
        else
            printf("NO\n");
    }
    return 0;
}
```

##### **接龙数列**

[蓝桥杯2023年第十四届省赛真题-接龙数列 - C语言网](https://www.dotcpp.com/oj/problem3152.html)(动态规划)

```c++
#include <iostream>
#include <cstring>
#include <algorithm>

using namespace std;
int g[10] = {0};
int main()
{
    int res = 0;
    int n;
    cin >> n;
    for (int i = 0; i < n; ++i)
    {
        string s;
        cin >> s;
        int l = s[0] - '0', r = s[s.size() - 1] - '0';
        int f = g[l] + 1;
        g[r] = max(g[r], f);
        res = max(res, f);
    }
    cout << n - res << endl;
    return 0;
}
```

##### **岛屿个数**

[蓝桥杯2023年第十四届省赛真题-岛屿个数 - C语言网](https://www.dotcpp.com/oj/problem3153.html)

```c++
#include <iostream>
#include <cstring>
using namespace std;
const int N = 60;
int g[N][N];
bool st[N][N];
int dx4[] = {0, 0, 1, -1}, dy4[] = {1, -1, 0, 0};								//四个方向的向量
int dx8[] = {-1, -1, -1, 0, 1, 1, 1, 0}, dy8[] = {-1, 0, 1, 1, 1, 0, -1, -1};	//八个方向的向量
int cnt, T, m, n;
 
inline void dfs1(int x, int y) {
	st[x][y] = true;
	for (int i = 0; i < 4; i++) {
		int nx = x + dx4[i], ny = y + dy4[i];
		//没有走过，符合陆地的范围并且是陆地
		if (!st[nx][ny] && x >= 1 && x <= m && y >= 1 && y <= n && g[nx][ny]) dfs1(nx, ny);
	}
}
 
inline void dfs2(int x, int y) {		//八方向搜索
	st[x][y] = true;
	for (int i = 0; i < 8; i++) {
		int nx = x + dx8[i], ny = y + dy8[i];
		//没有走过，符合海洋的范围
		if (!st[nx][ny] && nx >=0 && nx <= m+1 && ny >= 0 && ny <= n+1) {
			if (!g[nx][ny]) dfs2(nx, ny);		//下一步是海洋继续搜索
			else if (g[nx][ny]) {				//下一步是陆地就将相邻的所有陆地设置为走过的状态
				dfs1(nx, ny);
				cnt += 1;						//并且岛屿数量加一
			}
		}
	}
}
 
int main() {
	ios::sync_with_stdio(false);				//取消IO同步流
	cin.tie(0);									//解除cin&cout绑定
	cin >> T;
	while (T--) {
		cin >> m >> n;
		memset(g, 0, sizeof g);					//重新分配地图
		memset(st, false, sizeof st);			//重新分配地图状态
		cnt = 0;
		for (int i = 1; i <= m; i++) {
			for (int j = 1; j <= n; j++) {
				char x;cin >> x;
				g[i][j] = x - '0';
			}
		}
		dfs2(0, 0);
		cout << cnt << endl;
	}
}
```

##### 子串简写

[蓝桥杯2023年第十四届省赛真题-子串简写 - C语言网](https://www.dotcpp.com/oj/problem3154.html)

```c++
//子串简写：二分
#include<bits/stdc++.h>
#define int long long
using namespace std;

void solve()
{
	int k;string s;
	char c1, c2;
	cin >> k >> s >> c1 >> c2;
	
	vector<int>pc1;//存储c1的位置

	int ans = 0;	
	for(int i = 0; i < s.size(); i ++)
	{
		if(s[i] == c1)
			pc1.push_back(i);
		
		if(s[i] == c2)
		{
		    if(i - k + 1 < 0 || !pc1.size())
		        continue;

		    int l = 0, r = (int)pc1.size() - 1;
		    while(l < r)
		    {
		    	int mid = l + r + 1 >> 1;
		    	if(pc1[mid] <= (i - k + 1))
		    		l = mid;
		    	else
		    		r = mid - 1;
		    }
		    
		    if(pc1[l] <= i - k + 1)
		    	ans += (l + 1);
		}
	}

	cout << ans << endl;
}

signed main()
{
	ios::sync_with_stdio(0);
	cin.tie(0);
	int t = 1;
	// cin >> t;
	while(t--)
	solve();
}
```

##### **整数删除**

[蓝桥杯2023年第十四届省赛真题-整数删除 - C语言网](https://www.dotcpp.com/oj/problem3155.html)

```c++
#include <bits/stdc++.h>
using namespace std;
const int N = 5e5 + 10;
#define ll long long
int n, k;
int pre[N], ne[N];
ll cnt[N], a[N], t;
priority_queue<pair<ll, int>, vector<pair<ll, int>>, greater<pair<ll, int>>> q;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    scanf("%d %d", &n, &k);
    for (int i = 1; i <= n; i++)
    {
        scanf("%lld", &t);
        q.push({t, i});
        pre[i] = i - 1;
        ne[i] = i + 1;
    }
    while (q.size() > n - k)
    {
        ll x = q.top().first;
        int id = q.top().second;
        q.pop();
        if (cnt[id])
        {
            q.push({x + cnt[id], id});
            cnt[id] = 0;
        }
        else
        {
            int left = pre[id], right = ne[id];
            cnt[left] += x;
            cnt[right] += x;
            pre[right] = left;
            ne[left] = right;
        }
    }
    while (q.size())
    {
        ll x = q.top().first;
        int id = q.top().second;
        q.pop();
        a[id] = x + cnt[id];
    }
    for (int i = 1; i <= n; i++)
    {
        if (a[i])
            cout << a[i] << " ";
    }
    return 0;
}
```

##### **景区导游(LCA模版题)**

[蓝桥杯2023年第十四届省赛真题-景区导游 - C语言网](https://www.dotcpp.com/oj/problem3156.html)

```c++
#include<iostream>
#include<cmath>
#include<queue>
#include<cstdio>
#include<algorithm>
using namespace std;
const int N=3e5+10;
int fu[N][25],d[N];
long long f[N][25];
int h[N],e[N],ne[N],idx;
long long w[N];
int a[N];
void add(int x,int y,long long z)
{
	e[idx]=y;
	w[idx]=z;
	ne[idx]=h[x];
	h[x]=idx++;
}
void dfs(int x,int fa,long long dis,int hh)
{
	d[x]=hh;
	fu[x][0]=fa;
	f[x][0]=dis;
	for(int i=1;i<=23;i++)
	{
		f[x][i]=f[x][i-1]+f[fu[x][i-1]][i-1];
		fu[x][i]=fu[fu[x][i-1]][i-1];
	}
	for(int i=h[x];i!=-1;i=ne[i])
	{
		int j=e[i];
		if(j==fa) continue;
		dfs(j,x,w[i],hh+1);
	}
}
long long lca(int a,int b)
{
	if(d[a]<d[b]) swap(a,b);
	long long ans=0;
	for(int i=23;i>=0;i--)
	if(d[fu[a][i]]>=d[b])
	{
		ans+=f[a][i];
		a=fu[a][i];
	}
	if(a==b) return ans;
	for(int i=23;i>=0;i--)
	if(fu[a][i]!=fu[b][i])
	{
		ans+=f[a][i]+f[b][i];
		a=fu[a][i];b=fu[b][i];
	}
	ans+=f[a][0]+f[b][0];
	return ans;
}
int main()
{
	int n,k;
	cin>>n>>k;
	for(int i=1;i<=n;i++)
		h[i]=-1;
	for(int i=1;i<n;i++)
	{
		int u,v;
		long long z;
		scanf("%d%d%lld",&u,&v,&z);
		add(u,v,z);add(v,u,z);
	}
	dfs(1,0,0,1);
	long long ans=0;
	for(int i=1;i<=k;i++)
	{
		scanf("%d",&a[i]);
		if(i>1) ans+=lca(a[i],a[i-1]);
	}
	for(int i=1;i<=k;i++)
	{
		if(i==1) printf("%lld ",ans-lca(a[1],a[2]));
		else if(i==k) printf("%lld ",ans-lca(a[k],a[k-1]));
		else
			printf("%lld ",ans-lca(a[i],a[i-1])-lca(a[i],a[i+1])+lca(a[i-1],a[i+1]));
	}
	return 0;
}
```

##### **好数**

[蓝桥杯2024年第十五届省赛真题-好数 - C语言网](https://www.dotcpp.com/oj/problem3209.html)

```c++
#include<iostream>
using namespace std;

// 判断一个数字是否满足"好数"的条件
bool judge(int a)
{
    int count = 0, good = 0;
    int ori_a = a;  // 保存原始数字，以便在后续操作中再次使用

    // 计算数 a 的位数
    while (a != 0)
    {
        count++;      // 每次除以 10，位数增加
        a /= 10;      // 去除数字的最后一位
    }

    a = ori_a;  // 重置 a 为原始值，以便再次遍历数字的每一位

    // 遍历每一位
    for (int i = 1; i <= count; i++)
    {
        int current = a % 10;  // 获取数字的当前位（个位、十位等）

        // 判断数字的当前位是否符合要求
        // 偶数位并且当前数字是偶数，或者奇数位并且当前数字是奇数
        if ((i % 2 == 0) && (current % 2 == 0))  // 偶数位并且当前数字为偶数
            good++;  // 满足条件，计数增加
        else if ((i % 2 == 1) && (current % 2 == 1))  // 奇数位并且当前数字为奇数
            good++;  // 满足条件，计数增加

        a /= 10;  // 去除数字的当前位，处理下一位
    }

    // 如果满足的"好数"位数等于数字的总位数，说明是一个符合条件的好数
    return (good == count);
}

int main()
{
    int N;
    cin >> N;  // 输入 N，表示我们要统计从 1 到 N 的所有数字

    int ans = 0;  // 用于存储符合条件的好数的数量

    // 遍历 1 到 N 的所有数字，判断每个数字是否为好数
    for (int i = 1; i <= N; i++)
    {
        if (judge(i))  // 如果当前数字 i 是好数
            ans++;      // 增加计数
    }

    // 输出符合条件的好数的数量
    cout << ans;
    return 0;  // 程序结束
}
```

##### **砍树**

[蓝桥杯2023年第十四届省赛真题-砍树 - C语言网](https://www.dotcpp.com/oj/problem3157.html)

```c++
//砍树：树上差分 + 最近公共祖先
#include<bits/stdc++.h>
using namespace std;
typedef pair<int, int> pii;
const int N = 1e5 + 10;
int n, m;
int w[N];//记录每个点的点权。

map<pii,int>id;
vector<int>edge[N];

int siz[N], dep[N], fa[N], son[N], top[N];

void dfs1(int u, int father)
{
	siz[u] = 1, dep[u] = dep[father] + 1;
	fa[u] = father;
	for(int i = 0; i < edge[u].size(); i ++)
	{
		int s = edge[u][i];
		if(s == fa[u])
			continue;
		dfs1(s, u);
		siz[u] += siz[s];
		if(siz[son[u]] < siz[s])
			son[u] = s;
	}
}

void dfs2(int u, int t)
{
	top[u] = t;
	if(son[u] == 0)
		return;
	dfs2(son[u], t);
	for(int i = 0; i < edge[u].size(); i ++)
	{
		int s = edge[u][i];
		if(s == fa[u] || s == son[u])
			continue;
		dfs2(s, s);
	}
}

int lca(int x, int y)
{
	while(top[x] != top[y])
	{
		if(dep[top[x]] < dep[top[y]])
			swap(x, y);

		x = fa[top[x]];
	}
	return dep[x] < dep[y] ? x : y;
}

void cal_sum(int u, int father)
{
	for(int i = 0; i < edge[u].size(); i ++)
	{
		int son = edge[u][i];
		if(son == father)
			continue;
		cal_sum(son, u);
		w[u] += w[son];
	}
}

void solve()
{
	 cin >> n >> m;
	 for(int i = 1; i <= n - 1; i ++)
	 {
	 	int x, y; cin >> x >> y;
	 	edge[x].push_back(y);
	 	edge[y].push_back(x);
	 	id[{x, y}] = i;
	 	id[{y, x}] = i;
	 }	

	 dfs1(1, 0);
	 dfs2(1, 1);

	 for(int i = 0; i < m; i ++)
	 {
	 	int a, b; cin >> a >> b;

	 	//做树上差分
	 	w[a] ++, w[b] ++;
	 	w[lca(a, b)] -= 2;
	 }


	 cal_sum(1, 0);


	 int ans = -1;
	 for(int i = 1; i <= n; i ++)
	 {
	 	if(w[i] == m)
	 	{
	 		int ID = id[{i, fa[i]}];
	 		ans = max(ans, ID);
	 	}
	 }
	 cout << ans << endl;
}

signed main()
{
	ios::sync_with_stdio(0);
	cin.tie(0);
	int t = 1;
	// cin >> t;
	while(t--)
	solve();
}
```

##### **分布式队列**

[蓝桥杯2024年第十五届省赛真题-分布式队列 - C语言网](https://www.dotcpp.com/oj/problem3227.html)

```c++
#include <bits/stdc++.h>
using namespace std;
const int N = 20;
int q[N];
int n;
void query()
{
    int result = q[0];
    for (int i = 1; i < n; i++)
    {
        if (q[i] < result)
            result = q[i];
    }
    cout << result << endl;
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cin >> n;
    char s[1000];
    while (cin >> s)
    {
        if (strcmp(s, "add") == 0)
        {
            int m;
            cin >> m;
            q[0]++;
        }
        else if (strcmp(s, "sync") == 0)
        {
            int m;
            cin >> m;
            if (q[m] < q[0])
                q[m]++;
        }
        else
        {
            query();
        }
    }
    return 0;
}
```

##### **R 格式**

[蓝桥杯2024年第十五届省赛真题-R 格式 - C语言网](https://www.dotcpp.com/oj/problem3210.html)

```c++
#include <iostream>
#include <cstring>
#include <algorithm>
#include <cstdio>
#include <vector>

using namespace std;

int n;
string str;

vector<int> mul(vector<int> A, int b)
{
    vector<int> C;
    for (int i = 0, t = 0; i < A.size() || t; ++ i )
    {
        if (i < A.size())
            t += A[i] * b;
        C.push_back(t % 10);
        t /= 10;
    }
    while (C.size() > 1 && C.back() == 0)
        C.pop_back();
    return C;
}

vector<int> add(vector<int> A, vector<int> B)
{
    vector<int> C;
    for (int i = 0, t = 0; i < A.size() || i < B.size() || t; ++ i )
    {
        if (i < A.size())
            t += A[i];
        if (i < B.size())
            t += B[i];
        C.push_back(t % 10);
        t /= 10;
    }
    
    return C;
}

int main()
{
    cin >> n >> str;
    
    int k = str.size() - 1;
    while (str[k] != '.')
        k --;
    str.erase(k, 1);
    
    k = str.size() - k;
    
    vector<int> A;
    for (int i = str.size() - 1; i >= 0; -- i )
        A.push_back(str[i] - '0');
    
    for (int i = 1; i <= n; ++ i )
        A = mul(A, 2);
    
    int p = A[k - 1];
    reverse(A.begin(), A.end());
    while (k -- )
        A.pop_back();
    reverse(A.begin(), A.end());
    
    if (p >= 5)
        A = add(A, {1});
    
    for (int i = A.size() - 1; i >= 0; -- i )
        cout << A[i];

    return 0;
}
```



## 常用语法&函数

#### **字符串去重**

- 使用 `std::sort` 和 `std::unique` 去除字符串中的重复字符。
- `std::unique` 返回去重后的末尾迭代器，可以通过 `erase` 删除重复部分。

```cpp
// 字符串去重
void removeDuplicateCharacters(string &a) {
    sort(a.begin(), a.end());  // 排序字符串
    a.erase(unique(a.begin(), a.end()), a.end());  // 去重并删除重复区间
}
```

**示例用法**：

```cpp
string a = "11322";
removeDuplicateCharacters(a);
cout << a;  // 输出：123
```



#### **Vector 去重**

- 使用 `std::sort` 和 `std::unique` 删除 `vector` 中的重复元素。

```cpp
// Vector 去重
void removeDuplicateInVector(vector<int> &q) {
    sort(q.begin(), q.end());  // 排序
    q.erase(unique(q.begin(), q.end()), q.end());  // 去重并删除重复区间
}
```

**示例用法**：

```cpp
vector<int> q = {1, 1, 2, 3};
removeDuplicateInVector(q);
for (auto i : q) cout << i << ' ';  // 输出：1 2 3
```



#### 读取整行字符串

- 使用 `getline()` 来读取包含空格的字符串。

```cpp
// 使用 getline() 读取整行字符串
void readLineExample() {
    string s;
    getline(cin, s);  // 从标准输入中读取一行
    cout << s << endl;  // 输出输入的内容
}
```



#### 字符串替换

- 使用 `replace()` 方法替换字符串中的某些部分。

```cpp
// 替换字符串内容
void replaceExample() {
    string s = "hello,world!";
    s.replace(6, 5, "girl");  // 结果："hello,girl!"
    s.replace(s.begin() + 6, s.begin() + 10, "boy");  // 结果："hello,boy!"
}
```



#### **插入字符串或字符**：

- 使用 `insert()` 方法在字符串中指定位置插入字符或子字符串。

```cpp
// 插入字符串或字符
void insertExample() {
    string s = "12345";
    s.insert(2, "abc");  // 结果："12abc345"
    s.insert(s.begin() + 1, 'x');  // 结果："1x2abc345"
}
```



#### 删除字符串内容

- 使用 `erase()` 删除指定位置的字符或子串。

```cpp
// 删除字符串内容
void eraseExample() {
    string s = "123456789";
    s.erase(s.begin() + 1);  // 结果："13456789"
    s.erase(1, 3);  // 结果："16789",删除3个字符
    s.erase(s.begin() + 2, s.end() - 2);  // 结果："1289"
}
```



#### 字符串反转

- 使用 `reverse()` 反转字符串。

```cpp
// 字符串反转
void reverseExample() {
    string s = "12345";
    reverse(s.begin(), s.end());  // 结果："54321"
}
```



#### 子串截取

- 使用 `substr()` 截取字符串的部分。

```cpp
// 子串截取
void substringExample() {
    string s = "12345";
    string sub = s.substr(3, 1);  // 结果："4"
}
```



#### 字符串查找

- 使用 `find()` 查找子串或字符。

```cpp
// 字符串查找
void findExample() {
    string s = "dog bird chicken bird cat";
    size_t pos = s.find("chicken");  // 结果：9
    pos = s.find('i', 6);  // 结果：11
    pos = s.rfind("bird");  // 结果：16
    pos = s.rfind('i');  // 结果：18
}
```



#### **max_element 和 min_element**

- 使用 `std::max_element` 和 `std::min_element` 查找最大值和最小值。

```cpp
// 查找最大最小值
int position = max_element(a, a + n) - a;
int data = *max_element(a, a + n);

int position = max_element(v.begin(), v.end()) - v.begin();
int it = *max_element(v.begin(), v.end());
```



#### stable_sort 保序排序

- `stable_sort` 保证排序时相等元素的相对顺序不变。



#### map 和 unordered_map 

- `map` 根据键值自动排序，`unordered_map` 无序。
- 用 `find()` 查找键值对，`erase()` 删除元素。

```cpp
// 使用 map 查找和删除
map<int, int> mp;
mp[1] = 2;
mp[2] = 3;
auto it = mp.find(2);
cout << it->second;  // 输出 3
mp.erase(2);  // 删除键 2 的元素
```



#### **字符串与字符的转换**

- 使用 `c_str()` 将 `string` 转换为 C 风格字符串。

```cpp
const char* c;
string s = "1234";
c = s.c_str();
cout << c;  // 输出：1234
```



#### 结构体与排序

- 定义结构体并使用重载运算符或自定义排序函数对结构体数组进行排序。

```cpp
struct Student {
    string name;
    int grade;
    float high;
    bool operator < (const Student &tmp) const {
        return grade < tmp.grade;  // 按成绩从小到大排序
    }
};

struct Student stu[100];
sort(stu, stu + 100);
```



#### 自定义排序函数

- 使用 `cmp1` 自定义排序规则，按成绩排序，如果成绩相同则按身高排序。

```cpp
bool cmp1(Student a, Student b) {
    if (a.grade != b.grade)
        return a.grade < b.grade;
    else
        return a.high < b.high;
}

sort(stu, stu + 100, cmp1);  // 使用自定义排序
```



#### atoi 与 atof 

- `atoi` 将字符串转换为整数，`atof` 将字符串转换为浮点数。

```cpp
const char* c;
string s;
cin >> s;
c = s.c_str();
int n = atoi(c);  // 将字符串转换为整数
cout << n << endl;  // 输出 -100
```



#### find 与 erase 

- `find` 查找子串，`erase` 删除字符串的部分内容。

```cpp
// 查找字符串
string s = "ifisfs";
if (s.find("is") != string::npos)
    cout << s.find("is");  // 输出 2

// 删除字符串部分
string str = "welcome to my blog";
str.erase(11, 3);  // 删除 "my"
```

------



#### 容器操作

- #### **Set**

- **set** 是一个自动排序的容器，不允许重复元素。
- 访问元素时使用迭代器。
- `insert()` 用于插入元素，返回一个 pair，其中第二个元素表示插入是否成功。

```cpp
set<int> s;
s.insert(1);  // 插入元素 1
s.insert(2);  // 插入元素 2
s.insert(1);  // 不插入重复元素

for (auto it = s.begin(); it != s.end(); ++it) {
    cout << *it << " ";  // 输出：1 2
}
for(auto i : s) {
    cout << i << " ";
}
```

- `find()` 用于查找元素，返回一个迭代器。
- `erase()` 用于删除元素。

```cpp
auto it = s.find(2);  // 查找元素 2
if (it != s.end()) {
    s.erase(it);  // 删除元素 2
}
```



- #### Map

- **map** 是一个按键自动排序的容器，键值对存储。
- `find()` 返回指向元素的迭代器，若找不到返回 `map::end()`。
- `operator[]` 用于访问和插入元素。

```cpp
map<int, string> m;
m[1] = "one";  // 插入键值对
m[2] = "two";  // 插入键值对
cout << m[1] << endl;  // 输出 "one"

auto it = m.find(2);
if (it != m.end()) {
    cout << it->second << endl;  // 输出 "two"
}
for (auto &i : m) {
    cout << i.first << " " << i.second << endl;
}
for (auto [a, b] : m) {
	cout << a << " " << b << endl;
}
```



- #### Vector

- **vector** 是一个动态数组，允许随机访问。
- 可以通过 `push_back()` 插入元素，通过 `size()` 获取元素个数。
- 支持索引访问元素。

```cpp
vector<int> v;
v.push_back(1);  // 插入元素 1
v.push_back(2);  // 插入元素 2

for (int i = 0; i < v.size(); ++i) {
    cout << v[i] << " ";  // 输出：1 2
}
```



- #### Queue

- **queue** 是一个先进先出（FIFO）的容器，常用于任务排队。
- 使用 `push()` 插入元素，`front()` 访问队头元素，`pop()` 移除队头元素。

```cpp
queue<int> q;
q.push(1);  // 插入元素 1
q.push(2);  // 插入元素 2
cout << q.front() << endl;  // 输出：1
q.pop();  // 移除队头元素

cout << q.front() << endl;  // 输出：2
```



- #### Priority Queue

- **priority_queue** 是一个优先队列，元素根据优先级自动排序（默认最大堆）。
- `push()` 插入元素，`top()` 返回堆顶元素，`pop()` 移除堆顶元素。

```cpp
priority_queue<int> pq;
pq.push(1);  // 插入元素 1
pq.push(3);  // 插入元素 3
pq.push(2);  // 插入元素 2

cout << pq.top() << endl;  // 输出：3 (最大元素)
pq.pop();  // 移除堆顶元素

cout << pq.top() << endl;  // 输出：2
```

------



#### 字符串格式输出

- #### **printf 格式化输出**

- ```
  printf
  ```

   支持多种格式说明符： 

  - `%d`：输出整数（`int`）。
  - `%f`：输出浮点数（`float` 或 `double`）。
  - `%s`：输出字符串（`char*` 或 `const char*`）。
  - `%c`：输出字符（`char`）。
  - `%x`：输出十六进制（`int`）。
  - `%o`：输出八进制（`int`）。
  - `%.2f`：输出浮点数，保留 2 位小数。

```cpp
int a = 10;
float pi = 3.14159;
const char* str = "Hello, World!";

printf("Integer: %d\n", a);  // 输出：Integer: 10
printf("Float: %.2f\n", pi);  // 输出：Float: 3.14
printf("String: %s\n", str);  // 输出：String: Hello, World!
```



- #### cout 格式化输出

- **setprecision()**：设置浮点数输出的精度。
- **fixed 和 scientific**：控制浮点数输出的格式（定点表示或科学计数法）。

```cpp
#include <iostream>
#include <iomanip>  // 包含 setprecision 等格式控制函数

double pi = 3.14159265358979;

cout << fixed << setprecision(2) << pi << endl;  // 输出：3.14
cout << scientific << setprecision(2) << pi << endl;  // 输出：3.14e+00
```

- **setw()**：设置输出的字段宽度（对齐）。
- **left, right 和 internal**：控制输出的对齐方式。

```cpp
cout << setw(10) << left << "Hello" << endl;  // 输出：Hello     
cout << setw(10) << right << "Hello" << endl;  // 输出：     Hello
cout << setw(10) << internal << 123 << endl;  // 输出：    123
```



- #### **cin 输入格式**

- **cin >> variable**：标准输入，用于读取数据。
- **getline()**：读取整行数据，包括空格。

```cpp
int num;
string str;

cin >> num;  // 读取整数
cout << num << endl;  // 输出整数

cin.ignore();  // 清除输入缓冲区，防止 getline() 读取到残留的换行符
getline(cin, str);  // 读取一行字符串
cout << str << endl;  // 输出字符串
```

- **跳过空格**：`cin` 会自动跳过空格、制表符和换行符，直到遇到有效字符。

```cpp
int x, y;
cin >> x >> y;  // 跳过空格并读取整数 x 和 y
```

------



- #### 其他常见输出

- ##### **setfill() 与 setw()**：填充空格

```cpp
int num = 42;
cout << setw(6) << setfill('0') << num << endl;  // 输出：000042
```

- ##### **boolalpha 与 noboolalpha**：输出布尔值

```cpp
bool flag = true;
cout << boolalpha << flag << endl;  // 输出：true
cout << noboolalpha << flag << endl;  // 输出：1
```

- ##### **showbase 与 noshowbase**：显示或不显示数字基数（如 `0x` 或 `0`）

```cpp
int num = 255;
cout << showbase << hex << num << endl;  // 输出：0xff
cout << noshowbase << hex << num << endl;  // 输出：ff
```

- ##### **uppercase 与 nouppercase**：控制十六进制字母的大小写

```cpp
cout << uppercase << hex << 255 << endl;  // 输出：FF
cout << nouppercase << hex << 255 << endl;  // 输出：ff
```



#### priority_queue

- ##### **priority_queue 基本用法**

```cpp
#include <iostream>
#include <queue>
using namespace std;

int main() {
    priority_queue<int> pq;  // 默认最大堆

    pq.push(10);  // 插入元素 10
    pq.push(30);  // 插入元素 30
    pq.push(20);  // 插入元素 20

    // 打印并移除堆顶元素
    while (!pq.empty()) {
        cout << pq.top() << " ";  // 打印堆顶元素
        pq.pop();  // 移除堆顶元素
    }

    // 输出：30 20 10
    return 0;
}
```

------



- ##### **最小堆（greater<type>）**

```cpp
#include <iostream>
#include <queue>
#include <functional>  // greater
using namespace std;

int main() {
    priority_queue<int, vector<int>, greater<int>> pq;  // 最小堆

    pq.push(10);
    pq.push(30);
    pq.push(20);

    // 打印并移除堆顶元素
    while (!pq.empty()) {
        cout << pq.top() << " ";  // 打印堆顶元素
        pq.pop();  // 移除堆顶元素
    }

    // 输出：10 20 30
    return 0;
}
```

------



- ##### 使用自定义结构体的优先队列

假设我们有一个 `Person` 结构体，包含 `name` 和 `age`，并且我们希望按照年龄（`age`）的大小来进行优先级排序。下面是如何实现自定义结构体优先队列的步骤。

```cpp
#include <iostream>
#include <queue>
#include <vector>
#include <string>
using namespace std;

// 自定义结构体
struct Person {
    string name;
    int age;

    // 构造函数
    Person(string n, int a) : name(n), age(a) {}
};

// 自定义比较器：按年龄升序排序（最小堆）
struct CompareAge {
    bool operator()(const Person& p1, const Person& p2) {
        return p1.age > p2.age;  // 如果 p1.age > p2.age，p1 的优先级低
    }
};

int main() {
    // 创建优先队列，使用自定义比较器
    priority_queue<Person, vector<Person>, CompareAge> pq;

    // 插入元素
    pq.push(Person("Alice", 25));
    pq.push(Person("Bob", 30));
    pq.push(Person("Charlie", 20));

    // 打印并移除堆顶元素
    while (!pq.empty()) {
        Person top = pq.top();  // 获取堆顶元素
        cout << top.name << " (" << top.age << ")" << endl;
        pq.pop();  // 移除堆顶元素
    }

    // 输出：
    // Charlie (20)
    // Alice (25)
    // Bob (30)

    return 0;
}
```



- ##### 使用 `std::pair` 的 `first` 元素的优先队列

```c++
#include <iostream>
#include <queue>
#include <utility> // for std::pair

int main() {
    // 定义一个优先队列，使用 std::less 来构造最大堆
    auto cmp = [](const std::pair<int, int>& a, const std::pair<int, int>& b) {
        return a.first < b.first; // 根据 first 元素排序
    };

    std::priority_queue<std::pair<int, int>, std::vector<std::pair<int, int>>, decltype(cmp)> pq(cmp);

    // 插入元素
    pq.push({1, 10});
    pq.push({3, 30});
    pq.push({2, 20});

    // 访问并删除元素
    while (!pq.empty()) {
        std::pair<int, int> top = pq.top();
        std::cout << "(" << top.first << ", " << top.second << ")" << std::endl;
        pq.pop();
    }

    return 0;
}
```



#### `fill` 和 `memset`

- ##### `fill` 函数


```cpp
std::vector<int> vec(5); // 创建一个包含5个元素的vector
// 使用fill将vector中的所有元素设置为42
std::fill(vec.begin(), vec.end(), 42);
for (int i : vec) {
    std::cout << i << " "; // 输出: 42 42 42 42 42
}
```

- #####  `memset` 函数


```cpp
char str[10];
// 使用memset将str数组的所有字节设置为 'A'
std::memset(str, 'A', 10);
for (int i = 0; i < 10; ++i) {
    std::cout << str[i]; // 输出: AAAAAAAAAA
}
```

- `memset` 是按字节操作的，因此适用于原始内存块（如数组、结构体等）。
- 由于是按字节操作，`memset` 不适合用于初始化非字符类型的数组（如 `int` 数组），因为可能会导致未定义行为。

- 如果用于非字符类型的数组，`memset` 可能会导致意外的结果。例如：
  ```cpp
  int arr[10];
  std::memset(arr, 1, sizeof(arr)); // 错误：每个int元素的每个字节都被设置为1，而不是整个int被设置为1
  ```

